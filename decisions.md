# Decisions for Mitos

<!-- This file is managed by mitos. LLM integration: see .mitos/skill.md once V5 ships. -->
<!-- DO NOT MODIFY ABOVE THIS LINE -->

## SAMPLE FORMAT — auto-restored by mitos sync, do not modify or delete

### example-slug

**Decided:** We will use SQLite in WAL mode for the graph store.
**Rejected:** pgvector (too heavy for local-first portfolio audience), sqlite-vec (defer to v0.2 to preserve V1 ship date).
**Mechanisms:** sqlite, wal-mode
**Scope:** substrate
**Context:** We need a local-first graph that supports concurrent reads and writes gracefully.

[DECISION_TRANSCRIPT]
User: Let's use Postgres.
Claude: That breaks the local-first requirement in P10. Let's use SQLite.
[/DECISION_TRANSCRIPT]

<!-- BEGIN ENTRIES — new decisions go directly below this line, newest first -->

### scope-recovery-soft-one-rule-with-loud-in-band-signal

**Decided:** An unknown/unused scope on a hard-filter read (`list`/`open-questions` and MCP `list_decisions`) is handled by ONE soft rule, never a hard-error: a scope absent from the live vocabulary returns exit-0 plus a recovery pointer (`run mitos scopes / list_scopes`, naming `--state all` for archived history), and a scope present-but-empty-for-this-read returns honest data; the structured (`--json`/MCP) envelope additionally carries the absent-scope signal IN-BAND (exit-0, e.g. a `scope_known: false`-style flag plus the pointer) so a machine consumer's typo is never a silent `{total: 0}` it forges past, but it is never a hard-error/exception (which agents misread as a call-syntax mistake and retry). No inline candidate list, no 3-case split, no dead-domain special-case, no secondary spine probe.
**Rejected:** The hard-error delivery (non-zero exit + error object on an unknown scope — the prior unused-scope-typo-hard-errors-real-tag-empty-is-valid stance): REJECTED — a read verb carrying a non-zero exit for a structurally-valid query is a P1 weaponized-read-verb smell, it false-errors a legitimate never-written-but-valid scope, and LLM agents misread a tool error as a call-syntax mistake and enter retry/hallucinate loops; a silent `{total:0}` is also rejected on the structured surface (the OPERATIONAL_ECONOMICS §11 expensive machine-miss), so the resolution is soft-everywhere with a loud in-band signal on the structured surface.
The bounded inline candidate list (the prior reactive-scope-hint-is-bounded-enumeration-not-static-pointer stance): REJECTED — discovery is `list_scopes`' dedicated job; a static pointer to it suffices and drops the candidate-generation machinery.
The 3-case split with a secondary spine probe distinguishing typo from dead-domain (the prior scope-recovery-fires-on-live-vocabulary-absence-spine-is-secondary + typo-vs-dead-discriminator-is-targeted-probe-not-spine-fetch stances): REJECTED — disproportionate to a typo error-path on a plumbing vision; a dead domain is just absent-from-live and gets the same soft pointer (which names `--state all`), so the discriminator and the spine probe are unnecessary.
**Scope:** ax, cli
**Context:** Resolved by Vinga at the manual round of the AX/UX-hardening vision (v-mitos-ax-ux-hardening-20260623), VINGA_QUESTIONS Q3 — a radical simplification of the unused-scope recovery, chosen over the four prior incremental stances this supersedes. The trigger remains scope-absence-from-the-live-vocabulary, not weak confidence (scope-hint-keys-on-unused-scope-tag-not-confidence is kept). The vision-body prose (§1/§3/§4/§6/§10) is reconciled to this rule in the out-of-loop consolidation pass.
**Supersedes:** unused-scope-typo-hard-errors-real-tag-empty-is-valid, scope-recovery-fires-on-live-vocabulary-absence-spine-is-secondary, reactive-scope-hint-is-bounded-enumeration-not-static-pointer, typo-vs-dead-discriminator-is-targeted-probe-not-spine-fetch


### near-dup-clear-by-reference-stays-stateless-no-held-draft

**Decided:** Mitos stays stateless on the near-dup-pause clear path — it never holds server-side pending-draft state: the STATEFUL clear-by-reference design (the pause caches the validated draft server-side; a follow-up `commit_pending=<hash>` commits the held draft) is foreclosed permanently, while a STATELESS improvement stays open as a future option — the pause may return the validated content-hash and a follow-up re-sends the full payload plus that hash for the server to refuse on a mismatch (an integrity check that holds no state).
**Rejected:** Building the STATEFUL clear-by-reference (server-side held pending-draft + a `commit_pending=<hash>` follow-up): REJECTED PERMANENTLY — held ephemeral draft-state is against the stateless / source-of-truth spirit; it adds a new write-path failure mode (missing/expired hash), is lost on a server restart between pause and commit (so the full re-send fallback is needed anyway), and an abandoned draft never evicts — a heavy price to save a rare ~2K-token re-send whose transcription-drift risk is already neutralized by byte-identical file-driven re-sends.
Foreclosing the affordance ENTIRELY, including the stateless hash-guard: REJECTED — that would slam the door on a cheap, genuinely stateless drift-guard that holds no state; only the STATEFUL version is banned.
Leaving the whole question open/undecided: REJECTED in favour of banking the statelessness guarantee now, so the future write-protocol vision inherits a settled stance.
**Scope:** ax, sync
**Context:** Resolved by Vinga at the manual round of the AX/UX-hardening vision (v-mitos-ax-ux-hardening-20260623), VINGA_QUESTIONS Q2. The clear-by-reference affordance itself stays deferred to with/after the Conflict sensor (it touches the sacred buffer-first+rollback write contract, ax-ux-hardening-precedes-conflict-sensor); this decision settles only the forward-looking foreclosure Vinga reserved in EXTRA_CONTEXT — the statelessness guarantee for if/when it is ever built (option c: foreclose the stateful version, keep the stateless hash-guard open).
**Cites:** ax-ux-hardening-precedes-conflict-sensor


### dash-c-positional-wins-and-positionals-kept-indefinitely

**Decided:** When both the global `-C`/`--directory` and a verb's path positional (`status`/`agent-block`) are given, the positional wins — the verb's explicit target overrides the `-C` launch-context, which is free under the entry `chdir` via the existing `args.path or os.getcwd()` and matches git/tar intuition — and the `status`/`agent-block` path positionals are kept indefinitely as back-compat aliases (`-C` canonical, the uniform way to target a workspace), not deprecated.
**Rejected:** An explicit conflict-error ("workspace specified twice") when both `-C` and a differing positional are given: REJECTED — it layers validation on top of the free `chdir` composition and treats an ordinary launch-context-global-plus-explicit-override as a mistake; positional-wins is git/tar-consistent and costs zero code.
Deprecating the `status`/`agent-block` positionals (stderr warning + scheduled removal): REJECTED — that is a behavioural change to existing `mitos status /path` calls now plus a future breaking change, buying only tidiness; keeping them as permanent back-compat aliases is zero behavioural change, with `-C` the canonical uniform path going forward.
**Scope:** cli, ax
**Context:** Resolved by Vinga at the manual round of the AX/UX-hardening vision (v-mitos-ax-ux-hardening-20260623), VINGA_QUESTIONS Q1. The `-C` chdir mechanism and the total-retarget of relative file-args were already settled and recorded (dash-c-chdir-at-entry-not-config-threading); these were the two sub-decisions reserved for Vinga — the positional-conflict semantics and the deprecation fate. She took both recommendations: positional-wins (free, git-consistent) and keep-the-positionals (zero behavioural change).
**Depends-On:** dash-c-chdir-at-entry-not-config-threading


### blackout-vector-shape-is-plan-call-not-handle-mandate

**Decided:** The all-superseded blackout recovery vector — the soft P3 signal a ranked query/surface read fires when its top-k retrieves a non-empty set but every match is superseded-filtered so the display drops to zero — must fire rather than read as a silent empty, but its exact shape is the plan's call, bounded only by two constraints: it must not collapse to a bare-empty result, and it must not pre-decide the §7-deferred mixed-result recall policy; so returning the retired matches inline marked-superseded or emitting an actionable pointer to them are both legitimate shapes, and the prior mandate to surface the filtered handles specifically is lifted.
**Rejected:** Mandating that the blackout vector surface the filtered superseded handles specifically (the superseded ADR all-superseded-blackout-vector-surfaces-handles-not-just-names-tools): REJECTED — surfacing N handles the agent must then dereference one-by-one via show/show_node is MORE round-trips than returning the retired matches inline marked-superseded, which contradicts that ADR's own P15 actionability goal; and in a blackout there are no active matches to rank against, so ANY shape (inline-marked or pointer) leaves the §7 mixed-result recall policy undecided, making the handle-only constraint an over-specified plan-level mechanic that does not belong in a vision.
Returning a bare-empty result: REJECTED — re-opens the §11 assume-novelty dead-end (the agent sees zero, assumes novelty, re-derives a retired decision).
Extending the rescue to the partial-graze (mixed active+superseded) case: still deferred, not adopted (interim-graveyard-handle-surfacing-bounded-at-blackout-partial-graze-deferred) — that would augment a SUCCESSFUL read with graveyard metadata, which is the §7 recall-policy bet, not a P3 dead-end rescue.
**Scope:** ax, cli
**Context:** Settled in the AX/UX-hardening vision review (v-mitos-ax-ux-hardening-20260623), round 14, paired Gemini->Claude. Gemini's simplification lens flagged the handle side-channel as a brittle halfway house; Claude's altitude lens agreed it is plan-level over-specification whose P15 round-trip argument is self-defeating (inline-marked is fewer round-trips than handles-then-dereference). Lifted the blackout-vector SHAPE to the plan's call (bounded), preserving the parent soft-vector contract (ranked-recall-all-superseded-empty-fires-soft-vector) and the partial-graze deferral (interim-graveyard-handle-surfacing-bounded-at-blackout-partial-graze-deferred). Vision §3 change #7, §6 --limit bullet, §7, and §10-P3 updated to match (handle-mandate prose removed in favor of "shape is the plan's call, bounded").
**Supersedes:** all-superseded-blackout-vector-surfaces-handles-not-just-names-tools


### mitos-rebuild-recommits-the-corpus-through-the-current-catalog

**Decided:** Mitos ships a `mitos rebuild` verb as the supported upgrade path: it re-commits the full corpus (decisions.md + the quarterly archives) oldest-first through the CURRENT edge catalog and mechanism registry into a build-aside graph, then atomically swaps it in (the old graph backed up to graph.sqlite.bak_<ts>), so a corpus whose graph was migrated in place — the schema ladder widens the DDL but never re-commits, leaving the new edge types and mechanism registry empty — gains the full catalog without re-authoring. No decisions are ever at risk: the markdown is the source of truth (M7/P6) and the rebuild writes only the aside until the completeness gate passes.
**Rejected:** Relying on steady-state `mitos sync` to backfill the catalog: REJECTED — it structurally cannot. Sync reads the buffer ALONE (the archives are never re-parsed), and its per-entry idempotency skip short-circuits an unchanged canonical core before `commit_parsed_entry`, so the warn-deferred edges + mechanisms of already-committed nodes never re-reconcile. Empirically a newest-first buffer re-sync committed 7/129 (forward-ref-at-scale); the oldest-first archive+buffer replay commits the whole corpus. Leaving no rebuild path at all (the prior status quo): REJECTED — it strands every in-place-upgraded corpus with an empty catalog and no supported way to populate it.
**Mechanisms:** mitos-rebuild, cutover, build-aside-swap, oldest-first-replay
**Scope:** cli, cutover, substrate
**Context:** Built 2026-06-23 (0.5.0) after the V1b vision-complete review, when dogfooding the refreshed global install revealed the live corpus held the V1b schema on pre-V1b data (0 mechanisms / 0 non-kill edges despite 113 Mechanisms + 41 amends + 56 cites authored in the buffer). A risk-free throwaway replay proved the oldest-first rebuild populates the full catalog (cites 59, amends 41, mechanisms 197). `mitos status` nudges when a rebuild is due. Closes the upgrade-UX gap rollback-no-post-v1a-rebuild-gates-are-sole-defense named.
**Cites:** rollback-no-post-v1a-rebuild-gates-are-sole-defense

### rebuild-shares-the-cutover-engine-with-per-caller-strict-resilient-policy

**Decided:** `mitos rebuild` and the one-time `cutover` share ONE oldest-first replay engine — a per-entry quarantine + intra-sync fixpoint extracted as the module-level `mitos.replay.commit_quarantine_fixpoint` primitive (steady-state sync's fixpoint becomes a thin wrapper over it) — differing only by a per-caller policy: cutover is STRICT (the first casualty aborts with a CutoverError, because a one-time prototype migration must halt on a genuine corpus defect), rebuild is RESILIENT (casualties ride back on RebuildResult and surface as a guided punch-list, never raised).
**Rejected:** A separate replay path for rebuild (duplicating the quarantine+fixpoint): REJECTED — single source of truth; the order-independent convergence logic is load-bearing and must not fork. Unifying both verbs as resilient (dropping cutover's abort): REJECTED — it changes cutover's shipped one-time-migration contract (a prototype migration with a real defect must halt, not proceed-and-drop). Abort-on-first for rebuild (cutover's old behaviour): REJECTED — an upgraded corpus with ANY stale citation could then never rebuild (fix-one-abort-again whack-a-mole); the resilient path surfaces ALL casualties in one pass.
**Mechanisms:** mitos-rebuild, cutover, quarantine-fixpoint, oldest-first-replay
**Scope:** cutover, sync, substrate
**Context:** Built 2026-06-23 (0.5.0). The V1b Phase-4b fixpoint was already self-contained (store + an embed callback only), so the lift to `mitos.replay` was faithful and sync's behaviour is preserved (a thin wrapper); cutover's abort-on-first replay was replaced by the shared replay with strict=True. Vinga chose "shared helper, per-caller policy" over a separate path or a full unification.
**Cites:** v1a-cutover-wipe-and-rebuild

### rebuild-completeness-gate-baselines-the-current-graph-and-refuses-silent-drops

**Decided:** The rebuild completeness gate baselines against the CURRENT (V1a/V1b) graph's active set — not only the prototype, where it previously vacuous-passed — so a `mitos rebuild` that would drop or deactivate a node the live graph holds surfaces it as a shortfall; combined with the per-entry casualty list the swap is REFUSED by default (the live graph untouched) unless --allow-drops, and a refusal prints a per-failure-class remediation guide naming the live successor to re-point a stale citation to. No silent active-set shrinkage (P5/P6).
**Rejected:** Keeping the gate's vacuous-pass on a non-prototype graph (relying on the quarantine residual + the .bak backup alone): REJECTED — the residual catches entries that fail to COMMIT, but a node that commits yet computes inactive in the rebuild for another reason would slip through; baselining the current graph is the principled no-silent-loss check. Silently dropping casualties and proceeding (recovery via .bak rather than prevention): REJECTED — prevention before the destructive swap is the contract; the markdown stays authoritative and the dropped entries re-enter on the next fixed rebuild.
**Mechanisms:** mitos-rebuild, completeness-gate, active-view, build-aside-swap
**Scope:** cutover, cli, substrate
**Context:** Built 2026-06-23 (0.5.0–0.5.1). Vinga chose to generalize the gate over relying on residual+backup. The 0.5.1 follow-up turned a bare "refused" into a reassuring per-class guide (dangling_edge → re-point to the named active successor; missing_target → fix/remove the citation; --allow-drops the safe escape) so a stranger upgrading is never left stuck. A real run on the dogfood corpus surfaced exactly one casualty (a cites to a since-superseded decision), now fixed.
**Cites:** v1a-cutover-wipe-and-rebuild

### relationship-fields-comma-separated-multivalued

**Decided:** Mitos relationship fields are comma-separated multi-valued in the format-spec (like Mechanisms: and Scope:): the parser splits on comma and the reconciler commits N edges per field, a lone slug with no comma parses as a 1-element list so existing single-valued usage is unchanged (additive, no migration), and the agentic record API may stay single-slug in v0.1 and widen to lists later with no format change.
**Rejected:** Pinning the relationship fields single-slug for v0.1: REJECTED. This is a clean P20 (Retrofit Test) fire, both conditions holding:
- Known future need — the AX dogfood log (the vision-loop's own use of mitos) establishes it: consolidating one architecture flip that invalidated a 4-decision cluster cost 5 records; single-valued `supersedes` repeatedly forces "N records for fewer conceptual changes" (a cleanup record per retired neighbour); multi-`cites` is a recurring ask.
- Destructive retrofit — widening single→multi later is a C5 format-contract arity change (the field's value contract is re-typed in the format-spec, the parser, and the `ParsedEntry` attribute) on data already in the wild, requiring a P1 deprecation epoch — re-opening the exact two surfaces (format-spec + `_reconcile_edges`) v1b's warn-defer→commit flip is editing now.

Adopting comma-separated now is nearly free and MORE consistent, not less: the parser already comma-splits `Mechanisms:`/`Scope:` (the identical pattern, three lines from the relationship-field handling), committed edges are already N rows per field in the store, and it collapses two conventions (multi-valued tags vs single-valued relations) into one. A lone slug with no comma parses as a 1-element list, so all existing V1a single-valued `Supersedes:`/`Corrects:` usage is unchanged — additive, no migration. YAGNI does not override P20 when both its conditions hold.

Doing the agentic API (record args) as multi NOW: deliberately deferred (NOT rejected). The format/parser/store going multi-valued is the wall that must open now; the `record`/`record_decision` args can keep single-slug in v0.1 and widen to `List[str]` later with no further format change (no wall — YAGNI applies to the API surface).
**Scope:** format, parser, substrate
**Context:** Ruled by Vinga in the terminal, 2026-06-22. Surfaced by mitos-pub's AX_FEEDBACK dogfood log — the vision-loop Claude's own real use of mitos for decision work — where the single-valued relationship fields produced a recurring, escalating write-path pain (N cleanup records to retire a cluster; multi-cites asks). A dogfood loop closes here: the feedback that exposed the gap becomes a recorded decision in the very graph it is about.

V1b is pre-implementation, so this is zero-churn: it lands as part of v1b's §2 warn-defer→commit flip, which already commits v1b to rewriting the format-spec relationship-fields section (the stale "in V1a the graph commits the two kill-edges" note). Make the arity a conscious, recorded decision rather than an accidental pin.

Scope split (the P20 wall vs the YAGNI defer): the FORMAT-SPEC + parser + ParsedEntry attrs + store go multi-valued now (the wall — widening later would be a breaking C5 contract change); the agentic record API stays single-slug in v0.1 and widens to lists later for free (no wall).

Two adjacent items noted but NOT part of this decision: (1) the transitive near-dup (get_lineage) suppression should be design-tested against the "retire/refine a whole lineage-cluster" workflow — the sibling-pause, where N decisions amending one root each trip their siblings' similarity, not just the declared target; (2) once these relationship fields commit as real edges, a future prose-vs-graph drift sensor (v0.2 conflict-sensor territory) can diff authored prose ("this amends X") against the committed edges — v1b enables it by committing the edges, does not build it.


### forward-ref-convergence-via-intra-sync-fixpoint-retry-loop

**Decided:** Mitos sync converges same-sync cross-file forward references via an intra-sync fixpoint retry loop (retry the per-entry-quarantined set until no further progress, then surface what remains): any acyclic cross-file dependency chain commits in ONE sync, order-independent, while per-entry quarantine stays the floor so malformed entries and genuine cycles still surface loudly after the fixpoint.
**Rejected:** Quarantine + manual re-sync as the sole convergence (the prior recorded stance): REJECTED. A deep acyclic cross-file chain (OQ→D→OQ→D, where `derives_from` is OQ→D and `resolves` is D→OQ — opposite directions, so no file order satisfies both) takes O(chain-depth) manual re-runs of perfectly valid markdown — a real P3/P5 flow harm on a common authoring pattern (a cluster of open questions plus the decisions that spawn/resolve them, authored together).

Entry-level topological sort: REJECTED — its age-proxy is file position (newest-on-top), nondeterministic on reused mutable slugs (tried and reverted in cutover).

Two-phase all-DELETEs-then-all-INSERTs: REJECTED — breaks V1a's per-entry commit atomicity and the MI-12 crash-safety resting on it.

A file-processing order (decisions before questions, or a node-pass-then-edge-pass split): REJECTED — `derives_from` (OQ→D) and `resolves` (D→OQ) point opposite ways, so no single file order satisfies both; a genuine circular ordering dependency.

The fixpoint loop is the one remedy that is BOTH order-independent AND preserves per-entry atomicity/MI-12 (~4-10 lines), and it distinguishes by construction a transient orderable failure (clears within the sync) from a genuine error (a real 2-node cycle or malformed entry survives the fixpoint and surfaces loudly).
**Scope:** sync, substrate, store
**Context:** Ruled by Vinga in the terminal, 2026-06-22 (VINGA_QUESTIONS flag #1). Joint Claude+Gemini recommendation, sharpened this review cycle with the O(chain-depth) proof (the status quo was mis-framed as "rare 2-sync" — it is O(depth) for a deep cross-file chain). V1b is pre-implementation, so the fixpoint loop is built in from the start: zero churn, no reversal of shipped code.

What stays: per-entry quarantine remains the FLOOR (re-stated here, not dropped) — it isolates malformed entries and genuine 2-node cycles, which surface loudly after the fixpoint converges. The oldest-first batch heuristic (steady-state-batch-oldest-first-flow-heuristic-over-quarantine) is KEPT but is now cosmetic (casing/near-dup ordering); it is no longer load-bearing for cross-file correctness, which the fixpoint handles order-independently. Only the prior "no intra-sync retry; re-running sync IS the retry" stance is reversed.

Note: the §4.1 same-sync handle-handoff case this flag previously cited as also collapsing to 1-sync is moot — the slug-alias/handle subsystem is being stripped (see strip-slug-alias-subsystem-renames-break-loud-via-missing-target).

Enactment (batched with the other 2026-06-22 rulings): vision §6.1 cluster 3 (reframe the anti-retry MUST-NOT to the fixpoint loop), DoD #12 (one-sync in-batch convergence for any acyclic chain).
**Supersedes:** cross-file-forward-ref-per-entry-quarantine-self-heal


### strip-slug-alias-subsystem-renames-break-loud-via-missing-target

**Decided:** Mitos ships no slug-alias citation-rot subsystem (no node_handles table, no Aliases field, no fallback resolution, no union-uniqueness, no backfill): slugs stay mandatory, explicit, capped at 100, and mutable, but a citation to a renamed-away slug breaks loudly as missing_target so the author fixes the markdown, and kernel M2 is amended to drop its slug_aliases old-citations-still-resolve clause.
**Rejected:** Keeping the slug-alias subsystem (kernel M2 as written): REJECTED. It heals the disposable SQLite graph while leaving stale `Cites: old-name` references in the Gold-Standard markdown — a P6/M7 inversion, where the authoritative source goes internally inconsistent while only the derivative resolves. Its value is narrow: already-committed edges resolve by node-ID and are rename-immune, so only a *newly authored* citation using an *old* name benefits — a case a human fixes with search-replace and an agent self-heals by re-querying for the live slug. And it cost ~17 ADRs of safety machinery (union-uniqueness across all current+historical handles, archived-slug backfill, collision disambiguation, fold/validation parity, handle-aware idempotency skip) — the recurring-amendment "stop patching, ask the redesign question" smell VISION_GUIDANCE names.

Author-time prefix expansion / unique-prefix parse-time resolution: separately REJECTED — a stored prefix that resolves uniquely today turns ambiguous the moment a later unrelated node is minted, manufacturing the very citation-rot the subsystem claimed to prevent.

The enforcer instead is the existing `missing_target` quarantine, which keeps the markdown truthful by construction: a stale citation breaks loudly and the author updates the source. Deprecating a name while preserving history is a `supersedes` deletion decision (M1), not an alias.
**Scope:** identity, substrate, store
**Context:** Ruled by Vinga in the terminal, 2026-06-22. Origin: Gemini's high-altitude saboteur strike during the V1b vision review; Claude concurred on the merits. V1b is pre-implementation (V1a completed; node_handles was never built), so the strip is zero-churn — it removes un-built scope, not shipped code. Q5 (slug-removed-from-canonical-core-hash) stands unchanged: slugs remain mutable handles out of the identity hash.

This decision RETIRES the entire slug-alias / node_handles ADR cluster (all now moot): slug-alias-union-strict-casefold-uniqueness, slug-uniqueness-spans-all-node-slugs-not-just-active, aliases-corpus-authored-via-aliases-field, node-handles-single-table-structural-union-uniqueness, node-handles-fold-matches-slug-casefold-exactly, node-handles-validation-matches-slug-write-side, node-handles-legality-parity-v1b-established-not-inherited, node-handles-migration-backfills-archived-slugs, node-handles-backfill-collision-warned-disambiguation-not-dead-end, node-handles-backfill-forecloses-insert-or-ignore, node-handles-insert-is-same-node-idempotent-not-blind-conflict, node-handles-same-node-idempotency-is-the-recompute-delete, node-handles-recomputed-per-parsed-entry-removed-handles-dropped, idempotency-skip-includes-handle-inputs-not-hash-only, archived-slug-reclamation-is-explicit-valve-not-uniqueness, prefix-matching-is-author-time-expansion-not-parse-time.

Two related ADRs are only PARTIALLY affected and are NOT retired: per-entry-quarantine-isolates-whole-commit-error-class still governs the non-alias rejection codes (missing_target, mutation-cycle, kind_constraint_violation, dangling_edge); nodes-slug-guard-kept-as-core-table-defense-in-depth-not-dropped reverts to V1a's active-only slug uniqueness (the all-node tightening was an alias-subsystem consequence).

Follow-up enactment: amend FRAMEWORK.md kernel M2 (drop the slug_aliases clause); strip the subsystem from the V1b vision (§1, §1.2, §2, §4.1, §6.1 cluster 6, §6.2, §8.2, §9 R14, §10); check ROADMAP for alias references.
**Supersedes:** node-handles-single-table-structural-union-uniqueness


### mechanism-alias-storage-shape-not-prescribed-column-or-table

**Decided:** V1b's deferral of mechanism-alias storage to v0.2 does NOT prescribe its storage shape — v0.2 may add a nullable column OR a relational table (e.g. symmetric to node_handles) once its alias-merge semantics are designed, both additive so P20 still does not fire — because pinning a JSON aliases_json column would prejudge v0.2 toward a model divergent from the relational node_handles namespace before mechanism aliasing's merge semantics (a many-to-one merge whose cross-row-uniqueness needs differ from the handle namespace) are settled.
**Rejected:** Prescribing the `aliases_json` JSON column — the parent ADR's "v0.2 adds them via a non-destructive ALTER TABLE ADD COLUMN" framing applied to alias storage: rejected because casually naming a JSON column locks v0.2 into a model that breaks the relational uniqueness/JOIN model just established for node_handles (an anti-pattern, P19/Cathedral). Crucially, the P20-does-not-fire deferral argument the parent ADR rests on holds regardless of shape — a CREATE TABLE is equally additive/non-destructive with no lost-data backfill — so the column shape was never load-bearing for the deferral and should not have been pinned.

Prescribing a `mechanism_handles` relational table symmetric to node_handles (the proposed fix this round): rejected as over-specifying in the opposite direction. node_handles is relational SPECIFICALLY because it needs a declarative cross-row UNIQUE to make silent citation capture structurally impossible (OD1, a unique-handle namespace). Mechanism aliasing is a many-to-one MERGE ("LINT:wal -> lint-wal"), and the fold (mechanism_canonical_norm) already unifies the fuzzy cases at registration — so whether v0.2's alias merge even needs cross-row uniqueness is undesigned. Mandating the table guesses v0.2's semantics as much as aliases_json did. The vision should prescribe NEITHER shape; v0.2 picks it when its merge semantics exist.
**Scope:** mechanisms, store, substrate
**Context:** Surfaced in V1b vision-review (paired round, 2026-06-21, with Gemini). The vision wove a specific aliases_json JSON column through §1.1/§4.6/§7/§8.2/§10; neutralized to shape-agnostic. Parent ADR deferral reasoning (P20 does not fire) is unchanged and holds for either shape.
**Amends:** mechanism-registry-defers-aliases-json-and-kind-columns


### idempotency-skip-includes-source-as-third-no-op-input

**Decided:** The Mitos per-entry idempotency skip's widened no-op condition must include source as a third out-of-core input alongside the handle set (slug and Aliases) — skipping only when canonical core, handle set, AND source are all unchanged — because the sync-level if-existing short-circuit gates the source_reencounter signal-evaluation pass and the node_handles recompute alike, so a (core-and-handle)-only widening silently skips a same-core/same-handle re-encounter from a different source before source_reencounter can fire, failing DoD #1.
**Rejected:** A (core ∪ handle)-only widening — the parent ADR's literal scope (handle inputs = slug ∪ Aliases): rejected because `source` is out of BOTH the canonical-core hash (M2/MI-4) and the handle set, so a same-core/same-handle entry re-encountered from a different `source` matches the widened "unchanged" condition and is skipped by the blanket `if existing:` gate before `commit_parsed_entry` runs — so `source_reencounter` never fires and DoD #1 ("re-sync identical entry from a different source → exactly one signal row") silently fails. signals-insert-or-ignore cannot rescue it: the skip prevents the signal INSERT from being attempted at all, upstream of any ON CONFLICT.

Treating the `source_reencounter` short-circuit-bypass and the handle-aware idempotency skip as two independent mechanisms — the prior vision framing carried them as two separate §6.2 bullets with two different mental models (one a per-table-write bypass, one a widened blanket skip): rejected because they gate the SAME sync-level `if existing:` short-circuit (verified in code: perform_sync `continue`-on-existing and record_decision_entry `return "exists"`, both ABOVE commit_parsed_entry). Implemented separately, a planner widens the skip to (core ∪ handle), passes DoD #5's incremental handle-edit gate, and silently regresses DoD #1 — the gap hides behind one green gate while the other breaks.
**Scope:** identity, store, substrate, sync, signals
**Context:** Surfaced in V1b vision-review (paired round, 2026-06-21). Verified in sync.py that both write paths blanket-skip above commit_parsed_entry (perform_sync continue-on-existing sync.py:485; record_decision_entry return-exists sync.py:1164). The §6.2 source_reencounter bullet and handle-aware-skip bullet prescribed one short-circuit two ways; this unifies them and adds source as the third no-op input.
**Amends:** idempotency-skip-includes-handle-inputs-not-hash-only


### v1b-migration-snapshot-retained-on-success-not-dropped

**Decided:** The V1b pre-ladder migration snapshot is RETAINED on a successful migration, never auto-dropped-on-pass — restore-on-failure and keep-on-success — so the one-step binary reversal outlives the gate-pass boundary; its cleanup is an explicit user action or a later lifecycle sweep, not an automatic drop.
**Rejected:** Drop-on-pass (the parent ADR's sub-clause: discard the snapshot once the post-migration faithfulness gates pass): rejected because the gates catch only a DETECTABLY lossy rebuild, while a successful-but-buggy migration can pass every gate and still commit a SILENT semantic corruption no gate covered (e.g. a backfill logic flaw that drops aliases without raising) — exactly the parent ADR's own rejected-path (c) failure mode. Destroying the only pre-migration image the instant the gates go green re-opens that exposure at the worst moment: it is a wrongful-advance (P5 Ironclad — automated recovery never destroys user data on the assumption of its own correctness), and the graph DB is single-digit MB so retention costs effectively nothing. Also rejected: a fixed auto-expiry (drop after N syncs) — the same wrongful-advance in slow motion, since a silent corruption can surface after any interval; cleanup stays explicit, human- or lifecycle-gated.
**Mechanisms:** sqlite
**Scope:** schema, store, substrate, sync, migrations
**Context:** V1b vision review 2026-06-21, Gemini's Saboteur-lens finding (drop-on-pass destroys the only binary escape hatch the instant a silent semantic migration bug would later surface); Claude concurred on P5-Ironclad grounds. mitos surface confirmed no prior decision weighed keep-vs-drop-on-success — the parent recorded drop-on-pass as an unweighed sub-clause. Applied to vision DoD #16 and section 9. Recorded directly in decisions.md because the dogfood graph predates V1a and awaits cutover, so the buffer is the only writable surface this round; the amend edge reconciles on rebuild.
**Amends:** v1b-migration-takes-pre-ladder-db-snapshot-as-binary-reversal


### oq-rotation-deferral-passes-retrofit-test-additive

**Decided:** The V1b deferral of open-question rotation passes the P20 Retrofit Test — condition 2 (destructive retrofit) fails because the only accrued cost is recoverable bloat in an always-read buffer whose later cleanup by rotation is purely additive — so P20 does not fire and the P19 plus P11 deferral governs.
**Rejected:** P20 forces building OQ rotation into V1b now (the economics-lens review claim that deferral creates a destructive "migrate a massive questions.md" retrofit): rejected — the retrofit is additive, not destructive. questions.md holds parked plus resolved OQs and every reader reads it (nothing lost or hidden); a later rotation moves resolved OQs (computed state, M3) to an archive the same future vision teaches the rebuild to glob, with edges resolving by node identity not file location — so no schema change, no backfill, no consumer rewrite, no data loss. A deferral whose only accrued cost is recoverable bloat with a purely additive cleanup structurally cannot meet P20 condition 2 (destructive retrofit) — the exact inverse of mechanisms.source's genuine lost-if-not-captured hit, which V1b DOES wire now. Also rejected: leaving the deferral justified on P19 plus correctness plus scope alone without an explicit Retrofit Test — the framework forbids a deferral that never runs the test, so the determination is now recorded.
**Mechanisms:** questions.md
**Scope:** substrate, sync, store
**Context:** Raised in the V1b parser-and-store economics-lens vision-review round (2026-06-21) by Gemini, who argued P11/P20 mandate building OQ rotation now. Resolved by explicitly running the Retrofit Test: condition 1 (known future need) holds, condition 2 (destructive retrofit) fails. Hardens the rejected-path of the parent deferral so a future P20-grounded re-litigation bounces off precedent. Vision text updated at section 10 P20 (counter-case) and section 6.1 cluster 3.
**Amends:** questions-md-persistent-buffer-oq-rotation-deferred


### v1b-migration-takes-pre-ladder-db-snapshot-as-binary-reversal

**Decided:** The first populated-schema migration in V1b snapshots the SQLite graph DB file before the in-place ladder runs (restore-on-failure, drop-on-pass), giving the migration a one-step binary reversal independent of the absent post-V1a corpus rebuild.
**Rejected:** (a) Gates-only with no defined reversal (the recorded rollback-no-post-v1a-rebuild-gates-are-sole-defense stance): rejected because the faithfulness gates PREVENT a lossy migration but are not themselves a REVERSAL, and the framework holds a change is not done until its reversal is defined. (b) Building a re-runnable full-corpus rebuild verb in V1b: already rejected by the parent ADR as substrate/ROADMAP scope; the snapshot is the cheaper third option the parent never weighed, needing no rebuild verb. (c) Relying on the in-place migration transaction alone: it rolls back a crash mid-step but COMMITS a successful-but-buggy rebuild that drops archived edges or handles, so it is no reversal for the failure mode that matters.
**Mechanisms:** sqlite
**Scope:** schema, store, substrate, sync, migrations
**Context:** V1b vision review 2026-06-21 (Claude and Gemini converged). migrations.py runs the ladder in-place (transaction-per-step bumping user_version); only the one-time cutover build-asides. Reuses the cutover build-aside idiom in its lightest form (a file copy). Exact mechanism (file copy vs VACUUM INTO vs SQLite backup API) and retention are the plan call. Pinned as vision DoD #16, verified by fault injection (P10 Provoke the Failure).
**Amends:** rollback-no-post-v1a-rebuild-gates-are-sole-defense


### questions-md-persistent-buffer-oq-rotation-deferred

**Decided:** Mitos V1b ingests questions.md into steady-state sync as a persistent write-buffer (exactly as decisions.md is one today, not per-sync-evacuated) plus the minimal oq_state_view visibility read, and defers per-sync open-question rotation and any open-question archive convention to V3a/V3b/Vision 7, because no OQ archive convention exists today and the post-V1a rebuild reads the OQ buffer alone, so rotating synced OQs out would break P6/M7 rebuild-faithfulness
**Rejected:** Rotating questions.md on successful sync "exactly as decisions.md does" (the superseded questions-md-rotates-as-write-buffer-with-minimal-oq-visibility): rejected — cutover._load_oq_stream reads only the questions buffer (its own docstring: "no open-question archive convention yet"), the post-V1a rebuild reads the buffer alone (rollback-no-post-v1a-rebuild-gates-are-sole-defense), and decisions.md itself is NOT per-sync-evacuated (rotation is threshold-gated + interactive, V3b, sync.py), so rotating a synced OQ out to a non-existent archive the rebuild cannot read loses it on any wipe-and-rebuild — breaking the exact P6/M7 rebuild-faithfulness the corpus-authored alias subsystem (aliases-corpus-authored-via-aliases-field) is anchored on.
Forcing per-sync rotation to close the prune-divergence trap: rejected — that divergence is the same accepted no-graph-GC property as decisions.md, swept by Vision 7's Lifecycle Gardener (oq-hygiene-drop-is-lifecycle-gardener-not-forced-decision); the minimal oq_state_view read, not rotation, is what keeps questions.md from being a hand-managed worklist.
Building an OQ archive + a re-runnable full-corpus rebuild in V1b so rotation is safe: rejected — substrate/ROADMAP scope, not V1b; keeping the OQ in its buffer (which every reader already reads and the user can port) preserves faithfulness with zero new machinery (P19).
**Scope:** store, substrate, sync
**Context:** SUPERSEDES questions-md-rotates-as-write-buffer-with-minimal-oq-visibility (the Supersedes edge cannot be authored yet — that prior ADR is still an unsynced buffer entry; add the kill-edge once it is synced). Raised by Gemini in the V1b paired vision round 2026-06-21, verified against cutover.py (_load_oq_stream reads the OQ buffer alone — docstring: 'no open-question archive convention yet'), converged with Claude. Cites aliases-corpus-authored-via-aliases-field, rollback-no-post-v1a-rebuild-gates-are-sole-defense, oq-hygiene-drop-is-lifecycle-gardener-not-forced-decision.


### corrects-excluded-from-mutation-cycle-probe

**Decided:** Mitos V1b deliberately excludes corrects from the write-time mutation-cycle probe and from get_lineage's walk, and the exclusion is safe because the mutation family (supersedes/amends/narrows) and the corrects-only transcript family are walked by two disjoint primitives no consumer traverses together, so a corrects-mixed cycle is never walked as a cycle.
**Rejected:** Extend the write-time mutation-cycle probe (and get_lineage's walk) to include `corrects`, so the whole temporal edge family `supersedes ∪ corrects ∪ amends ∪ narrows` is DAG-checked together (Gemini's V1b finding, flagged CRITICAL, 2026-06-21): rejected. The exclusion is safe because Mitos walks the mutation family and the corrects family with TWO DISJOINT primitives that no consumer ever traverses together: `get_lineage` walks `supersedes ∪ amends ∪ narrows` (the near-dup transitive-suppression and cycle-probe set), while `get_transcript` walks corrects-only (its transitive form deferred to V5). `corrects` is a kill-edge (`_KILL_EDGE_TYPES_SQL`), so V1a's active-source guard — a kill-edge mints only from a non-killed source (store.py source_active check) — already keeps the corrects-only graph acyclic; and the active-view, modifier-stamping, and OQ-Stage-2 reads are single-hop, not recursive walks. Worked example (D1 `corrects` D2, then D2 `amends` D1): the abstract full-family union has a 2-cycle, but `get_lineage(D2) = [D1]` and `get_lineage(D1) = []` because the `corrects` edge is never followed, so no walker loops and computed state stays coherent (D2 inactive, its dead amends-edge does not stamp the still-live D1 via the source-liveness filter). No walker ever traverses a combined `supersedes ∪ corrects ∪ amends ∪ narrows` cycle — the temporal paradox lives only in an abstract union nothing walks.

Merge `corrects` into `get_lineage` so one walk covers the entire temporal family: rejected — it violates the M1 mutation-vs-transcript split [get-lineage-separate-store-primitive-not-get-transcript-overload]: `corrects` are in-buffer typo fixes, not semantic evolution, so folding them into the lineage walk would corrupt the immutable-history reading and change near-dup transitive-suppression semantics, for zero safety gain (the exclusion is already proven safe above).
**Mechanisms:** get_lineage, get_transcript
**Scope:** edges, retrieval, store, substrate
**Context:** V1b vision-review round, 2026-06-21: Gemini's fresh pass re-raised (as CRITICAL) whether corrects belongs in the mutation-cycle-prevention union; this pins why the exclusion is safe so the probe's scope is not re-opened by a future fresh-eyes reviewer.
**Cites:** get-lineage-carries-loud-homeostasis-cycle-bound


### node-handles-backfill-collision-warned-disambiguation-not-dead-end

**Decided:** On the one-time node_handles migration backfill, a pre-existing cross-node casefold-slug collision (legal under V1a active-only slug uniqueness) is disposed by explicit, warned auto-disambiguation of the inactive or archived duplicate handle with a -N suffix (the active node, if any, keeps the bare handle so no live citation re-resolves) rather than a bare loud slug_collision raise that strands the operator, because the colliding nodes are archived with no buffer-edit lever and a transactional migration rollback on the raise would otherwise deadlock V1b adoption; the steady-state same-sync handle-handoff keeps its loud-raise-converge disposition because there both entries are in the buffer and re-sync is a genuine recovery path
**Rejected:** Bare loud-fail (raise slug_collision and stop, "operator resolves") as the backfill disposition — rejected because the colliding nodes are pre-existing and archived: steady-state sync re-parses the buffer alone and there is no post-V1a full-corpus rebuild (rollback-no-post-v1a-rebuild-gates-are-sole-defense), so the operator has no buffer lever to rename either side, and a transactional migration that rolls back on the raise leaves them unable to reach V1b at all — the framework-forbidden P5/P3 "stranded with no path forward", a migration deadlock. This is the gap the parent ADR's "surface as the loud slug_collision rather than silently swallow" framing missed: it weighed loud-raise only against silent-swallow, never against the warn-and-self-resolve third path.

Silent INSERT OR IGNORE / ON CONFLICT DO NOTHING — already foreclosed by the parent node-handles-backfill-forecloses-insert-or-ignore (it orphans the duplicate handle, the OD1 citation-capture break); explicit auto-disambiguation differs precisely by being loud AND warned, so the OD1 foreclosure stands intact (this ADR amends, not reverses, the parent).

Graph surgery (operator hand-edits nodes.slug in the SQLite graph) as the sanctioned primary escape — rejected as the contract because it is not self-completing and makes the operator edit a derivative store the system tells them is disposable; it remains available as a manual last resort, but is not the disposition.

Extending the steady-state same-sync handle-handoff loud-raise-converge disposition (same-sync-handle-handoff-surfaces-collision-not-batch-ordering) to the backfill — rejected because the two cases differ in exactly the property that matters: in the steady-state handoff both entries are in the buffer and A's relinquish lands this sync so B converges on re-sync (a real recovery path, so P19 fewest-moving-parts rightly prefers loud-raise there), whereas the one-time backfill collides two pre-existing archived nodes with no buffer lever and no convergence path. Auto-disambiguation is therefore scoped to the one-time backfill only and does not touch the steady-state handoff.
**Scope:** identity, schema, store, substrate, sync
**Context:** Surfaced in the V1b vision review (2026-06-21, paired round with Gemini, simplification lens). The node_handles all-node backfill (DoD #5, R14, vision §6.2) hits UNIQUE(handle_casefold) on a pre-existing active+inactive (or multi-inactive) casefold-slug duplicate that V1a active-only slug uniqueness legally permits (idx_nodes_slug_casefold is non-unique by design; the mint-time collision check is active-scoped — both verified in shipped code). Vision §4.1's slug_collision remedy ("rename or re-home the citation") assumes a buffer-editable entry, which an archived node is not. Gemini and Claude converged on explicit-warned -N auto-disambiguation of the inactive duplicate as the OD1-safe, deadlock-breaking disposition. Applied to vision §4.1 / §6.2 / R14 / DoD #5.
**Amends:** node-handles-backfill-forecloses-insert-or-ignore
**Cites:** same-sync-handle-handoff-surfaces-collision-not-batch-ordering


### rollback-no-post-v1a-rebuild-gates-are-sole-defense

**Decided:** There is no re-runnable post-V1a full-corpus rebuild, so V1b R13 and R14 faithfulness gates are the sole defense for archived-row survival rather than a backstop behind an easy rollback: the P6/M7 wipe-and-rebuild from buffer plus archives is wired only into the one-time prototype cutover (rebuild_and_gate), which no-ops on an already-V1a, absent, or empty graph, while steady-state sync re-reads the buffer alone, so a shipped populated-schema-migration faithfulness bug cannot be recovered by delete-and-resync.
**Rejected:** Relying on P6/M7 the-graph-is-disposable-delete-and-resync as the rollback for V1b populated-schema migrations: rejected because delete-and-resync re-reads the buffer only, archived entries rotated out of decisions.md are not re-derivable from the buffer, and the full-corpus replay that would restore them (cutover rebuild_and_gate) is gated to is_pre_v1a_schema and no-ops post-V1a (verified in cli.py cmd_cutover). Also rejected: building a re-runnable full-corpus rebuild verb in V1b, which is substrate/ROADMAP scope; the V1b correct response is to state the honest recovery contract and make R13 and R14 hard closeout gates.
**Mechanisms:** rebuild_and_gate, is_pre_v1a_schema
**Scope:** store, substrate, sync, schema
**Cites:** node-handles-migration-backfills-archived-slugs


### oq-read-surfaces-stamp-modifiers-like-decision-reads

**Decided:** The new OQ read surfaces (oq_state_view and the minimal CLI open-question read) stamp amended_by and narrowed_by exactly as every decision-read surface does, because amends and narrows are legal OQ-to-OQ in the kind matrix and cluster 3 commits OQ-authored ones, so an amended-but-active open question must not read as the final word: the every-read-surface modifier invariant applies to OQ reads, not only decision reads.
**Rejected:** Scoping modifier stamping to decision-read surfaces only (the prior DoD 7 every-decision-read-surface phrasing): rejected because it leaves the new OQ read surfaces ungated while the kind matrix makes OQ-to-OQ amends and narrows legal and cluster 3 commits them, recreating the amended-axioms-read-as-live trap for open questions. Also rejected: scoping OQ amends and narrows out of v0.1 to avoid stamping, since the kind matrix lists them legal and ingestion commits them, so the edges exist and omitting them from the read payload is a silent under-report, not a clean scope cut.
**Mechanisms:** oq_state_view, amended_by, narrowed_by
**Scope:** retrieval, store, substrate
**Depends-On:** v1a-ships-modifier-stamping-seam-not-reserved-slot


### node-handles-backfill-forecloses-insert-or-ignore

**Decided:** The one-time node_handles migration backfill forecloses INSERT OR IGNORE, extending the steady-state handle-write OD1 foreclosure to the backfill path: because V1a slug-uniqueness is active-only, the pre-existing corpus may legitimately hold an active and an inactive node sharing a casefold slug that V1b all-node tightening renders a genuine collision, so the backfill replay-safety must surface a pre-existing cross-node collision as the loud slug_collision P3 vector error rather than silently swallow it.
**Rejected:** INSERT OR IGNORE or ON CONFLICT DO NOTHING for the backfill replay-safety: rejected because, unlike a benign signals re-insert, a backfill INSERT OR IGNORE keeps one node slug-handle and orphans the duplicate node handle, the OD1 citation-capture break the subsystem exists to prevent; the section 6.2 INSERT-OR-IGNORE-vs-table-empty-probe phrasing offered the foreclosed pattern for the backfill specifically, whereas the steady-state recompute path was already foreclosed by node-handles-insert-is-same-node-idempotent-not-blind-conflict. Also rejected: assuming the V1a corpus has no casefold-duplicate slugs, since idx_nodes_slug_casefold is non-unique by design (active-view-scoped uniqueness) so an active-plus-inactive duplicate is legal pre-existing state the all-node tightening newly rejects.
**Mechanisms:** node_handles, idx_nodes_slug_casefold, slug_collision
**Scope:** identity, store, substrate, sync, schema
**Amends:** node-handles-insert-is-same-node-idempotent-not-blind-conflict


### idempotency-skip-includes-handle-inputs-not-hash-only

**Decided:** The Mitos per-entry idempotency skip — the slug-free-canonical-core early-return that gates whether commit_parsed_entry runs (perform_sync continue-on-existing and record_decision_entry return-exists) — must include the handle inputs, the entry current slug and Aliases, not key on the canonical-core hash alone, so a same-hash slug rename or Aliases edit re-commits and the node_handles recompute actually runs, while a genuinely unchanged entry still skips.
**Rejected:** Hash-only skip (the V1a status quo): rejected because the slug is out of the canonical-core hash (Q5 / slug-removed-from-canonical-core-hash) and the Aliases field is new in V1b, so a same-hash entry that only renames its slug or edits its Aliases line early-returns before commit_parsed_entry — the node_handles recompute never runs and the authored rename/alias is silently dropped. It lands only on a full wipe-and-rebuild (where every entry re-commits) and on brand-new entries, so DoD #5 rebuild-faithfulness still passes and the gap hides behind a green gate. That silently defeats the corpus-authored alias model (aliases-corpus-authored-via-aliases-field) on exactly the rename-then-alias flow it exists for — a P6/M7 corpus-faithfulness break wearing the costume of an economics optimization. It also undercuts node-handles-recomputed-per-parsed-entry-removed-handles-dropped, whose drop-removed-handles guarantee only holds when the recompute fires.

Always-run recompute (drop-and-reinsert the handle rows unconditionally, off the skip's gated path): rejected because it churns node_handles (and the WAL) on every re-sync of an unchanged corpus, breaking P5's re-syncing-an-unchanged-decisions.md-is-a-no-op at the write tier and wasting I/O (P17). The recompute must be gated by the widened skip, not unconditional. The exact site of the handle-aware comparison — widen the early-return condition, or move the handle recompute onto the always-run side of the skip — is the plan's call.
**Mechanisms:** commit_parsed_entry, perform_sync, record_decision_entry, node_handles
**Scope:** identity, store, substrate, sync
**Context:** Surfaced in the V1b economics-lens vision review on 2026-06-21 by grounding the section 8.2 nodes.slug-and-node_handles-cannot-diverge invariant against the real sync code: both steady-state write paths early-return on the slug-free hash before commit_parsed_entry (sync.py line 484 and sync.py line 1164). Claude found it, Gemini endorsed. Amends vision sections 8.2 and 6.2.
**Cites:** node-handles-recomputed-per-parsed-entry-removed-handles-dropped


### nodes-slug-guard-kept-as-core-table-defense-in-depth-not-dropped

**Decided:** V1b keeps an independent slug_casefold guard on the core nodes table rather than dropping it: node_handles UNIQUE(handle_casefold) is the union-uniqueness anchor across all node slugs and aliases, but the core nodes table retains an independent slug guard as P7 defense-in-depth, because an integrity constraint on the core table natural key is not duplicated derived state and constraints do not drift; since V1b tightens slug-uniqueness to all-node scope, promoting the existing plain non-unique slug_casefold index to a UNIQUE constraint becomes legitimate, but keep-plain versus promote-to-UNIQUE is a plan-level call.
**Rejected:** Dropping nodes.slug_casefold as a redundant second uniqueness enforcer (the prior vision-inline P19 single-source stance): rejected on two grounds. (1) Factually it is a plain NON-unique lookup index today (migrations.py CREATE INDEX, non-unique by design under V1a active-view-scoped uniqueness), so there is no second uniqueness enforcer to retire. (2) P19 single-source governs duplicated derived STATE, not independent integrity constraints: dual enforcement of an invariant is P7 defense-in-depth, and dropping a free guard is net-negative migration work that couples core-table integrity to the correctness of the alias subsystem.
**Mechanisms:** node_handles, slug_casefold, nodes
**Scope:** identity, store, substrate
**Cites:** node-handles-single-table-structural-union-uniqueness


### node-handles-same-node-idempotency-is-the-recompute-delete

**Decided:** node_handles same-node insertion idempotency is delivered structurally by the per-node recompute DELETE rather than a conditional INSERT: because the prior handle rows of a parsed entry are dropped before re-insert, the handle write is a plain INSERT whose only possible UNIQUE(handle_casefold) violation is necessarily cross-node and surfaces as slug_collision, so no conditional-idempotency form is needed.
**Rejected:** A same-node-vs-cross-node discriminator (WHERE NOT EXISTS, a SELECT pre-check, or a same-node UPSERT): rejected as unnecessary once the per-node recompute DELETE already clears the prior handle rows of that node, and the obvious same-node UPSERT is precisely the form that silently swallows the cross-node collision. Blind INSERT OR IGNORE or ON CONFLICT DO NOTHING (the signals-table pattern): rejected because it swallows the cross-node collision that union-uniqueness exists to raise loudly (OD1).
**Mechanisms:** node_handles, slug_collision
**Scope:** identity, store, substrate, sync
**Amends:** node-handles-insert-is-same-node-idempotent-not-blind-conflict
**Cites:** node-handles-recomputed-per-parsed-entry-removed-handles-dropped


### agent-file-block-thin-and-version-marked-for-drift-detection

**Decided:** The mitos agent-file pointer block that projects paste into AGENTS.md/CLAUDE.md/GEMINI.md is kept deliberately thin (durable pointers only: run `mitos status`, read SETUP.md, and the tools are self-describing) with the volatile detail (required fields, the slug rule and its length cap, when-to-record) living in the always-fresh surfaces -- the MCP tool schemas (which ship with the code and enforce the required args) and SETUP.md -- never duplicated into the pasted copy; the block carries a hidden version marker (mitos-agent-guide vN), `mitos agent-block` is the single source that emits it, and `mitos status` plus `mitos agent-block --check` detect a stale paste by the marker or the block's distinctive markdown heading so a copied note is refreshed rather than silently rotting.
**Rejected:** (1) Inlining the volatile detail (required fields, slug cap) directly into the pasted agent-file block (the prior iteration's approach): rejected -- the block is a copy, and neither a pipx upgrade nor the GitHub-hosted SETUP.md ever updates a copy already pasted into a project's agent file, so inlined detail rots silently; the MCP schema already enforces and states the required slug, so the copy should POINT at the self-describing tools, not duplicate them. (2) Keeping the canonical block inlined in SETUP.md SS6 as the thing to copy: rejected -- that is a second copy that drifts against the code; making `mitos agent-block` (code) the single source and having SETUP.md tell the user to run it eliminates the second copy entirely. (3) Detecting drift with a loose 'file mentions mitos' heuristic: rejected -- it false-positives on files that merely document mitos or its tools (mitos's own dev guide names record_decision/surface_decisions), and a signal that cries wolf gets ignored; keying detection on the version marker OR the block's distinctive heading is precise (precision over recall). (4) Piggybacking the existing daily 'update available' notice with a one-line 'guidance changed' note: rejected as insufficient -- it cannot tell a given project whether ITS pasted copy is behind, and fires every release regardless; a per-project marker plus a status check answers 'is THIS copy stale?'.
**Mechanisms:** mitos agent-block, mitos status, AGENT_GUIDE_VERSION, mitos-agent-guide marker
**Scope:** cli, docs, onboarding
**Context:** Arose from the slug-mandatory work: an agent only learns record_decision needs a slug by hitting the error. Putting the detail in the agent file made it discoverable but the file is a copy that goes stale. This decision resolves that tension structurally.


### node-handles-legality-parity-v1b-established-not-inherited

**Decided:** The alias-to-slug write-side legality parity (an Aliases handle admits exactly the handles a node slug could) is an invariant V1b must establish by applying one legality rule-set to both minted slugs and aliases, not one inherited from V1a, which unifies no single slug-legality validator: V1a character-class-restricts only the auto-mint slugify path while a hand-authored header slug is casefold-only, and only the str.casefold fold (no NFC) is the concrete inherited half.
**Rejected:** Asserting the parity already exists in V1a's r1/r2 discipline (the prior framing of the buffered ADR node-handles-validation-matches-slug-write-side): rejected on codebase verification. r1 is a permissive casefold/matching discipline that accepts cased non-ASCII and rejects nothing on character grounds, r2 is the FieldName-recognition char-class (field-name parsing, not slug legality), and slugify's ASCII char-class is the auto-mint transform that never gates a hand-authored slug. There is no unified slug-legality validator for an alias to reuse, so claiming one would send a planner hunting for code that is not there.
**Mechanisms:** node_handles, slugify, casefold
**Scope:** identity, store, substrate
**Context:** Corrects the grounding of the buffered ADR node-handles-validation-matches-slug-write-side (decisions.md, not yet synced to the graph), which claimed the parity is inherited from V1a's r1/r2 discipline. Fold it as an amends onto that entry once it syncs. The decision itself (parity of legality plus fold between alias and slug) stands; only its grounding is corrected, and the concrete shared legality rule is the plan's call. Surfaced during V1b vision-review codebase verification.


### steady-state-batch-oldest-first-flow-heuristic-over-quarantine

**Decided:** Mitos V1b processes each steady-state sync file's entry batch oldest-first (reverse the newest-on-top buffer, reusing cutover's existing _parse_file_reversed chronological reversal) as a common-case flow heuristic that resolves same-file unidirectional forward-references and the common handle-handoff in one sync, while per-entry quarantine plus the loud slug_collision and re-sync convergence remains the correctness floor for the cross-file bidirectional OQ-to-D dependency and any convention-violating batch, so correctness never rests on the file-position age-proxy.
**Rejected:** (1) Leaving steady-state top-down with quarantine only (the prior vision stance): rejected because newest-on-top authoring guarantees same-batch amend/narrows/supersedes chains and handoffs forward-ref on the first sync, making the 2-sync tax a routine flow break on the most-used amend-chain workflow. (2) A topological sort or two-phase all-DELETEs-then-all-INSERTs batch ordering: rejected, it adds machinery, breaks per-entry atomicity and MI-12 crash-safety, and topo-sort is nondeterministic on reused mutable slugs (already tried and reverted in cutover). (3) An intra-sync retry queue: rejected as pure added machinery, since per-entry quarantine already makes re-running sync the retry. (4) Treating oldest-first as a correctness mechanism: rejected because its age-proxy is file position (the newest-on-top convention), the same unreliable authoring-age proxy rejected for the rowid cycle-check, so it stays a flow optimization layered over the quarantine floor.
**Mechanisms:** perform_sync, parse_file_reversed, quarantine
**Scope:** sync, store, substrate
**Context:** Verification this round confirmed steady-state perform_sync processes the decisions buffer top-down with no reorder, while cutover already reverses each newest-on-top file to oldest-first via _parse_file_reversed. Gemini raised that top-down processing of a newest-on-top buffer makes the same-file forward-ref 2-sync tax a routine flow break, not a rare edge case; reusing the existing oldest-first reversal removes it for the common case at zero new machinery. Conceptually amends both the quarantine self-heal and the handle-handoff ADRs by adding the oldest-first common-case layer while preserving their quarantine floor.
**Amends:** cross-file-forward-ref-per-entry-quarantine-self-heal
**Cites:** same-sync-handle-handoff-surfaces-collision-not-batch-ordering


### mutation-cycle-prevented-by-write-time-reachability-not-scalar-insertion-order

**Decided:** Mitos V1b enforces mutation-graph acyclicity (the supersedes ∪ amends ∪ narrows union get_lineage walks) with a genuine write-time reachability probe — before inserting a mutation edge A→B, walk the existing mutation union outward from B and reject the insert as a vector error iff B already reaches A (the edge would close a cycle), O(reachable lineage) ≈ O(chain depth) via a recursive CTE or visited-set — NOT the O(1) scalar insertion-order comparison (source strictly newer than target on a tie-free rowid/sequence). The probe rejects exactly the cycle-closing edges and only those (zero false positives); it enforces acyclicity, not a semantic new→old direction (unenforceable, since no faithful authoring-age key exists). get_lineage keeps its loud read-time homeostasis bound for out-of-band corruption.
**Rejected:** The O(1) scalar insertion-order check (the superseded mechanism — every mutation edge's source must be strictly newer than its target on a tie-free rowid/monotonic sequence): rejected because the insertion-order key encodes COMMIT order, never authoring age, so it produces systematic, PERMANENT false rejections of valid acyclic edges. Concrete: decisions.md is authored newest-first and steady-state perform_sync parses top-down with no reorder (oldest-first is cutover-only), so two entries committed in one batch get the NEWER one inserted first with the SMALLER rowid; later adding `Amends: <older-slug>` to the newer entry (newer-amends-older — the canonical valid direction, EXTRA_CONTEXT §1's amend-as-repair workflow) commits the edge in place (edges are not canonical core, so the rowid is frozen), the target already exists (no quarantine defers it), and `source.rowid > target.rowid` is FALSE → the valid edge is rejected forever, no re-sync heals it. The scalar's claimed direction-enforcement is phantom (it enforces rowid order, not authoring age) and its claimed strictly-stronger-than-reachability property is false (it is strictly WRONG on valid input). A reachability probe rejects only edges that actually close a cycle, with no false positives, at O(chain depth) the get-lineage-carries-loud-homeostasis-cycle-bound cost analysis already blessed as within budget (O(reachable lineage), not O(corpus)). Also rejected (unchanged from the parent): dropping write-time prevention for read-only defense; and dropping get_lineage's loud homeostasis bound (still required for a corrupt/out-of-band cycle the write path never authored).
**Mechanisms:** reachability, recursive-cte, rowid, insertion-order, supersedes, amends, narrows, get-lineage, cycle-violation
**Scope:** substrate, store, edges, retrieval
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-21, Gemini's Saboteur-lens finding. The scalar rowid check was Gemini's OWN prior-round Finding-3 optimization (mutation-cycle-prevented-by-scalar-insertion-order-not-reachability-traversal, 2026-06-21) — it assumes insertion order matches logical dependency order, which it does not. The loop's anti-knowledge caught an overly-aggressive simplification one round after it landed (the recall→"we weighed this; new info?" workflow working as designed). Claude grounded the false-positive in the live code (newest-first buffer + top-down steady-state perform_sync + in-place edge commit freezing the rowid) and confirmed it is PERMANENT, not quarantine-healed (the target pre-exists, so no missing_target defers the source to a later, larger-rowid insert). Both converged on reverting to genuine write-time reachability. Supersedes mutation-cycle-prevented-by-scalar-insertion-order-not-reachability-traversal; restores and cites mutation-cycle-prevented-at-write-time-not-read-defended (the reachability design the scalar ADR had amended away) and get-lineage-carries-loud-homeostasis-cycle-bound (the O(chain-depth) cost blessing). A Claude+Gemini loop ADR, framework-settled on correctness (P3 — a wall on valid input is worse than a vector error). Recorded directly in decisions.md because the dogfood graph predates V1a and awaits cutover, so the buffer is the only writable surface this round; the supersede/cite edges reconcile on rebuild. Applied to vision §1.1, §4.3 (Cycle prevention), §1.2 DoD #11, §6.1 cluster 4, §10 P16/P17.
**Supersedes:** mutation-cycle-prevented-by-scalar-insertion-order-not-reachability-traversal
**Cites:** mutation-cycle-prevented-at-write-time-not-read-defended, get-lineage-carries-loud-homeostasis-cycle-bound


### edge-catalog-check-foreclosure-holds-at-scale-not-on-cheap

**Decided:** Mitos forecloses the edge_type_matrix reference table in favour of the single table-level CHECK on durability grounds that hold even at Dyson scale: a CHECK-widening edges-rebuild is a rare, one-time, blocking migration step (O(edges), run during ladder replay on a local single-user no-SLA tool) whereas the reference table would tax every edge write forever with a composite-FK lookup, so the foreclosure does NOT rest on the scale-fragile premise that rebuilds stay milliseconds at Mitos scale.
**Rejected:** Re-justifying the edge_type_matrix reference table by puncturing the parent ADR scale-fragile premise (that rebuilds are milliseconds at Mitos scale): rejected, because the rebuild-versus-reference-table economic asymmetry holds regardless of rebuild cost. Even when a CHECK-widening rebuild is genuinely not cheap at Dyson scale (O(edges), seconds on a 100k-edge corpus), it is paid rarely, once, as a blocking migration step on a local no-SLA tool, while the reference table pays a composite-FK lookup on every edge write forever. The parent ADR reached the right conclusion via a scale-naive justification; this amends hardens that justification so a future fresh-eyes reviewer cannot re-open the reference table by attacking the cheap-rebuild premise. Also rejected: leaving the parent rationale as-is, because a too-weak rejected-path is the decision-health smell that lets relitigation leak through (the foreclosure must be as durable as the decision).
**Mechanisms:** check-constraint, edges-rebuild, edge-type-matrix
**Scope:** edges, schema, store, substrate
**Context:** V1b parser-and-store vision-loop paired review (Claude plus Gemini), economics lens, 2026-06-21. Finding A: the parent ADR forecloses the reference table partly on rebuilds-are-milliseconds-at-Mitos-scale, a P11 scale-naive premise a fresh-eyes reviewer could puncture to re-open the alternative. Decision unchanged (CHECK, no reference table); this amends only hardens the rationale onto the rebuild-is-rare-one-time-migration versus reference-table-is-per-write-forever-tax asymmetry, which holds regardless of rebuild cost. Vision sections 3 and 4.3 updated to match.
**Amends:** edge-catalog-stays-check-constraint-not-reference-table


### per-entry-quarantine-isolates-whole-commit-error-class

**Decided:** V1b's per-entry sync quarantine (the cross-file-forward-ref-per-entry-quarantine-self-heal mechanism) isolates the offending entry on the WHOLE per-entry store-rejection class — every FailureItem the single §5.2.2 CommitError/EntryFailure envelope can carry: missing_target, slug_collision, the mutation-cycle rejection, kind_constraint_violation, dangling_edge — not a missing_target-only filter. Every per-entry structural rejection therefore quarantines uniformly and the rest of the batch advances; the catch granularity (catching the CommitError class vs an all-codes allowlist) is the plan's call, the breadth is the contract.
**Rejected:** A missing_target-scoped quarantine (isolate iff the FailureItem code is missing_target, else re-raise / abort the sync): rejected — it silently breaks same-sync-handle-handoff-surfaces-collision-not-batch-ordering's convergence guarantee. If node B claims handle foo before node A relinquishes it in the same sync, B's slug_collision aborts the whole sync, A never relinquishes, and every re-sync re-wedges at B — a permanent wedge, not the promised at-most-two-syncs convergence — and it makes the P7 Bulkhead non-uniform (a forward-ref isolates but a collision / cycle / kind-violation bricks the batch). The per-entry TRANSACTION (V1-D10) is not enough on its own: it rolls B back atomically but does not stop B's CommitError from propagating out of the loop; only per-entry fault-ISOLATION keeps the batch advancing.
Catch the CommitError class but collapse every code to the forward-ref guiding vector ('target not committed yet — re-run mitos sync'): rejected — slug_collision, the mutation-cycle rejection and kind_constraint_violation each already carry their own P3 guiding vector (the colliding handle + its holder; the cycle path; the offending kind shape), so the quarantine preserves each FailureItem's own message rather than mislabelling them all as a forward-ref.
**Mechanisms:** per-entry-transaction, slug-collision, missing-target, commit-error, node-handles
**Scope:** sync, store, substrate
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-20, Gemini's Vector-Error / Quarantine-Integration finding. The parent quarantine ADR's Decided was scoped to "isolates the missing_target", while same-sync-handle-handoff-surfaces-collision-not-batch-ordering's "at most two syncs regardless of processing order" convergence silently depends on the quarantine ALSO catching slug_collision — an unrecorded cross-ADR coupling. Claude verified the error taxonomy in the shipped store.py (all per-entry structural rejections — missing_target, slug_collision, cycle_violation, kind_constraint_violation, dangling_edge — funnel into one store_failures list raised as a single CommitError inside the per-entry `with conn:`, store.py:1186-1201; the sync loop has no per-entry try/except, sync.py:585-593) and both converged. Amends cross-file-forward-ref-per-entry-quarantine-self-heal — widens its error-class scope from missing_target to the whole CommitError class without invalidating it; cites same-sync-handle-handoff-surfaces-collision-not-batch-ordering (the convergence guarantee that motivates the breadth). A Claude+Gemini loop ADR, not a Vinga manual-round call. Recorded directly in decisions.md because the dogfood graph predates V1a and awaits cutover, so the buffer is the only writable surface this round; the amend/cite edges reconcile on rebuild. Applied to vision §1.2 DoD #12, §4.1, §6.1 cluster 3, §10 P10.
**Amends:** cross-file-forward-ref-per-entry-quarantine-self-heal
**Cites:** same-sync-handle-handoff-surfaces-collision-not-batch-ordering


### cross-file-forward-ref-per-entry-quarantine-self-heal

**Decided:** A cross-file OQ<->decision forward-reference (derives_from OQ->D, resolves D->OQ) whose target is not committed yet is handled by per-entry quarantine: perform_sync isolates the missing_target as a per-entry vector error so the other entries including the target still commit, and the quarantined entry stays in the buffer and self-heals on the next sync once its target landed — never an intra-sync retry queue, never a cross-file reorder, never a sync-abort
**Rejected:** Claim it 'self-heals on the next sync exactly as supersedes forward-refs do today' (the vision's prior wording): rejected as inaccurate — the code shows supersedes forward-refs self-heal only via the cutover rebuild's oldest-first reorder (cutover.py), not steady-state perform_sync, and that reorder provably cannot order the bidirectional OQ<->D dependency (no single file order satisfies both derives_from D-first and resolves OQ-first).
An intra-sync retry queue (Gemini's V1b proposal): rejected — it is another instance of the in-sync ordering machinery the project twice chose against on P19 grounds (same-sync-handle-handoff-surfaces-collision-not-batch-ordering, cutover-single-pass-oldest-first-drops-commit-batch); it still raises on a genuine 2-node mutual-reference cycle; and per-entry quarantine (needed for cluster-3 malformed-entry isolation anyway) already self-heals the realistic acyclic case across two syncs at zero extra machinery.
Leave perform_sync's current behaviour (no per-entry try/except at sync.py:586, so an uncaught CommitError aborts the whole sync): rejected — once V1b flips derives_from/resolves from warn-defer to commit, a plausible 'author a decision plus a derived OQ in one session' aborts the entire sync; perform_sync must isolate the fault per entry (P5 dead-letter / P7 bulkhead) and the missing_target must be a guiding vector ('target not committed yet — re-run mitos sync'), not the bare 'does not match any entry' wall that reads as a typo (P3).
**Scope:** sync, store, substrate
**Context:** V1b review (2026-06-21). Gemini flagged the cross-file forward-ref UX as a P3 trap; code verification (store.py:1191-1201 whole-entry rollback on missing_target, sync.py:586 no per-entry try/except, cutover.py oldest-first reorder as the only existing self-heal) showed the vision's 'self-heals exactly as supersedes' analogy was inaccurate and that perform_sync per-entry fault-isolation is load-bearing V1b work, not a free V1a inheritance. Extends the loud + self-heal-on-resync, no-ordering-machinery stance to cross-file OQ<->decision forward-refs.
**Cites:** same-sync-handle-handoff-surfaces-collision-not-batch-ordering


### get-lineage-cycle-bound-is-nonfatal-partial-return-not-abort

**Decided:** The get_lineage read-time homeostasis cycle bound is non-fatal: on a corrupt or out-of-band cycle it truncates the walk, emits a loud diagnostic naming the node, and returns the partial lineage — it never raises or aborts the caller, so a single corrupt cycle in the derivative graph never bricks the hot record write path (P5/P7/P14)
**Rejected:** Read 'raises a diagnostic ... and terminates' as throw-and-abort (the vision's ambiguous wording): rejected — get_lineage runs on every record via the near-dup gate, so a throw on one out-of-band corrupt cycle would lock the user out of recording until a manual wipe-and-rebuild, violating P5 (Unplugged Standard / self-healing) and P7 (a corrupt derivative must not take down the write path). Surfaced by Gemini, V1b round 2026-06-21.
A silent visited-set that merely truncates (the parent ADR's already-rejected path): still rejected — it hides the corruption. The bound stays LOUD (a diagnostic naming the node); the refinement is only that 'loud' is a stderr/log diagnostic plus partial return, not an exception that aborts the caller. Loud+partial, not silent+partial and not loud+abort.
Make the near-dup gate's caller catch and swallow the get_lineage exception: rejected — pushes crash-safety into every consumer (the parent ADR's 'lives once in the store-owned walk' concern); the non-fatal behaviour belongs in the single store-owned walk that v0.2 Drift and V5 traversal inherit.
**Scope:** edges, retrieval, store, substrate
**Context:** V1b review (2026-06-21). get_lineage is not yet implemented (V1b-planned; the near-dup gate is still pure vector-similarity, confirmed in code), so pinning non-fatal semantics now steers the build before it is written. The parent ADR get-lineage-carries-loud-homeostasis-cycle-bound already wants 'loud' AND a 'crash-safe hot-path primitive' — this resolves the wording ambiguity in favour of the parent's own intent: truncate the walk, emit a loud diagnostic, return the partial lineage, never abort the caller.
**Amends:** get-lineage-carries-loud-homeostasis-cycle-bound


### oq-hygiene-drop-is-lifecycle-gardener-not-forced-decision

**Decided:** Retiring an open question via a resolves non-action decision is the substrate path only for an abandonment whose reason is worth keeping (M5); pure workspace-hygiene drops of a never-load-bearing OQ are not forced through a decision but left to Vision 7's Lifecycle Gardener (the named P4 GC for the open-questions channel), and V1b adds no OQ retirement marker (M3/Q1)
**Rejected:** An [ABANDONED]/[WITHDRAWN] inline OQ marker parsed as a state (Gemini's V1b-round proposal): rejected — it reintroduces a stored OQ state token that M3 (state computed from edges, never stored) and Q1 (which deliberately removed inline OQ markers) both forbid, and reverses by symmetry Vinga's 2026-06-20 terminal ruling that killed the [RETIRED] marker for decisions in favour of the supersedes-deletion-decision.
An OQ->OQ supersedes 'tombstone' carrying a 'closed' semantic so the tombstone doesn't sit parked: rejected — OQ->OQ supersedes already means 'this question refines/replaces that one' and the refined question is legitimately still open (parked), so overloading it with 'closed' both conflates two meanings and leaves the tombstone parked forever (the trap Gemini identified). The graph physics make a clean marker/tombstone drop impossible without a stored state M3 forbids.
Keep 'the single sanctioned retirement is a resolves decision' (the un-narrowed parent claim): rejected — it overclaims by denying the pure-hygiene case and offers no path for the parked-orphan a buffer-drop leaves; Vision 7's Lifecycle Gardener IS the named P4 GC for those, so the claim is factually incomplete against the ROADMAP.
**Scope:** substrate, retrieval, store
**Context:** V1b review (2026-06-21). Gemini's fresh pass argued the resolves-decision pollutes the millennial decision log with non-decisions. Narrowing, not reversing: the resolves non-action decision stays correct for warranted abandonment (M5 — 'we decided not to pursue X' is a real decision); the pure-hygiene drop routes to Vision 7's Lifecycle Gardener (P20-deferrable, additive, no walls broken), and a buffer-dropped OQ computes parked and waits (accepted v0.1 entropy, same bucket as R4) until then. No marker (M3/Q1/Vinga 2026-06-20).
**Amends:** oq-retirement-is-resolve-with-non-action-not-cross-kind-kill


### v4-render-full-regen-not-cascade-targeted

**Decided:** Mitos V4 renders by full regeneration from the computed active view each sync (recompute all scope renders, write only content-changed files: O(active corpus) compute, O(delta) writes), not by a CommitDelta.cascade_affected_scopes-targeted rerender set — so the transitive cascade is dropped, MI-6 (cascade exhaustiveness) dissolves, and cascade_affected_scopes stays a first-order produced hint that V4's render path no longer consumes for targeting
**Rejected:** Keep CommitDelta.cascade_affected_scopes transitive (the superseded commit-delta-cascade-transitive): rejected — under full regeneration there is no targeted scope-rerender set to make exhaustive, so the transitive walk and the MI-6 exhaustiveness invariant it served are dead weight; the zombie-render seam it closed (an OQ resolved by a now-superseded decision rendering as 'resolved') is structurally unreachable when every scope is recomputed from the computed active view each sync.
Defensive re-render of every scope file on every sync (rewrite all): rejected — it churns git on the first one-decision change of any multi-scope project, breaking P5's re-sync-is-a-no-op guarantee. Full regen computes O(active corpus) but writes O(delta) — an unchanged scope produces no write via content-equality (compare-on-write or stored content hash, plan's call).
Leave the pivot recorded only in the vision (§5.2.4) and the stale code comments (store.py:1204, get_transcript docstring): rejected — commit-delta-cascade-transitive then keeps surfacing as ACTIVE precedent for a transitive cascade the design abandoned (the project's own 'amended axioms read as live' trap); the decision log must carry the supersession so a future planner's surface() reads the live full-regen design.
**Mechanisms:** candidate-invariant-mi-6, cascade-affected-scopes
**Scope:** render, store, substrate
**Context:** V1b review round (2026-06-20/21), found by fresh-eyes review via mitos surface. The vision §5.2.4 and the shipped code (store.py:1204, "cascade_affected_scopes is FIRST-ORDER as of 5c") already adopted full-regen, but the pivot was never recorded as an ADR, leaving commit-delta-cascade-transitive active and contradicting the live design. commit-delta-cascade-contract stays active (the first-order CommitDelta payload is still produced by the commit path); only its V4-render-targeting-consumption clause ("V4 unions cascade_affected_scopes into the scope-rerender set") is amended — V4 full-regenerates instead. The vision already flags the stale code comments (line 164); this closes the decision-log half.
**Supersedes:** commit-delta-cascade-transitive
**Amends:** commit-delta-cascade-contract


### node-handles-recomputed-per-parsed-entry-removed-handles-dropped

**Decided:** Mitos node_handles rows for a parsed entry are fully recomputed every sync — the node's prior rows are dropped and re-inserted from the union of its current slug and current Aliases — so a handle removed from the source is dropped (never accumulated), with the recompute scoped to entries parsed this sync so unparsed, deleted, or archived nodes' handles persist untouched.
**Rejected:** Append-only / accumulate-on-insert handle writes (never dropping a handle the source removed): rejected because a removed or typo'd alias would then resolve forever, permanently polluting the all-node uniqueness namespace and silently resolving citations to a node after the author deleted that handle. Also rejected: routing a live-entry alias removal through the explicit archived-slug reclamation valve — that valve exists only because archived entries are never re-parsed in steady-state sync; a live entry's edit is already the explicit, user-chosen action, and the citation it strands fails loudly as missing_target, so OD1 holds without invoking a separate valve.
**Mechanisms:** sqlite, node-handles
**Scope:** identity, store, substrate, sync
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-20. Gemini's Finding 4 asked that the flat-set node_handles rebuild be a per-node DELETE-then-INSERT so removed aliases drop rather than accumulate; Claude agreed and pinned the scope (parsed-this-sync only; unparsed/deleted/archived persist per no-GC) plus the complementarity with the section 4.1 archived-slug reclamation valve. Applied to vision section 8.2 node_handles row.
**Amends:** aliases-corpus-authored-via-aliases-field
**Cites:** archived-slug-reclamation-is-explicit-valve-not-uniqueness


### mechanism-registry-defers-aliases-json-and-kind-columns

**Decided:** Mitos V1b's mechanisms registry ships exactly four columns (canonical_name PK, authored_name, source, created_at) and pre-creates no aliases_json or kind column — both are deferred to v0.2, which adds them via a non-destructive ALTER TABLE ADD COLUMN — because P20's Retrofit Test does not fire for a nullable, non-destructively-addable column with no lost v0.1 data.
**Rejected:** Pre-creating aliases_json and kind as reserved-empty v0.1 columns (the prior V1b vision stance, which claimed P20 wire-while-the-walls-are-open cover): rejected as a P20 false-positive. P20's Retrofit Test fires only when adding the thing later is destructive; a nullable ALTER TABLE ADD COLUMN that v0.2 populates forward loses no v0.1 data and breaks no wall, so its second condition fails and P12 (don't pre-solve) governs. The parent registry ADR already found P20 does not fire for aliases_json; this applies that finding consistently and extends it to kind by the sibling-asymmetry test (kind is likewise nullable, has no v0.1 consumer or populator since auto-registration sets only canonical_name/authored_name/source, and is re-derivable later). Also rejected: deferring source the same way — source is genuine P20 because the triggering entry's provenance is lost if not captured at registration, cannot be backfilled, and must ship in v0.1.
**Mechanisms:** sqlite, mechanisms-registry, alter-table-add-column
**Scope:** substrate, store, mechanisms
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-20. Gemini's Simplification-lens Finding 1 flagged aliases_json as a P20 false-positive; Claude agreed via the Retrofit Test and extended it to kind by the sibling-asymmetry test. Applied to vision sections 1.1, 4.6, 7, 8.2 mechanisms row, and 10 (P20 + Progressive Activation).
**Amends:** mechanisms-stay-dedicated-registry-table-not-graph-nodes


### get-lineage-carries-loud-homeostasis-cycle-bound

**Decided:** Mitos V1b keeps write-time mutation-cycle prevention as the primary invariant and additionally gives the single store-owned get_lineage walk a cheap, loud read-time homeostasis bound, so a corrupt or out-of-band cycle the supported write path never authored surfaces as a diagnostic naming the node rather than infinite-looping or silently truncating the hot-path primitive; this is defense-in-depth (the write path stays clean, the bound is loud, and it lives once where v0.2 Drift and V5 traversal inherit it), not a reversal of write-time prevention.
**Rejected:** Dropping write-time cycle prevention and defending only at read time (Gemini's Finding-2 headline, 2026-06-20): rejected because the mitos anti-knowledge surfaced it as the very rejected path recorded in the parent ADR mutation-cycle-prevented-at-write-time-not-read-defended — and as Gemini's own prior-round (2026-06-18) position, reversed. The parent's grounds still hold: a mutation cycle is a temporal paradox violating the new->old directional invariant; read-only defense permits illegal state at the core and silently truncates; write-time rejection is a clean P3 author-time vector error. The P17 cost alarm against the write-time probe is calibrated down by real complexity — it walks O(reachable lineage) ~= O(chain depth), not O(N corpus) — so the probe is within budget and the clean-core benefit is worth its price.

Keeping the parent ADR's literal "get_lineage needs no read-time cycle-defense" with no defensive bound at all: rejected because it leaves the hot-path get_lineage (run on every record via the near-dup gate) able to infinite-loop on a corrupt or out-of-band cycle. The graph is a rebuildable, inspectable SQLite derivative (M7/P6), so an out-of-band edit, a bug, or a future migration can present a cycle the supported write path never authored — a P5 (Unplugged Standard) / P7 (Bulkhead) homeostasis breach the 2026-06-18 round never weighed. This is the genuine gap Gemini's fresh pass found even while relitigating the headline.

A silent visited-set that merely truncates the walk at a cycle: rejected — it hides the corruption, the exact "silently truncates and hides the bug" failure the parent ADR named. The bound MUST be loud (raise a diagnostic naming the node), surfacing the bug rather than swallowing it.

Re-rolling cycle defense in each consumer (get_lineage now, v0.2 Drift and V5 traversal later): rejected — the parent ADR's "pushes defense into every shell consumer" concern. The bound lives once, in the store-owned get_lineage walk, and future consumers inherit it; the discipline "store-owned graph walks carry a loud cycle bound" is one rule, not per-consumer machinery.
**Mechanisms:** get-lineage, cycle-violation, amends, narrows, supersedes, edges
**Scope:** edges, retrieval, store, substrate
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-20, ECONOMICS lens round. Gemini's Finding 2 proposed dropping write-time cycle prevention in favour of read-time defense. Checking precedent with mitos surfaced the parent ADR mutation-cycle-prevented-at-write-time-not-read-defended (2026-06-18), whose rejected_paths already hold exactly that option — and whose Context credits the write-time direction to Gemini's own Finding that round; fresh-eyes Gemini had reversed himself, and the loop's anti-knowledge caught it (the recall->"we weighed this; new info?" workflow working as designed). But his fresh pass surfaced one genuinely-new angle the prior round never weighed: with literally no read-time defense, the hot-path get_lineage can infinite-loop on a corrupt/out-of-band derivative graph (P5/P7). Claude's synthesis keeps write-time prevention (no reversal of the parent) and adds a cheap LOUD homeostasis bound inside the single store-owned get_lineage walk — defense-in-depth that threads all three of the parent ADR's rejection grounds (clean core, loud not silent, single-sourced not per-consumer). Both converged. Applied to vision sections 1.1, 4.3 (the single-sourced home), 1.2 DoD #11 (added a third "homeostasis case" gate axis — a deliberately-seeded corrupt cycle must raise a diagnostic and terminate), and 6.1 cluster 4. Amends, does NOT supersede, the parent. Recorded directly in decisions.md because the dogfood graph predates V1a and awaits cutover, so the buffer is the only writable surface this round; the amend edge reconciles on rebuild.
**Amends:** mutation-cycle-prevented-at-write-time-not-read-defended


### node-handles-fold-matches-slug-casefold-exactly

**Decided:** Mitos's node_handles.handle_casefold MUST be produced by the identical fold transform as nodes.slug_casefold — the shipped bare str.casefold() with no NFC normalization — so a node slug and a same-string alias fold to identical bytes and actually collide under the UNIQUE(handle_casefold) index that enforces slug-alias union-uniqueness.
**Rejected:** An independent or NFC-augmented handle fold (e.g. NFC-then-casefold as mechanism_canonical_norm uses, or any fold differing from the shipped slug path slug.casefold() in store.py) — rejected because a slug stored under one fold and a same-string alias under a divergent fold produce different handle_casefold bytes, pass the UNIQUE index as distinct, and let a fresh slug silently capture an existing alias's citations: the exact OD1 (never silently mis-resolve) break union-uniqueness exists to prevent, made invisible because the index still looks like it is enforcing. NFC robustness on handles alone is the tempting wrong move; closing NFC-variant citation rot is a coordinated slug+handle change, out of V1b extend-not-rewrite scope.
**Mechanisms:** handle_casefold, slug_casefold, str-casefold, node_handles
**Scope:** identity, store, substrate
**Context:** Surfaced in the V1b vision review (2026-06-20). Shipped code: store.py computes slug_casefold = slug.casefold() (bare, no NFC), while canonical_core_string_norm and mechanism_canonical_norm both NFC-normalize first, so the slug fold is deliberately bare. Vision section 8.2 specified handle_casefold as Python str.casefold() without pinning equality to the slug path, and section 4.6 calls the slug fold the Appendix B slug-fold rule, which reads like more than bare casefold — an ambiguity a planner could resolve wrongly.
**Amends:** slug-alias-union-strict-casefold-uniqueness
**Cites:** node-handles-insert-is-same-node-idempotent-not-blind-conflict


### oq-retirement-is-resolve-with-non-action-not-cross-kind-kill

**Decided:** An unwanted open question is retired by authoring a decision that resolves it (including a non-action decision such as deciding not to pursue the line of inquiry, with the reason), never by a cross-kind kill-edge from a decision, because the edge kind-matrix structurally forbids a decision-to-open-question supersedes or corrects; resolving flips the open question to resolved and drops it from the open-questions channel while preserving why it was set aside (M5).
**Rejected:** Allowing a cross-kind decision→open_question `supersedes` (or `corrects`) kill-edge so a decision can directly kill an OQ — rejected on two grounds. First, it breaks the edge kind-matrix: `supersedes` and `corrects` are same-kind (D→D or OQ→OQ), and widening them to cross-kind would let a decision deactivate a question, conflating two node kinds the substrate deliberately keeps distinct. Second, and decisively, a kill-edge drops the target from the active view leaving no surfaceable record of *why* the question was abandoned — the same M5 anti-knowledge loss that steered successor-less decision death toward a deletion decision rather than a `[RETIRED]` marker. Modelling OQ retirement as a `resolves` edge from a non-action decision keeps the *why* alive and surfaceable (the resolving decision stays active), exactly mirroring the deletion-decision form for decisions (`successor-less-death-as-deletion-decision-not-retired-marker`).

Silently dropping the OQ entry from the `questions.md` buffer — rejected because the no-graph-GC rule keeps the node, so a dropped-from-buffer OQ with no incoming kill-edge and no `resolves` sits `parked` forever and floods V5's open-questions channel. Deletion from the buffer is not a retirement signal (the boundary case is symmetric with §4.5.1's decision path).
**Mechanisms:** resolves, oq_state_view, edge-kind-matrix
**Scope:** substrate, store, retrieval
**Context:** Surfaced in the V1b vision review (2026-06-20, Gemini's pass) by stress-testing the OQ lifecycle: how does an OQ the author no longer cares about actually die? The answer is emergent from two prior decisions — the edge kind-matrix forbidding cross-kind `supersedes`/`corrects`, and `oq-stage2-resolution-stays-explicit-edge` — but was never named, leaving a P4 ("explain how it dies") coverage gap symmetric to the thorough successor-less-decision-death treatment in §4.5. This ADR names the OQ-death path so V3a (OQ deletion-reconciliation UX) and V5 (open-questions channel) build on it, and forecloses the tempting cross-kind-kill-edge alternative. Parallels `successor-less-death-as-deletion-decision-not-retired-marker` on the OQ side.


### node-handles-insert-is-same-node-idempotent-not-blind-conflict

**Decided:** Writes to the node_handles table are idempotent only for the same node re-inserting its own handle; a handle already held by a DIFFERENT node must raise slug_collision, so the handle write path must NOT use the blind ON CONFLICT DO NOTHING / INSERT OR IGNORE pattern that the signals table uses, because that would swallow the cross-node collision the union-uniqueness rule exists to raise loudly.
**Rejected:** Blind `ON CONFLICT DO NOTHING` / `INSERT OR IGNORE` on `node_handles` (the pattern `signals-insert-or-ignore` mandates for the `signals` table) — rejected because `node_handles` is not `signals`: a duplicate signal row is a benign no-op, but a duplicate handle from a *different* node is exactly the cross-node citation-capture collision `slug-alias-union-strict-casefold-uniqueness` promises to raise as a loud `slug_collision` (P3 vector error). A blanket swallow-all idempotency makes the cross-node INSERT shrug and return cleanly — the `slug_collision` never fires, node B silently fails to claim the handle, and the citation rots with no diagnostic: a direct OD1 (never silently mis-resolve) violation, the very failure the subsystem exists to prevent.

Bare INSERT that raises even when the *same* node re-inserts its own unchanged handle — rejected because under per-entry atomic commit (V1-D10) a re-sync of an unchanged node would abort the whole entry commit on its own handle row; same-node re-insertion must be a clean no-op for `mitos sync` idempotency (MI-3).

The structural form is therefore conditionally idempotent: a no-op only when the conflicting row already belongs to the same `node_id`, otherwise letting the global `UNIQUE(handle_casefold)` index throw so the store can raise `slug_collision`. The exact SQL shape (a guarded `INSERT ... WHERE NOT EXISTS (... AND node_id = :node_id)`, or a same-node pre-check, etc.) is the plan's call.
**Mechanisms:** node_handles, slug_collision, sqlite-unique-index
**Scope:** identity, store, substrate, sync
**Context:** Surfaced in the V1b parser-and-store vision review (2026-06-20, Gemini's pass). The slug-alias subsystem (`slug-alias-union-strict-casefold-uniqueness`, `same-sync-handle-handoff-surfaces-collision-not-batch-ordering`) promises a loud `slug_collision` on any cross-node handle clash. The hazard is that the obvious sync-idempotency shortcut for re-syncing unchanged handles — the same blind `ON CONFLICT DO NOTHING` the project already uses for `signals` (`signals-insert-or-ignore`) — silently swallows that raise. The vision pins this as a cross-cutting plan constraint in §6.2 (sibling to the `source_reencounter` short-circuit-bypass constraint); this ADR is the foreclosing record so the plan never reaches for the signals pattern on the handle write path.


### successor-less-death-as-deletion-decision-not-retired-marker

**Decided:** Mitos models successor-less decision death (a decision whose governed feature is deleted, with no replacing decision) as an ordinary deletion decision authored in the buffer carrying **Supersedes:** old-slug — not a [RETIRED] inline marker backed by a retired signal. The existing supersedes kill-edge drops the dead decision from the active view while the deletion decision itself stays active and surfaceable, so a later re-proposal of the dropped feature surfaces the deletion decision and its reasoning (M5 anti-knowledge preserved). No new substrate: no marker parse, no retired signal write path, not even a new retires edge type; the reserved retired signal slot stays unwritten like drifted, and the active-view definition stays simply "no incoming kill-edge" (V1a V1-D11, with no compound AND-no-retired-signal clause). This reverses Vinga's Q6 manual-round decision and restores consistency with graph-store-append-only-edge-driven-retirement, which already pinned that retirement is edge-driven only (supersedes/corrects).
**Rejected:** Keep the [RETIRED] marker + retired signal (Vinga's Q6 choice, the prior vision mechanism): rejected on two faults surfaced in V1b review. (i) It destroys anti-knowledge (M5) — a [RETIRED] decision drops from the active view leaving no successor node, so the active view holds no record of the dropped feature; a later re-proposal surfaces nothing and the system has forgotten WHY the feature was removed, the exact failure M5's first-class anti-knowledge exists to prevent. (ii) Buffer-vs-archive inertness — the marker lands on a decision entry, but synced decisions rotate to archives mitos sync never re-parses, so it is inert for its dominant case (an old, already-archived retirement target); you cannot mark a file sync never re-reads without re-authoring the whole entry back into the buffer, at which point it does the same append-to-buffer work as the deletion decision, with extra machinery. A new retires kill-edge (the round's runner-up): rejected on P19/YAGNI — it preserves a removed-vs-replaced distinction at the substrate, but no known consumer needs it (even v0.3's Lifecycle Gardener's real job, proposing retirement candidates for human confirmation, is served by supersedes alone; it never queries past removals by edge type), so it adds an edge type for a distinction read just as well from the surviving deletion decision's axiom. Both option 1 and the retires-edge option cleanly dodge Q6's two rejected edge-forms (a retires edge from a global sentinel node; a self-directed retires edge): option 1's source is a real content-bearing deletion decision and its edge is the existing supersedes — neither a sentinel nor a self-edge — so Q6 never weighed the deletion-decision form and this is new ground, not a relitigation of a Q6 rejected path. The cost option 1 accepts: a retirement costs a short real buffer entry rather than a one-token marker, and "removal" files under the same supersedes bucket as "replacement" (no substrate-level what-was-removed query) — but that entry IS the anti-knowledge M5 wants, and nothing in v0.1–v0.3 needs the removed-vs-replaced query.
**Mechanisms:** supersedes, active-view, m5, anti-knowledge, retired-signal
**Scope:** store, substrate, identity, sync, render
**Context:** V1b parser-and-store vision-loop review escalated Q-Retire-Mechanism to Vinga (it reverses her Q6 manual-round decision and reaches ROADMAP Vision 7 / the ROADMAP text). Vinga ruled for the supersedes-only deletion-decision form in the terminal, 2026-06-20, after walking through the three options and confirming the key point: nothing is deleted — both removed and replaced decisions are retained append-only and droppable from the active view only; option 1 makes removal behave like replacement (a supersession), strictly MORE visible than the marker (which left no successor trace), the only thing given up being the typed removed-vs-replaced distinction, which no consumer needs. Supersedes successor-less-death-via-retired-marker-and-signal. Restores consistency with graph-store-append-only-edge-driven-retirement (retirement is edge-driven only). Applied to vision sections 1, 1.2 (DoD #6 and #7), 3, 4 (honours-list), 4.3 (modifier source-liveness filter), 4.5 (rewritten), 4.5.1, 4.7, 6.2, 7, 8.2, and 10 (M5, P4); and to ROADMAP Vision 7. Recorded directly in decisions.md because the dogfood graph predates V1a and awaits cutover, so the buffer is the only writable surface.
**Supersedes:** successor-less-death-via-retired-marker-and-signal
**Cites:** graph-store-append-only-nodes-declarative-mirror-edges


### this-should-fail-because-no-slug-is-provided

**Decided:** This should fail because no slug is provided
**Rejected:** Because it lacks a slug


### record-requires-explicit-slug

**Decided:** The mitos CLI requires an explicit slug for all recorded decisions to prevent immutable awkward auto-slugs.
**Rejected:** Leaving the slug parameter optional. Rejected because long axioms auto-derive awkward handles that get locked into the V1a canonical-core hash, forcing permanent ugly slugs or tedious corrects-edge renames.
**Scope:** cli, ax


### semantic-confidence-threshold-bands

**Decided:** The semantic search confidence threshold uses a three-tiered system: strong >= 0.75, weak (twilight zone) >= 0.60, and likely off-axis < 0.60.
**Rejected:** Retaining the binary 0.80 strong threshold. Rejected because the gemini-embedding-2 task prefixes ('search_query:' vs 'search_document:') create a mathematical ceiling near ~0.95 for perfect textual matches. The 0.80 threshold is too aggressive, incorrectly classifying question-phrased queries of exact precedents as weak noise. The <0.60 likely off-axis banner prevents the junk-drawer effect on forced-scope queries.
**Scope:** retrieval, ax


### mutation-cycle-prevented-at-write-time-not-read-defended

**Decided:** Mitos V1b makes the mutation graph — the union of supersedes ∪ amends ∪ narrows that get_lineage walks — acyclic by construction, rejecting at write time as a vector error any mutation-family edge whose insertion would close a union cycle (including a mixed cycle closed by a supersedes edge running back through an existing amends/narrows), so get_lineage is a plain transitive walk with no read-time cycle-defense; this extends V1a's structural-acyclicity-over-runtime-walker stance to the non-kill mutation edges, where V1a's active-source guard gives no protection because amends/narrows leave both endpoints active.
**Rejected:** Read-time cycle-safety inside get_lineage (the prior V1b vision stance — get_lineage defends via a recursive-CTE CYCLE clause or visited-set because the write path does not prevent amends/narrows cycles): rejected as the very runtime-walker Lesson 2 and kill-edge-acyclicity-requires-active-source already refused for the corrects walk — it pushes cycle-defense into every shell consumer (get_lineage now, v0.2 Drift and V5 traversal later), each re-rolling it, while permitting an illegal state at the core; a mutation cycle is a temporal paradox (A amends B amends A makes each newer than the other, violating the new→old directional invariant), and write-time rejection is a clean P3 author-time vector error where read-time defense silently truncates and hides the bug. "Simply extend V1a's existing kill-edge cycle check": rejected as a mischaracterization — V1a's guard is source-active + no-self-edge and yields acyclicity ONLY because a kill-edge deactivates its target (an inactive node cannot be a source, so the chain cannot close); amends/narrows are non-kill and leave both endpoints active, so that guard gives zero protection and a genuine write-time reachability traversal is required. Per-edge-type checks (forbid only an amends-only or narrows-only cycle): rejected because a cross-kind cycle (A supersedes B, B amends C, C narrows A) slips every per-type check and still loops get_lineage, which walks the union — the invariant must be over the union graph. Write-cycle-preventing the other four new edges (depends_on/contradicts/derives_from/cites): not done — cites cycles are legitimate and the rest are not in get_lineage's walk, so v0.1 leaves them a downstream consumer concern.
**Mechanisms:** get_lineage, edges, supersedes, amends, narrows, cycle-violation, source-active
**Scope:** substrate, store, edges, retrieval
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-18, Gemini's Finding 1 (Simplification / Core-vs-Shell: permitting illegal cycles at the write path while forcing every read consumer to defend is backwards). Claude concurred on direction and grounded it in the live store.py — V1a's source-active acyclicity guard works only via kill-edge deactivation, so non-kill amends/narrows need a genuine union-reachability check, not a "simple extension" — and added the union-graph requirement (a per-type check misses cross-kind cycles). Surfaces and resolves a latent inconsistency: the prior vision's get_lineage read-defense stance contradicted kill-edge-acyclicity-requires-active-source, which already rejected a runtime cycle-walker in favour of the structural guard (Lesson 2 structural-over-discipline). A vision-loop decision, not a Vinga manual-round call; resolved on framework grounds. Recorded directly in decisions.md because the dogfood graph predates V1a and awaits cutover, so the buffer is the only writable surface this round. Applied to vision sections 1.1, 4.3 (Cycle prevention), 1.2 DoD #11 (recast from get_lineage-cycle-safety to mutation-cycle write-rejection), and 6.1 clusters 1 and 4.
**Cites:** kill-edge-acyclicity-requires-active-source, get-lineage-separate-store-primitive-not-get-transcript-overload


### cross-file-oq-decision-forward-refs-self-heal-on-resync-not-ordered

**Decided:** Mitos V1b wires questions.md into steady-state sync as a strict, ordering-naive ingestion: a cross-file edge whose target is not yet committed — an open-question's derives_from to a host decision, or a decision's resolves to an open-question — is a loud missing_target vector error that self-heals on the next sync (the failed entry stays in the buffer and re-commits once its target exists), exactly as decisions.md's own forward-referencing supersedes behaves in the incremental path; V1b adds no file-processing order and no node-pass-then-edge-pass split.
**Rejected:** Process decisions.md fully before questions.md so derives_from (OQ→D) targets pre-exist (Gemini's Finding 2 fix): rejected as asymmetric and incomplete — resolves is D→OQ (authored as Resolves: on a decision, targeting an OQ in questions.md), so decisions-first commits the resolving decision before its OQ target exists and merely moves the same missing_target onto resolves; derives_from wants decisions-first while resolves wants questions-first, so no single file order satisfies both (a genuine circular ordering dependency). A node-pass-then-edge-pass (two-pass) commit dissolving all forward-refs: rejected as the exact shape cutover-single-pass-oldest-first-drops-commit-batch already refused — it relaxes V1-D10 per-entry atomicity (node in pass 1, edges in pass 2) and the MI-12 crash-safety resting on it, and a forward reference is always a causal violation under the new→old directional invariant, so there is nothing valid to protect. Topological sort of the combined stream: rejected per cutover-chronological-replay-by-file-position — a mutable reusable slug makes a global pre-commit DAG ambiguous and its tiebreak nondeterministic. The substrate-floor per-entry primitive stays strict and ordering-naive; live cross-batch ordering, if ever wanted, is V3a's per §1.1, added additively. The common path rarely hits the trap (an OQ is usually resolved by a decision authored after the OQ is already in the graph, and usually derives from an already-committed decision).
**Mechanisms:** derives_from, resolves, missing_target, questions-md, commit_parsed_entry
**Scope:** sync, store, substrate, retrieval
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-18, Gemini's Finding 2 (a forward-reference trap once questions.md is wired into steady-state sync). Claude agreed the trap is real but flipped the fix, grounded in the live store.py (_reconcile_edges documents missing_target as "a forward reference is always a causal violation; edges point new→old") and in V1a precedent: decisions-first is asymmetric (breaks resolves D→OQ), and any two-pass/topo-sort machinery was already rejected for the cutover (cutover-single-pass-oldest-first-drops-commit-batch, cutover-chronological-replay-by-file-position) on per-entry-atomicity and mutable-slug-ambiguity grounds. Mirrors this round's same-sync-handle-handoff-surfaces-collision-not-batch-ordering (loud + self-heal-on-re-sync over batch-ordering machinery); Gemini fully concurred ("trying to solve that with ordering is a fool's errand that ruins per-entry atomicity"). A vision-loop decision, not a Vinga manual-round call. Recorded directly in decisions.md because the dogfood graph predates V1a and awaits cutover. Applied to vision section 6.1 cluster 3 (the negative-space forward-ref pin).
**Cites:** cutover-single-pass-oldest-first-drops-commit-batch, same-sync-handle-handoff-surfaces-collision-not-batch-ordering


### node-handles-migration-backfills-archived-slugs

**Decided:** Mitos V1b migration that creates node_handles backfills it with every existing node's current slug from the nodes table including archived nodes, because steady-state sync never re-parses archived entries, so without the backfill the single-table union-uniqueness anchor is blind to archived slugs and silently captures their citations while nodes.slug casefold index cannot be safely dropped.
**Rejected:** Populate node_handles only from re-parsed buffer entries (the corpus-authored "never graph-only state" rule taken literally): rejected - archived entries are never re-parsed in steady-state sync, so every archived node's slug would be absent from node_handles until that node happened to be re-touched, leaving the all-node union-uniqueness a sieve on day one. This is the exact buffer-vs-archive asymmetry the edges-rebuild-faithfulness gate already guards for edges, applied here to handles: a fresh mint colliding with an archived handle would raise no slug_collision and silently capture its citations (OD1), and nodes.slug's casefold index could not be safely dropped (dropping it over an empty node_handles is zero slug-uniqueness enforcement). Force a full wipe-and-rebuild on the V1b upgrade to repopulate from all markdown including archives: rejected as a heavier, unnecessary rollout - the nodes table already holds every slug verbatim, so a targeted backfill from nodes.slug reconstructs the same derivative state corpus-faithfully (the slug was parsed from the entry originally) without re-embedding or re-parsing the archive. The backfill is current-slugs-only: no pre-V1b node carries an authored Aliases row (the field is new in V1b), so there are no historical aliases to recover.
**Mechanisms:** node-handles, slug-casefold, slug-collision
**Scope:** identity, store, substrate, sync, migration
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-18. Claude's independent Finding A, the populate-side mirror of Gemini's Finding 2 (archived-slug namespace exhaustion): node-handles-single-table-structural-union-uniqueness moved the uniqueness anchor from an index on nodes to node_handles, so node_handles must actually contain every node's slug for the all-node union-uniqueness invariant to hold - but the vision's stated parse-time-only population never reaches archived nodes (steady-state sync re-parses the buffer alone). Gemini concurred: "a brilliant catch ... without backfilling the archived slugs into node_handles, OD1 enforcement would be a sieve on day one." Amends node-handles-single-table-structural-union-uniqueness with the migration-backfill that decision requires (Amends field added directly to decisions.md because that parent is in the buffer, not yet the current graph; the edge reconciles on rebuild). Applied to vision sections 1.2 DoD #5, 8.2 node_handles row, and 6.1 cluster 6.
**Amends:** node-handles-single-table-structural-union-uniqueness
**Cites:** slug-uniqueness-spans-all-node-slugs-not-just-active


### oq-stage2-resolution-stays-explicit-edge

**Decided:** Mitos OQ Stage-2 resolution stays explicit-edge: an open question is resolved only by an incoming resolves edge from a still-active decision, never by transitive liveness flowing through the resolving decision's supersedes chain, so a superseding decision does not inherit the questions its predecessor resolved and a torn-up resolution re-surfaces the open question rather than silently keeping it resolved.
**Rejected:** Transitive-liveness through the supersedes chain (Gemini's Finding 1: an OQ stays resolved if its resolving decision is active OR has an active successor, reverting to parked only when the whole resolving lineage dies without a successor): rejected. It assumes B-supersedes-A means B still resolves everything A resolved, but supersession is exactly the moment of maximum semantic discontinuity - B may refine A and still resolve OQ-X, or pivot away and no longer resolve it - so transitive inference would silently keep an OQ resolved by a decision that abandoned it. That is the wrongful-advance direction P5 Ironclad and P3 forbid: a re-surfaced OQ fails safe and loud and is recoverable, a silently-resolved one is lost and never resurfaces. It also singles out resolves for inheritance through supersedes when no other edge type (depends_on, cites, derives_from) inherits that way - a special-case break of M3's explicit-edge model, where edges belong to nodes and state is computed from declared edges, not inferred across a different edge type's chain. The real friction Gemini identified - re-declaring resolves on a supersession that does carry coverage forward - is authoring-layer, and is fixed at V3a's reconciliation surface (offer to carry the predecessor's resolves edges onto the successor, confirm-per-OQ not retype), mirroring the [RETIRED]-parse-in-V1b / prompt-in-V3a split. The substrate keeps fail-safe exactness; the ergonomics live one layer up.
**Mechanisms:** resolves, oq-state-view, supersedes
**Scope:** substrate, store, retrieval, sync
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-18, Gemini's Finding 1 (Saboteur lens: the OQ Stage-2 self-healing re-surface is a UX booby-trap that forces manual re-linking of resolves on every supersession and floods the MCP channel if one is missed). Claude agreed the busywork is real but located the fix at the V3a authoring layer rather than weakening substrate semantics, on M3 (explicit-edge) plus P5 Ironclad grounds (re-surface fails safe, silent-resolved fails unsafe); Gemini concurred, calling "supersedes != resolution-continuity" the killer argument. No mitos precedent existed on OQ Stage-2 (surface returned only weak neighbours below 0.71), so this is recorded fresh to foreclose re-proposal of transitive-liveness. Resolved on framework grounds, not escalated: V1-D18 Stage 2 is a vision-loop decision, not one of Vinga's manual-round calls. Applied to vision section 4.5.1 (explicit-edge rationale plus the V3a carry-forward forward-ref).


### archived-slug-reclamation-is-explicit-valve-not-uniqueness

**Decided:** Mitos relieves slug-handle namespace exhaustion only through an explicit warned user-invoked handle-reclamation that evicts the dead handle from node_handles, never by weakening all-node casefold uniqueness to active-only or carving out archived nodes, because an explicit warned break is not the silent citation-capture OD1 forbids whereas a uniqueness weakening is.
**Rejected:** Revert to active-only uniqueness (Gemini's option A, "web-hyperlink-rot semantics users intuitively understand"): rejected - it is the literal rejected path of slug-uniqueness-spans-all-node-slugs-not-just-active. A fresh active slug reusing an inactive node's handle silently captures, via active-first resolution, the historical citations that meant the now-inactive node - the OD1 violation the all-node rule exists to prevent. Exclude archived nodes from the uniqueness set (option B): rejected for the same reason, merely scoped to archived nodes - still silent capture. Do nothing and let dead handles accrue forever with no relief (status quo): rejected at Dyson scale - over years the namespace fills with un-reclaimable superseded handles, so a clean name like auth-strategy stays reserved by a long-dead decision (P11). The sanctioned relief is Gemini's option C reframed: an explicit, warned, user-invoked handle-reclamation that evicts the dead handle from node_handles and warns that citations to it will stop resolving. That threads OD1 because the break is user-chosen and loud, not a silent mis-resolution; and it is purely additive (a DELETE from node_handles, no schema change, no consumer rewrite, P20), so V1b names the ceiling and the additive-valve direction but ships no valve - no v0.1 consumer needs it at current corpus scale.
**Mechanisms:** node-handles, slug-collision, slug-casefold
**Scope:** identity, store, substrate, sync
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-18, Gemini's Finding 2 (Core-vs-Shell lens: all-node casefold uniqueness permanently burns the slug namespace because sync never re-parses archives to free a dead handle - treating human-readable slugs, the Shell, with the cryptographic permanence of hashes, the Core). Gemini proposed reverting to active-only or excluding archived nodes. Claude grounded the rejection in the recorded slug-uniqueness-spans-all-node-slugs rejected path (both reintroduce silent OD1 capture) and reframed Gemini's own option C as the OD1-safe relief: an explicit warned eviction is not a silent mis-resolution. Gemini agreed option C is the right path. Resolved on framework grounds (OD1 plus P11 name-the-ceiling), not escalated - it reverses no Vinga manual-round call; the prior round's resolved-section already invited a re-open if active-only were wanted, and this litigates and closes that. Applied to vision section 4.1 (the P11 namespace-exhaustion ceiling note).
**Cites:** slug-uniqueness-spans-all-node-slugs-not-just-active


### mechanisms-stay-dedicated-registry-table-not-graph-nodes

**Decided:** Mitos keeps mechanism entities in a dedicated mechanisms registry table (canonical_name PK per mechanism_canonical_norm, first-seen-wins authored_name and source, reserved aliases_json) and references them via the canonical-core mechanism_refs set — it does NOT promote mechanisms to first-class graph nodes (kind=mechanism) with the decision-to-mechanism link modeled as a graph edge. The mechanisms-as-nodes unification was weighed and rejected because mechanism_refs is identity-constituting (a decision node id is SHA-256 over {kind, axiom, mechanism_refs}), so moving the reference to an edge would pull mechanism_refs out of the canonical core, re-mint every decision id, and break MI-1's pinned golden traces on the now-shipped V1a substrate; M6 (mechanisms are first-class entities) is already satisfied by the registry table (identity + source + aliases), which nowhere requires sharing the nodes table; and the only real benefit (structural, declaratively-unique mechanism aliases via node_handles rather than unindexed aliases_json) lands in v0.2 mechanism alias-merging, where aliases_json is still an unwritten reserved column, so v0.2 may choose any structure with zero destructive retrofit (P20 does not fire).
**Rejected:** Promoting mechanisms to kind=mechanism graph nodes with refs as edges: breaks MI-1 — mechanism_refs is in the content hash {kind, axiom, mechanism_refs} (confirmed identity.py compute_node_id) — and violates extend-V1a-do-not-modify-it, since a new node kind ripples through the edges kind-matrix CHECK, the active-view anti-join, and the modifier join on a substrate that shipped 2026-06-18; this is the same extend-not-modify ground node-handles-single-table-structural-union-uniqueness used to reject dropping nodes.slug. The MI-1-safe hybrid (keep mechanism_refs in the hash AND also mint mechanism nodes AND draw derivative decision-to-mechanism edges AND add node_handles rows for them): rejected as strictly more moving parts than the registry table, not a simplification, leaving the nodes table carrying two identity paradigms (content-hash for decisions/OQs, name-fold for mechanisms). Critiquing the v0.2 aliases_json storage choice from inside V1b: out of scope — mechanism alias-merging is v0.2 Drift-consumer territory and aliases_json is reserved/unwritten in v0.1.
**Mechanisms:** mechanisms-registry, mechanism-refs, mi-1, m6, extend-v1a
**Scope:** identity, store, substrate
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-18. Gemini raised mechanisms-as-nodes under his Simplification lens (Finding 1); Claude pushed back on MI-1 + extend-V1a grounds and Gemini fully concurred ("breaking MI-1 golden traces post-ship is an absolute non-starter"), agreeing to record the rejected path as M5 anti-knowledge so a future fresh-eyes round does not reopen it. Reaffirms V1-D5 / V1-D15 (the mechanism registry ADRs) rather than amending them; forecloses the recurring node-unification proposal. Recorded directly in decisions.md because the dogfood graph predates V1a and awaits cutover, so the buffer is the only writable surface this round. No vision text changed — the registry-as-table design in sections 3 / 4.6 / 8.2 already stands; this entry captures the rejected alternative and its counter.
**Cites:** slug-removed-from-canonical-core-hash, node-handles-single-table-structural-union-uniqueness

### node-handles-single-table-structural-union-uniqueness

**Decided:** Mitos V1b stores every citation handle — each node's current slug and all its historical aliases — in one node_handles table whose casefold column carries a declarative UNIQUE index, instead of a separate nodes.slug plus a standalone slug_aliases table, because cross-table casefold uniqueness is not expressible with a declarative constraint in SQLite and the trigger or SELECT-then-INSERT alternatives are the discipline-over-structure path Lesson 2 forbids; nodes.slug is retained as a denormalized current-handle convenience while node_handles is the sole structural uniqueness anchor.
**Rejected:** Keeping the two-table shape (nodes.slug plus a separate slug_aliases table) and enforcing union-uniqueness with BEFORE INSERT/UPDATE triggers or application-layer SELECT-then-INSERT checks: rejected because SQLite cannot express a UNIQUE constraint spanning two tables, so the union-uniqueness that slug-alias-union-strict-casefold-uniqueness mandated would fall to triggers (opaque, fragile) or app-layer discipline — the exact structural-over-discipline violation Lesson 2 and the edge-catalog-stays-check-constraint-not-reference-table ADR forbid. Dropping nodes.slug entirely and resolving everything through node_handles: rejected for V1b scope — nodes.slug is read across V1a's shipped surfaces (the modifier join's SELECT n.slug, render, every read surface) and V1a is mid-implementation (around Phase 8), so dropping it is a destabilizing substrate refactor that violates extend-V1a-do-not-modify-it; it stays as a denormalized current-handle column, and its now-redundant casefold index is plan-internal to drop or keep. Leaving the enforcement mechanism plan-internal: rejected — the two-table shape makes structural enforcement impossible, so the choice of a single handle table is itself the load-bearing design decision, not a plan detail.
**Mechanisms:** node-handles, slug-aliases, slug-casefold, slug-collision, m2
**Scope:** identity, store, substrate, sync
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-18. Gemini raised it under the Simplification lens (his Finding 1): the recorded slug-alias-union-strict-casefold-uniqueness ADR mandates structural enforcement, but the pinned two-table storage shape (nodes plus slug_aliases) cannot enforce cross-table casefold uniqueness with a declarative constraint. Claude agreed and added the scope guardrail (keep nodes.slug denormalized; do not refactor V1a's read surfaces mid-flight). A second-pass-review catch (round N+1 treats a round-N loop ADR as a claim, not settled context) on a Claude+Gemini ADR, not one of Vinga's manual-round calls — so recorded as an amending ADR rather than escalated. Preserves the union-uniqueness invariant and the corpus-authored, rebuild-faithful provenance (node_handles still derives from nodes.slug plus parsed Aliases, so it survives the wipe-and-rebuild). Amends slug-alias-union-strict-casefold-uniqueness (changes its enforcement from an abstract substrate guarantee to a single-table declarative UNIQUE) and also updates the enforcement-mechanism clause of slug-uniqueness-spans-all-node-slugs-not-just-active (the anchor moves from an index on the nodes table to node_handles). The exact node_handles columns and whether to drop nodes.slug's redundant index are plan-internal. Amends field added directly to decisions.md because the parent is in the buffer, not yet the current graph; the edge reconciles on rebuild. Applied to vision sections 1, 1.2 DoD #5, 3, 4.1, 6.1 cluster 6, 8.2, and 10 (M2 added, OD1, P6/M7).
**Amends:** slug-alias-union-strict-casefold-uniqueness
**Cites:** slug-uniqueness-spans-all-node-slugs-not-just-active


### get-transcript-deferred-to-v5

**Decided:** Mitos defers the get_transcript store primitive (the corrects-only correction-history ancestry walk) entirely to V5 and does not implement it in V1b, because V1b has no consumer for it and it is a pure read-only query whose later addition breaks no walls (P20 Retrofit Test fails, so YAGNI applies), leaving get_lineage as V1b's only implemented transitive walk.
**Rejected:** Implementing get_transcript in V1b so V5 can reuse it (the prior vision draft's soft-pin in §5.1 C3): rejected on P20/YAGNI. The draft's rationale was "since this is V1b's one transitive walk, implement it here" — but get_lineage (the separate mutation-family walk pinned in V1b for the near-dup gate) dissolved the "one walk" framing, and get_transcript has zero V1b consumer: OQ Stage 2 uses resolves edges, modifier-stamping uses direct edges, the near-dup gate uses get_lineage, and the dogfood self-parse only STORES transcripts (it does not walk the corrects-chain). get_transcript walks only V1a structures (the transcripts table + corrects kill-edges), depends on no new V1b edge, and is a pure read-only method — so adding it in V5 breaks no interface, requires no migration, and rewrites no consumer. The Retrofit Test's destructive-retrofit condition fails, so building it now is speculative. This mirrors V1a's own deferral logic (defer when the retrofit is non-destructive and additive).

Note the asymmetry with get_lineage, which DOES stay in V1b: it has a real V1b consumer (the near-dup gate V1b re-opens), so deferring IT would force a destructive gate retrofit — see `get-lineage-separate-store-primitive-not-get-transcript-overload`. The vision's "(if Vinga would rather defer)" flip-line is removed: with get_lineage in place, the flip is framework-settled, not a judgment call left for her.
**Mechanisms:** get_transcript, get_lineage, transcript-traversal
**Scope:** substrate, store, retrieval
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-18, Gemini's Finding 2 (Simplification/P20). Does not reverse transcript-traversal-single-store-primitive (V1a's pin that the transcript walk is a single store primitive) — the primitive stays single; only its implementation LOCATION moves from V1b to V5, which the vision always left open via a flip-line. With get_lineage now the genuine V1b transitive walk and the only one with a V1b consumer (see get-lineage-separate-store-primitive-not-get-transcript-overload, the sibling decision), get_transcript's only justification for landing in V1b evaporated. V5 owns get_transcript. Applied to vision sections 1, 5.1 C3, and 6.1 cluster 4.
**Cites:** transcript-traversal-single-store-primitive


### slug-uniqueness-spans-all-node-slugs-not-just-active

**Decided:** Mitos V1b slug-handle casefold-uniqueness spans the union of all node slugs and slug_aliases (not merely active node slugs), so a freshly-minted slug can never reuse an inactive superseded-or-retired node's handle and silently capture, via active-first resolution, the historical citations that meant the now-inactive node.
**Rejected:** Uniqueness over {ACTIVE node slugs ∪ slug_aliases} (the parent `slug-alias-union-strict-casefold-uniqueness`'s literal phrasing): rejected — "active node slugs" is too narrow to achieve the parent's OWN stated goal ("a citation handle resolves to at most one node"; "capture structurally impossible"). If only active slugs are protected, an inactive (superseded or retired) node's un-aliased slug becomes reusable by a fresh mint; a pre-existing `Cites: legacy-slug` that meant the now-inactive node then resolves active-first to the newly-minted node — a silent citation capture, the very Q-Alias-Shadowing the parent rule kills for the active-vs-alias case, reappearing for the inactive-node-slug case. OD1 (never silently mis-resolve) forbids it, so the protected handle namespace must be global. Widening to all node slugs is also CHEAPER to enforce: it is an indexed casefold-column lookup over the nodes table, versus computing the active view at every mint.

Convergence (two same-core-different-slug entries collapsing to one node under the slug-free hash) is unaffected — that is hash identity, not cross-node slug reuse, so the all-node namespace does not over-constrain it.
**Mechanisms:** slug-aliases, slug-casefold, slug-collision, m2
**Scope:** identity, store, substrate, sync
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-18. A second-pass-review precision catch on a same-round ADR: Claude flagged that the parent's active-node-slugs phrasing is ambiguous about inactive (superseded/retired) node slugs; Gemini concurred it must be reading-(a), all node slugs, because reading-(b) reintroduces the exact shadowing the parent exists to kill. Affirms the parent's entire strict-uniqueness apparatus (write-time enforcement, the loud slug_collision P3 vector-error, the V1b-owns-invariant / V3a-owns-disposition altitude split); corrects only the namespace SCOPE so the rule is consistent with its own resolves-to-at-most-one-node goal. Implementation-affecting: the uniqueness check runs against an indexed all-node slug_casefold column, not the computed active view. Amends slug-alias-union-strict-casefold-uniqueness (Amends field added directly to decisions.md because that parent is in the buffer, not yet the current graph; the edge reconciles on rebuild). Applied to vision sections 1.2 DoD #5, 4.1, and 8.2 slug_aliases.
**Amends:** slug-alias-union-strict-casefold-uniqueness


### edge-catalog-stays-check-constraint-not-reference-table

**Decided:** Mitos enforces the full nine-type edge catalog including the three cross-kind edges (cites any-to-any, derives_from OQ-to-D, resolves D-to-OQ) with a single table-level CHECK constraint on the STRICT edges table, extended via a contained edges-rebuild in V1b, and ships no edge_type_matrix reference table, because a CHECK with OR-clauses expresses the cross-kind catalog completely and the reference table's only advantage (adding a future edge type via an INSERT rather than a rebuild) is worthless once rebuilds are milliseconds at Mitos scale.
**Rejected:** The edge_type_matrix reference table + composite FK from edges (the prior `v1a-edge-catalog-check-defers-reference-table` model): rejected as relational tax for zero benefit. That ADR justified the reference table as "the right model for V1b's non-uniform cross-kind matrix" but never tested the claim against a CHECK-with-OR-clauses — it treated cross-kind as implying needs-a-table. A single CHECK expresses the whole nine-type catalog completely: (source_kind = target_kind AND edge_type IN the six same-kind types) OR (derives_from AND OQ→D) OR (resolves AND D→OQ) OR (cites, any→any). Once the CHECK suffices, the table's sole remaining advantage — adding a future edge type via an INSERT instead of a table rebuild — is worth ~nothing, because that same prior ADR already established rebuilds are "milliseconds at Mitos scale." So the table is pure cost: an extra schema entity, a populate-on-migration step, and a composite-FK lookup on every edge write, for zero query benefit — nothing reads the matrix as data (edge validation is store-internal; consumers bind to store methods, not the edges DDL). P19 (fewest moving parts; the 50-year-readable form keeps the legal edge shapes in ONE place, the DDL, rather than split across an FK declaration plus table rows) and P12 (do not pay a relational tax for unneeded flexibility) settle it toward the CHECK.

Application-layer Python enforcement of the catalog: stays rejected per Lesson 2 (structural over discipline) — the CHECK is substrate-enforced, exactly as the reference table would have been.
**Mechanisms:** edge_type_matrix, edges, check-constraint, edges-rebuild
**Scope:** substrate, store, schema, edges
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-18. Gemini raised it under the Simplification lens (Finding 1): the reference table is over-engineering whose only rationale — avoiding an expensive-later rebuild on catalog growth — was already dissolved by v1a-edge-catalog-check-defers-reference-table's own milliseconds-at-Mitos-scale rebuild finding. Claude confirmed the prior ADR never tested its reference-table-is-the-right-model claim against a CHECK-with-OR-clauses, and grounded the reversal in P19/P12. A second-pass-review catch (round N+1 treats a round-N loop ADR as a claim, not settled context) on a Claude+Gemini ADR, not one of Vinga's manual-round calls, so recorded as a superseding ADR rather than escalated. The exact CHECK expression is plan-internal; the vision pins only a table-level CHECK extended via the cheap edges-rebuild, no reference table. Supersedes v1a-edge-catalog-check-defers-reference-table, which itself superseded edge-catalog-reference-table — the reference table is now dropped from the architecture entirely, not merely deferred. Applied to vision sections 2, 3, 4.3, 6.1 cluster 1, and 8.2 edges row.
**Supersedes:** v1a-edge-catalog-check-defers-reference-table


### get-lineage-separate-store-primitive-not-get-transcript-overload

**Decided:** The transitive mutation-lineage walk (supersedes/amends/narrows) is a separate store-owned get_lineage primitive pinned in V1b, distinct from get_transcript (the corrects-only correction-history walk), so consumers like the near-dup gate never reimplement the walk and the two semantically-different walks are never conflated.
**Rejected:** Upgrade get_transcript to walk the full mutation family (supersedes/amends/narrows): rejected because get_transcript is the corrects-only correction-history walk — corrects are in-buffer typo fixes, while supersedes/amends/narrows are semantic evolution; conflating them violates M1 and corrupts the transcript primitive meaning. Let each consumer (the near-dup gate, V3a) reimplement the lineage walk over raw edges: rejected on P1/P17 — duplicated recursive walks over the edges table in every consumer, and a P20 retrofit when the walk is later extracted. Defer the primitive entirely: rejected because V1b is what creates the amends/narrows chains that make the near-dup transitive-lineage gap common, and the consumer is the existing record_decision_entry gate V1b already re-opens — so deferral forces a retrofit that rewrites the gate and its tests.
**Mechanisms:** get_lineage, get_transcript
**Scope:** substrate, store, retrieval
**Cites:** transcript-traversal-single-store-primitive


### prefix-matching-is-author-time-expansion-not-parse-time

**Decided:** Unique-prefix matching of slugs is an author-time expansion in V3a (expand a typed prefix to the full canonical handle and store the full handle), never a V1b parse-time resolution tier; V1b edge-target resolution stays exact (active slug then alias) so stored citations cannot rot when unrelated future nodes make a prefix ambiguous.
**Rejected:** A V1b parse-time unique-prefix resolution tier (resolve a stored Cites prefix to its unique match): rejected because a stored prefix that resolves uniquely today turns ambiguous the moment an unrelated later node is minted — the stored citation then breaks and the ambiguity raise fires on the innocent later author, not the one who wrote the lazy prefix. That manufactures fresh citation-rot (P11 expensive-later) and inverts the citation-rot subsystem purpose. Doing nothing (exact-only, no prefix help anywhere): rejected because the long head-truncated auto-handles are a real P3 momentum cost the AX log flags. Author-time expansion in V3a (expand the typed prefix to the full canonical handle and store the full handle) keeps stored citations exact and rot-proof while still sparing the author the long tail.
**Scope:** identity, substrate, sync


### symmetric-edges-via-bidirectional-accessor-not-consumer-burden

**Decided:** Mitos exposes semantically-symmetric edges (in v0.1, contradicts) through a single safe bidirectional store accessor that unions both directions internally, rather than storing the edge once and making every consumer query both directions by hand; the accessor boundary ships in V1b where the edge is introduced.
**Rejected:** Consumers query both directions themselves (WHERE source=X OR target=X): a leaky abstraction with a standing OD1 hazard — a consumer that forgets the OR silently under-reports contradictions (the silent mis-resolution OD1 forbids), plus a P7 cross-cutting latent bug that fires the day a consumer forgets. Mirroring the edge (storing both A->B and B->A rows): doubles storage and lets the two copies drift out of sync, an invariant to police forever. Deferring the accessor to the first consumer (v0.2): rejected on P20 — the edge is introduced in V1b, so deferral makes v0.2 Conflict, V5, and render each hand-roll the bidirectional query, and a later vision must hunt them all down to retrofit the accessor (rewriting shipped consumers).
**Mechanisms:** contradicts
**Scope:** substrate, store, edges, retrieval


### slug-alias-union-strict-casefold-uniqueness

**Decided:** Mitos V1b enforces casefold uniqueness across the UNION of {active node slugs ∪ slug_aliases} as one handle namespace, so a citation handle resolves to at most one node and the two remaining slug-alias design-opens become structurally impossible at write time rather than caught at read time: a newly-minted active slug can never silently capture an existing alias's historical citations (Q-Alias-Shadowing dissolves) and two nodes can never share an alias that multi-matches in the fallback tier (Q-Alias-Collision dissolves); the substrate raises slug_collision when an entry would violate the union (an active slug equal to another node's active slug or alias, or an Aliases handle already taken as a slug or alias; a node's own alias equal to its own active slug is a benign intra-node no-op), and the V1-D23 alias fallback tier therefore returns at most one node by construction.

**Rejected:** Uniqueness only among active slugs with aliases as a separate non-unique namespace (the shape the open questions left undecided): permits two nodes to share an alias (a citation multi-matches in the fallback tier) and permits a fresh active slug to shadow an existing alias (silently re-pointing the historical citations that meant the older node) — both are silent mis-resolutions, which OD1 forbids and P3 (silence is anxiety) rejects. Allowing the shadow-capture under a literal reading of "live slug wins over an alias" (the Q-Alias-Shadowing allow option): treats live-wins as a capture license rather than a resolution-order convention (active-view lookup first, alias fallback second), so it silently hijacks the citations that meant the older node — the exact citation-rot the slug_aliases subsystem exists to kill. Auto-disambiguating an alias-vs-alias collision by suffixing the alias: meaningless — an alias is a fixed historical handle (old citations say legacy-name, never legacy-name-2), so a suffixed alias matches nothing; the only correct response to two colliding historical handles is a loud raise for explicit reconciliation. Leaving the uniqueness shape plan-internal and undecided (the vision's status quo before this round): leaves the planning Claude to guess whether the alias namespace is protected, and the unprotected guess silently violates OD1.

**Mechanisms:** slug-aliases, slug-casefold, slug-collision, v1-d23, m2

**Scope:** identity, store, substrate, sync

**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-18. Resolves the last two slug-alias design-opens (Q-Alias-Shadowing, Q-Alias-Collision) that aliases-corpus-authored-via-aliases-field left open and parse-time-checkable. Gemini proposed the single strict-uniqueness namespace (his Finding 2); Claude independently reached the same {active ∪ aliases} union-uniqueness and added the write-vs-read framing (enforcing uniqueness at write time makes multi-match and capture structurally impossible, not merely raised at read time) plus the altitude split: V1b owns the uniqueness invariant and the loud slug_collision raise, while the disposition of a raised collision (auto-disambiguate a freshly-minted active slug with a -N suffix per V1a's independent-collision rule in section 6.3, vs an interactive prompt) is V3a sync-flow policy — V1b never silently auto-resolves. Framework-settled by OD1 (never silently mis-resolve): the prior round's framing that Q-Alias-Shadowing is Vinga's slug-reuse-ergonomics call is superseded, because the only freedom the union-uniqueness forecloses is the freedom to SILENTLY capture historical citations, which OD1 already forbids — and the Q5 slug ADR itself already chose an explicit-collision-rule for the active-vs-active case, so this extends the same rule to the alias namespace rather than reversing anything. Amends slug-removed-from-canonical-core-hash (extends its explicit collision rule across {active ∪ aliases}); does not touch the mutable-slug or aliases-corpus-authored decisions. Applied to vision sections 1, 1.2 DoD #5, 4.1, 6.1 cluster 6, and 8.2 slug_aliases. The auto-disambiguate vs interactive-prompt disposition and any unique-prefix-match ergonomics are V3a.

**Amends:** slug-removed-from-canonical-core-hash

**Cites:** aliases-corpus-authored-via-aliases-field


### v1b-drops-render-cascade-for-full-regen

**Decided:** Mitos V1b ships no per-commit render-targeting cascade and no CommitDelta payload; V4 renders by full regeneration from the computed active view at end of sync (M8), so a view-membership flip or an open-question Stage-2 parked-or-resolved change is recomputed at render time (M3) and a re-render cannot go stale, dissolving the MI-6 cascade-exhaustiveness invariant, risks R5 and R11, and the Appendix C oracle, while the committing-node embedding Outbox (MI-12) is untouched.
**Rejected:** The per-entry render-targeting cascade (the prior V1b draft — every commit computes a transitive set of nodes whose active-view membership OR computed state flipped, returned as a CommitDelta payload for V4 scope re-rendering, governed by MI-6 plus the Appendix C worked oracle): rejected as over-engineering against the framework's own rendering model. M8 mandates that renders are regenerated from the active node set, not patched incrementally; M3 makes open-question state computed at query time. So full regeneration from the computed view is correct by construction — there is no scope a cascade could miss (R5 dissolves), and OQ self-healing on a broken resolves-chain is automatic (R11 dissolves). The only motive for targeting is performance / git-churn, which is better served by a renderer skip-write-if-content-unchanged check than by a per-commit transitive graph walk. P20 seals it: V1a already ships a first-order cascade_affected_scopes field, so extending it to transitive is purely additive — YAGNI defers it until a real targeted-render consumer exists, and none does (V4 is not built).

Keeping MI-6 and the Appendix C oracle as a closeout gate: rejected — under full regeneration there is no cascade to be exhaustive about, so the invariant and its worked oracle have no anchor.

Solving the git-churn worry with the cascade: rejected — a renderer that content-hashes its output and skips writing byte-identical scope files removes the churn at far lower cost than a transitive walk plus a silent-failure invariant.
**Mechanisms:** render-cascade, commitdelta, full-regeneration, m8, m3, get-transcript
**Scope:** store, substrate, render, vector-store, sync
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-18. Gemini raised it under the Simplification lens (a per-entry cascade contradicts M8's regenerate-from-the-active-set render model). Claude reinforced with M3 (open-question Stage-2 state is computed at query time, so self-healing on a broken chain is automatic) and P20 (V1a already ships the first-order cascade_affected_scopes field, so the transitive extension is additive and deferrable until a real consumer needs it). No prior ADR pinned the render cascade — it was the residue of the pre-Q2 fused embedding-plus-render cascade; Q2 (drop-computed-state-from-qdrant-payload) split the two and kept the render half, which this removes. Dissolves vision MI-6, R5, R11, and Appendix C; recasts sections 4.4, 5.1 C2/C3, 5.2.4, 5.3, 6.1 cluster 4, 6.3, and 7. The committing-node embedding Outbox (MI-12) and the OQ Stage-2 computed view are unaffected; the only surviving transitive walk is get_transcript (corrects-only ancestry, V1b-or-V5). INVARIANTS.md reconciliation — retire MI-6, mark MI-12 V1a-only — is flagged for Vinga, since the invariant catalog is hers to edit.
**Cites:** drop-computed-state-from-qdrant-payload


### aliases-corpus-authored-via-aliases-field

**Decided:** Mitos V1b populates the slug_aliases citation-rot table from a corpus-authored Aliases field parsed from the markdown entry (mutable commentary, outside the canonical-core hash), not from graph-only in-place-rename state, so the alias set is a pure derivative that survives the wipe-and-rebuild and decisions.md stays internally self-consistent; V1b owns parse plus populate plus the V1-D23 fallback-resolution tier, and the interactive rename-preserve prompt is V3a.
**Rejected:** Graph-only aliases (the prior V1b draft — an in-place slug rename records the prior slug into slug_aliases, never written back to the corpus): rejected as a P6/M7 break. The graph is a disposable derivative that must rebuild from the markdown alone (P6), so a graph-only alias is non-derivative state lost on every wipe-and-rebuild. Worse, after an in-place rename foo->bar the corpus still contains `Cites: foo` while defining only `### bar` — an internally-inconsistent document that only the disposable graph can resolve, so decisions.md is no longer self-contained (M7). This is exactly the failure the project already avoided for provenance.

Accept-the-soft-loss (keep graph-only aliases, document that they break on rebuild): rejected — citation-rot resilience that evaporates on the next rebuild defeats the subsystem's whole purpose, and still leaves the decisions.md self-containment break.

Author the alias preservation inside V1b's sync path (auto-heal the Aliases line into the buffer when a rename is detected): deferred, not adopted in V1b — the interactive "you renamed X->Y, keep the old handle?" prompt is V3a's rename/deletion-reconciliation surface, mirroring the [RETIRED] split (parse plus signal in V1b, interactive UX in V3a). V1b ships only parse plus populate plus the fallback-resolution tier.
**Mechanisms:** slug-aliases, aliases-field, format-spec, rebuild-faithfulness
**Scope:** identity, parser, format-spec, substrate, store
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-18. Resolves Q-Alias-Provenance, one of the three slug-alias design-opens the vision flagged; the other two (Q-Alias-Shadowing, Q-Alias-Collision) stay open and become parse-time-checkable once aliases are corpus-authored. Gemini raised the finding (graph-only aliases violate P6/M8); Claude grounded the resolution in the direct project precedent provenance-source-field-in-markdown-for-rebuild-faithfulness (the Source field, placed in the markdown for the same survive-the-rebuild reason) and in the decisions.md self-containment argument (an in-place rename leaves dangling internal citations unless the old handle is authored in the corpus). Governed by the Q5 parent slug-removed-from-canonical-core-hash, which made the slug mutable and named slug_aliases but left provenance open. The Aliases field is a format-spec.md addition: mutable commentary, outside the canonical-core hash. Applied to vision sections 1, 3, 4.1 (Q-Alias-Provenance RESOLVED), 1.2 DoD, 8.2 slug_aliases, 6.1 cluster 6, and 10 (P6/M7). The cutover replay re-creates aliases for free because they now live in the markdown.
**Cites:** provenance-source-field-in-markdown-for-rebuild-faithfulness


### v1a-schema-built-in-2b-live-flip-deferred-to-5a

**Decided:** Mitos V1a's six-table STRICT schema is authored and fully proven as migration-ladder step 1 in Phase 2b but is NOT registered live or booted; the live MIGRATION_STEPS append, the _init_db retirement, and the is_pre_v1a_schema boot guard all defer to Phase 5a so the schema flip rides in lockstep with the commit_parsed_entry rebuild and the test suite stays 100% green through phases 2b-4b.
**Rejected:** Flipping the live boot at 2b (register step 1 + retire _init_db now, as the 2a handoff literally framed it): rejected because the prototype-shaped store read/write methods and the five consumers still bind the old columns (core_axiom, mechanisms, inline scope, from_id/to_id/type) until phases 5-8a, so a 2b flip turns ~70% of the suite red for six phases and guts the single-lane CI gate Phase 1b just stood up as the MI-1 golden-trace venue. Flipping only the fresh-DB path: rejected because the prototype methods break whichever DB they read, so the flip must wait for the methods to rebuild in 5a.
**Mechanisms:** sqlite-strict, pragma-user-version, migration-ladder
**Scope:** substrate, store, schema, migration
**Context:** Plan p2b Key Decision 1. Construction (author + injection-prove the schema via run_migrations(conn, steps=[(1,_v1_schema)])) is split from migration (the live flip) to contain the schema-swap red window to the store-rebuild arc 5a-to-8a instead of sprawling from 2b. 2a's Option-A empty-registry no-op boot is the pre-wiring that makes 5a's flip a near-pure addition: append (1,_v1_schema) + delete the _init_db() call + gate the boot with is_pre_v1a_schema (refuse + route a prototype graph to the section 2.1 cutover). A WIRING_LEDGER entry owned by 5a plus a deferred-flip tripwire test enforce the handoff.
**Depends-On:** migration-ladder-pragma-user-version-only


### migration-step-explicit-transaction-no-executescript

**Decided:** The Mitos migration runner drives each step transaction explicitly (conn.isolation_level=None, then BEGIN, the step DDL via conn.execute, PRAGMA user_version=N, COMMIT; ROLLBACK on any exception) and never uses executescript() for a migration step, so a failing step rolls back its DDL and its user_version bump together with no partial apply to repair (MI-3).
**Rejected:** Using executescript() (or the legacy auto-transaction context manager the prototype _init_db uses) to run a migration step: executescript() issues an implicit COMMIT before running, which force-commits any open transaction and breaks the atomic "DDL + version bump roll back together" contract — a step that half-applies its DDL and then bumps user_version is exactly the non-idempotent-replay bug MI-3 exists to prevent. Also rejected: a schema_version audit table or reversible down() migrations — over-engineering for a single-writer local SQLite whose transactional PRAGMA user_version is already the atomic, in-header version marker.
**Mechanisms:** sqlite, user-version-pragma, migration-ladder, executescript
**Scope:** substrate, store, schema
**Context:** Phase 2a of V1a. The runner owns the transaction so a step author cannot break atomicity: the step function only issues DDL via conn.execute and never touches transactions or user_version. The PRAGMA user_version = N interpolation is a documented P8 carve-out — PRAGMA rejects ? placeholders, and the version is a code-internal positive, strictly-increasing integer validated before interpolation (never user or LLM input). Complements the existing migration-ladder-pragma-user-version-only and migration-idempotency-via-transactional-version-gate decisions by pinning the executescript prohibition that makes their atomic-rollback guarantee real on the 3.13 floor.
**Depends-On:** migration-idempotency-via-transactional-version-gate


### connection-pragma-suite-single-chokepoint

**Decided:** Every SQLite connection Mitos opens flows through one module-level open_connection() helper that issues the full PRAGMA suite (WAL plus synchronous=NORMAL on write connections; foreign_keys=ON and busy_timeout=5000 on every connection) and a fail-fast SQLite>=3.37 version guard, with GraphStore._get_connection as a thin delegating wrapper, so a correctly-configured connection is a structural fact rather than per-call discipline.
**Rejected:** Leaving each call site to set its own PRAGMAs (the prototype per-method pattern): SQLite defaults foreign_keys OFF per-connection, so one forgotten PRAGMA silently disables the kind-matrix composite-FK enforcement the whole edge model rests on (MI-8) — the exact discipline-over-structure failure Lesson 2 forbids. Also rejected: removing or re-signaturing GraphStore._get_connection in favour of only the module-level function — it is a heavily-bound internal API (30+ call sites across production and tests pass store._get_connection() directly), so it must stay as a same-signature delegating wrapper over open_connection().
**Mechanisms:** sqlite, wal-mode, foreign-keys-pragma, busy-timeout, open-connection
**Scope:** substrate, store
**Context:** Phase 2a of V1a (Substrate Floor). MI-8 mandates the per-connection PRAGMA suite; vision 5.2.8 fixes the order (version-guard before connect, then WAL, synchronous=NORMAL, foreign_keys=ON, busy_timeout=5000 last) and the read-only carve-out (WAL/synchronous are write-connection only; foreign_keys+busy_timeout run on every connection). The SQLite>=3.37 version guard reads sqlite3.sqlite_version_info live on each connect so it stays monkeypatchable in tests and fails fast before any STRICT DDL could run.


### ci-single-lane-3-13-matrix-deferred

**Decided:** Mitos CI is a single-lane GitHub Actions workflow pinned to Python 3.13 that runs the substrate suite and the packaging install gate green with no secrets and no service containers, selecting tests by marker (never a file allowlist) so future phases' tests are gated automatically; the multi-Python matrix is deliberately deferred until the hashing goldens are portable across Unicode versions.
**Rejected:** A 3.13+3.14 multi-Python matrix now: rejected — the §11 hashing goldens are pinned to UCD 15.1.0 (which ships with Python 3.13), so a second Python/UCD lane could legitimately diverge on Unicode-edge digests and produce red lanes that are not bugs (R10), meaning the matrix could not gate; adding a matrix axis once the goldens are version-parameterized is additive, not a restructure (P20), so the deferral is correct, not debt. Wiring a Qdrant service container plus injecting API-key secrets to run the live integration tests in CI: rejected — it pulls V2 service-provisioning surface and secret-management blast radius into a v0.1 foundation phase, and the substrate suite is already green keyless/serviceless (live tests self-skip on absent keys, offline tests force degraded mode), so CI proves the deterministic substrate + packaging gate while the live Gemini/Qdrant tests stay a deliberate, logged coverage boundary deferred to V2. Declaring the test deps inline in the workflow (pip install pytest pytest-asyncio) or in a requirements-dev.txt: rejected — both fork the test-tooling source from pyproject and drift from local, whereas [project.optional-dependencies] test is the single declarative home installed identically by local dev and CI via pip install -e .[test].
**Mechanisms:** github-actions, pytest, pytest-asyncio, python-3.13, optional-dependencies, ucd-15.1.0
**Scope:** ci, packaging, testing, substrate
**Context:** First CI this project has ever had (.github/ was absent). It is the venue where the MI-1 golden-trace gate (Phase 3a) and the substrate suite run on every push to main and every PR (P10, P19). Third-party actions (checkout v6.0.3, setup-python v6.2.0) are SHA-pinned for supply-chain durability. Test tooling lives in [project.optional-dependencies] test (pytest, pytest-asyncio), installed via pip install -e .[test]; asyncio_mode=strict is set explicitly. Single-lane forward-compat: python-version is one value that becomes a matrix axis later without restructuring the job.


### packaging-pyproject-setuptools-dynamic-version

**Decided:** Mitos packaging is declared in a single pyproject.toml on the setuptools build backend, with the version read dynamically from mitos/__init__.py via tool.setuptools.dynamic attr and format-spec.md shipped as package-data; requires-python is pinned to >=3.13.
**Rejected:** A static [project] version literal: rejected because it forks the version from mitos/__init__.py and silently kills the once-a-day update nudge (_update.py parses that literal from GitHub main). A non-setuptools backend (hatchling/flit/poetry-core): rejected because setuptools is already the build tool, so a swap is pure tooling debt for zero v0.1 payoff (P19, dependency skepticism). A thin setup.py shim kept beside pyproject: rejected because two overlapping declarative sources fail the 50-year test (P19).
**Mechanisms:** pyproject.toml, setuptools, format-spec.md, requires-python
**Scope:** packaging, build
**Context:** Phase 1a of V1a (Substrate Floor). The 3.13 floor is load-bearing downstream: UCD 15.1.0 hashing goldens (vision section 11) and the SQLite 3.37+ STRICT tables Phase 2a builds both ship with Python 3.13. The setuptools>=64 build-system floor is the minimum covering PEP 621 metadata, dynamic attr version, and PEP 660 editable installs (the pip install -e . dev loop). Zero new runtime dependency; the five existing deps are preserved verbatim.


### qdrant-payload-scope-no-burial-constraint

**Decided:** Mitos V1a pins only the constraint that V2 scoped vector retrieval must avoid over-fetch burial (a quiet-scope query must never post-filter Qdrant global top-K to zero) and defers the mechanism to V2 — a has_id in-scope active id-set ranking within scope, or a low-churn scope payload pre-filter — while Option 3's active-hint payload and committing-node-only V1a Outbox stand unchanged.
**Rejected:** Pin scope INTO the Qdrant payload as a pre-filter hint (the round's first proposal): correct for burial, and scope is low-churn (the conservative flagless enqueue already re-pushes on commentary edits), but it re-pins a payload detail the parent ADR drop-computed-state-from-qdrant-payload explicitly left to V2, and re-adds a replicated mutable field Option 3 was minimizing. Kept as an explicit V2-permitted alternative, not the V1a pin.
Leave Option 3 (qdrant-payload-active-prefilter-hint) as-is: re-opens quiet-scope burial. Option 3 optimized the active dimension via the active payload hint but, rejecting Option 1 wholesale, removed the parent ADR has_id scope mechanism without replacing it — so a quiet-scope query whose global top-K are all out-of-scope post-filters to zero, contradicting the ROADMAP embedding-ranked / scope-filtered surface_decisions contract. This ADR closes that gap, not Option 3's active-hint core.
Pin the has_id mechanism as mandatory in V1a: over-reaches into V2 (V1a ships no Qdrant code or query path) and forecloses the equally-valid scope-payload alternative. P11 mandates the no-burial constraint, but the mechanism is a V2 engineering trade-off (P12) — has_id is cheap on quiet scopes where burial bites, while the array-bloat objection applies to busy scopes where over-fetch+post-filter is fine.
**Mechanisms:** qdrant, has-id, scope, active-hint, pre-filter
**Scope:** vector-store, store, substrate, retrieval, qdrant
**Context:** V1a parser-and-store vision paired-review round (2026-06-17, Claude+Gemini). Gemini flagged that dropping scope from the Qdrant payload (per qdrant-payload-active-prefilter-hint / Option 3) breaks the ROADMAP scope-filtered surface_decisions capability: with scope neither in the payload nor handled by has_id, Qdrant returns global top-K and SQLite post-filters scope, burying a quiet scope's hits to zero. Root cause: Option 3 added the active payload hint but, rejecting Option 1 wholesale, removed the parent ADR has_id scope mechanism without replacing it. The parent ADR drop-computed-state-from-qdrant-payload explicitly left whether scope leaves the payload a V2 detail (not pinned) and already specified has_id — SQLite computes the in-scope active id-set, Qdrant ranks within it — as the scoped-query mechanism. Framework: P11 forbids burial (resolve), the mechanism is a V2 engineering trade-off (P12 / V2 detail), so V1a records only the no-burial constraint and defers the mechanism. Vision touchpoints amended: section 5.1 C2 (payload description gains the scoped-retrieval no-burial constraint; the C2 parenthetical un-pins scope to a V2 detail). V1a ships unchanged (no Qdrant code). Amends qdrant-payload-active-prefilter-hint — keeps its active-hint payload and committing-node-only Outbox; closes only the scope-burial gap it opened.
**Amends:** qdrant-payload-active-prefilter-hint


### cutover-swap-clears-both-db-wal-sidecars

**Decided:** The Mitos V1a cutover atomic swap must atomically clear or move BOTH the new build's and the old/destination graph's WAL sidecars (-wal/-shm) as part of the swap, so a stale orphan -wal from a prior ungraceful exit cannot be replayed against the swapped-in graph file on next open and corrupt it.
**Rejected:** Renaming only the main DB file (the naive/prototype-shaped swap), leaving the old graph.sqlite-wal/-shm sidecars in place: SQLite binds a WAL sidecar to its database by path, so after os.rename swaps a new-content graph.sqlite into place, a surviving old -wal is replayed against the new file on next open -> SQLITE_CORRUPT, silently breaching R11's P5 crash-safe/Unplugged guarantee. Also rejected: relying on the quiesce precondition alone (stop serve/sync before the cutover) to cover this -- quiescing prevents a concurrent writer but cannot undo a PAST ungraceful exit that already left orphan -wal/-shm on disk before the swap, which is exactly the corruption window. Also rejected: clearing only the NEW build's sidecars (the prior cutover-build-aside-atomic-swap body, which named the new DB's checkpoint+close+clear but not the destination's) -- it leaves the old-DB orphan-WAL path open.
**Mechanisms:** cutover, atomic-swap, wal-mode, sqlite
**Scope:** substrate, store, sync, setup, cutover
**Context:** V1a parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-17. Gemini's archaeology finding #3: the §2.1 atomic swap named quiescing + the new build's WAL-sidecar checkpoint but not the old/destination DB's sidecar lifecycle, leaving a stale-orphan-WAL corruption mode that breaches R11's P5 promise. Amends cutover-build-aside-atomic-swap, whose body/Context covered only the new DB's -wal/-shm. The REQUIREMENT (the swap must atomically clear-or-move both databases' WAL sidecars) is pinned at vision altitude (§2.1 swap contract + R11) because it backs a P5 safety guarantee; the exact os.remove-vs-rename-aside MECHANISM stays plan-internal. Applied to vision §2.1 and R11.
**Amends:** cutover-build-aside-atomic-swap


### outbox-enqueue-conservative-node-changed-signal

**Decided:** Mitos V1a pending_embeddings enqueue is a conservative flagless node-changed signal fired unconditionally on every commit including commentary-only updates and carrying no task_type or enqueue_reason discriminator, so payload-flip-versus-re-embed discrimination and any no-op-skip filtering are V3b drain-time derivations from computed state (M3), never a V1a enqueue-time flag or a conditional skip inside commit_parsed_entry
**Rejected:** (1) Adding a task_type / enqueue_reason column (NEW_CORE vs PAYLOAD_HINT_ONLY) so V2 can call qdrant.set_payload for an active-flip vs upsert for a new core (Gemini, V1a round-11 ECONOMICS review, Finding 1): rejected — this is the needs_reembed flag already rejected by flagless-outbox-cache-absorbs-reembed, re-surfaced one round later on the I/O-cost (vector network upsert) rather than the token-cost angle. The set_payload-vs-upsert action is derivable at drain from the node's computed active-state (M3 — inactive ⇒ set_payload the flip, active-and-absent ⇒ embed+upsert), exactly the compute-at-read stance drop-computed-state-from-qdrant-payload took for retrieval, so the discriminator would store recoverable state merely to save a localhost (~3KB loopback), rare (supersession-only), already-batched I/O — reintroducing the computed-state-replica apparatus that ADR deleted. It is also V2-additive: V1a is committing-node-only; the kill-edge-target enqueue and its drain logic land in V2 via the section 5.2.5 migration ladder, so P20's Retrofit Test fails (no destructive wall) and YAGNI defers the column to V2 if V2 ever wants it.

(2) Conditionally skipping the enqueue on a pure commentary update with unchanged canonical core and no active-state flip (Finding 2): rejected as forward-UNSAFE, not merely redundant. V2's planned anti-knowledge vector keys on rejected_paths (a commentary field) and churns on commentary edits by design, so a commentary-blind enqueue would silently starve it the moment it ships — V1a must not encode "commentary is vector-irrelevant," a V2 policy already known to grow. The skip mechanism is also twice-rejected by precedent: outbox-reenqueue-resets-drain-state rejected "reset only when embedding_text changes" (it treats any re-commit, commentary edit included, as a fresh drain attempt), and cutover-bounds-embedding-seed-to-active rejected a do-not-enqueue flag inside commit_parsed_entry in favour of filtering outside the per-entry primitive (post-loop reconciliation / at drain). The governing economics is recall-over-precision (Operational Economics section 11): a missed enqueue is permanent vector staleness (the costly miss), a false enqueue is a free drain-time cache / computed-state no-op (the cheap false positive); and V3b wakes on its own volumetric/staleness trigger (section 4), batched, so a no-op row rides an already-scheduled drain and triggers no extra wake — the "forces V3b to wake unnecessarily" cost does not exist at the per-commit level.
**Mechanisms:** pending_embeddings, qdrant, embedding-cache, m3, anti-knowledge-vector
**Scope:** store, substrate, vector-store, schema
**Context:** V1a parser-and-store vision, round-11 paired review (ECONOMICS lens: FRAMEWORK + OPERATIONAL_ECONOMICS). Gemini raised two Outbox-economy findings — F1: add a task_type discriminator so V2 does set_payload vs upsert; F2: skip the enqueue on commentary-only updates — and both turned out to be the recorded rejected-paths of settled outbox ADRs. Claude surfaced the precedent; Gemini concurred and conceded the V2 anti-knowledge-vector dependency he had missed. This decision consolidates the enqueue-trigger + no-discriminator stance across flagless-outbox-cache-absorbs-reembed, outbox-reenqueue-resets-drain-state, and cutover-bounds-embedding-seed-to-active, and adds the new load-bearing forward-constraint: commentary is vector-relevant (the V2 anti-knowledge vector keys on rejected_paths), so the enqueue must stay conservative and unconditional. Fenced in the vision at section 5.1 C2 (full rationale + the anti-knowledge forward-trap) and section 8.2 pending_embeddings (pointer). Cites flagless-outbox-cache-absorbs-reembed as the central precedent; outbox-reenqueue-resets-drain-state and cutover-bounds-embedding-seed-to-active named in the rejected-paths.
**Cites:** flagless-outbox-cache-absorbs-reembed


### parser-comment-skip-matches-reserved-namespace-only

**Decided:** Mitos's parser comment-skip predicate (the V3a mark-rotation carve-out) matches only a reserved Mitos-namespaced sync-annotation comment, never a generic whole-line HTML comment, so a user's whole-line comment inside a Context or rejected-paths code example (such as a button-template comment line) stays literal field text and is never silently stripped; the V3a mark writer emits its annotation in that reserved namespace so it is the only comment the carve-out skips.
**Rejected:** Skipping ANY whole-line standalone HTML comment (the prior mitos-pins-in-v1a-implemented-in-v3a-that-the-parser-skips mechanism): too broad. A user can legitimately place a whole-line comment inside a Context or rejected_paths code example (a frontend snippet's button-template comment line), and a blanket whole-line skip silently drops it — corrupting user-authored content, with no Markdown-context awareness to tell a code-example comment from a sync annotation. Breaches P6 (never silently mutate the user's source) and P3 (no surprising silent drops). The original ADR pinned the skip to defeat V3a's mark annotation but did not weigh the collateral damage to generic user comments.

Adding Markdown-context awareness (fence / indent / blockquote tracking) to distinguish a code-example comment from a real annotation: the P19/P7 complexity trap the original ADR already rejected — and unnecessary, because namespacing the annotation solves it with a pure line-level predicate (match a reserved prefix), no context tracking.

Keeping generic-skip but exempting only transcript blocks (the original carve-out's only exception): insufficient — a user comment in a Context or rejected_paths code example sits outside any transcript block, so the transcript exemption does not protect it; only namespacing the skipped form protects user comments everywhere.
**Scope:** parser, substrate, format-spec, sync
**Context:** Refinement from the V1a vision paired-review round (2026-06-17, Gemini's finding). The pinned "skip whole-line standalone HTML comments" mechanism (mitos-pins-in-v1a-implemented-in-v3a-that-the-parser-skips) is broader than its goal: it defeats V3a's mark-rotation sync annotation but also silently strips any whole-line user comment (e.g. a frontend code example's button-template comment). Narrowing the skip to a reserved Mitos namespace keeps the line-level-predicate simplicity (no fence/indent tracking) while making generic user comments literal field text — aligning the carve-out with V1-D7's body rule that in-entry-stream HTML comments are literal. The exact namespaced pattern is V3a/plan-internal; the contract V1a pins is "skip a reserved Mitos namespace only, never generic comments." Coordinated requirement: V3a's mark writer must emit its annotation in that reserved namespace (not the bare synced-colon-DATE shape) so the carve-out matches it. Folded into the vision's V1-D7 breadcrumb this round.
**Narrows:** mitos-pins-in-v1a-implemented-in-v3a-that-the-parser-skips


### cutover-gate-keys-on-core-not-slug-for-q5-convergence

**Decided:** Mitos V1a's cutover reconstruction-completeness gate keys its reference-active subset-of reconstructed-active check on each node's canonical core (kind plus axiom plus mechanism_refs), not on its raw slug, so a Q5 slug convergence — two prototype entries that shared a canonical core under different slugs collapsing to one reconstructed node bearing the latest slug — satisfies the gate silently because the earlier slug's core is present under the surviving active node, instead of false-flagging the converged-away slug as a missing active decision; only a genuinely-absent canonical core triggers the existing abort (rebuild bug, P5) versus operator-override (intended purge, P6) fork.
**Rejected:** Slug-keyed gate (reference_active_slugs subset-of reconstructed_active_slugs) — the obvious implementation and the vision's original implicit shape. It is pre-Q5 thinking: it treats the human-readable handle as the identity key, but Q5 (slug-removed-from-canonical-core-hash) made the slug a mutable, convergeable handle and moved identity to the canonical core. A legitimate convergence (S2-tested: same core, different slugs collapse to one node bearing the latest slug) leaves the earlier slug absent from the reconstruction, so a slug-keyed gate false-flags it as a missing active decision and forces an abort or a spurious operator override — a "data loss" alarm on the highest-stakes V1a operation (R11) when nothing was lost. Post-Q5, "did we lose an active decision?" is a question about cores, not handles.

Routing convergence through the operator-override path (the same --allow-drops flag as an intended purge): conflates an automatic, correct, zero-loss dedup with a deliberate human deletion. The operator would have to eyeball and wave through convergences that are never losses, training them to override reflexively and eroding the gate's P5 freeze value for the real rebuild-bug case. Convergence is a silent pass ahead of the abort-vs-override fork, not an override.

Leaving the gate spec convergence-blind (status quo before this round): the cutover is the exact moment a pre-Q5 corpus (slug-in-hash) is re-parsed under the slug-free hash, so any latent same-core-different-slug pairs surface for the first time precisely here. A convergence-blind gate makes its debut failure a false data-loss alarm on the one operation that must not cry wolf — undermining R11's whole freeze-on-real-loss purpose.
**Scope:** substrate, store, sync, setup, cutover, identity
**Context:** Finding from the V1a vision paired-review round (2026-06-17, Claude+Gemini). The completeness gate (cutover-gate-defaults-abort-with-p6-operator-override) named only two shortfall causes — incomplete-rebuild bug (abort, P5) and intended purge (operator override, P6) — and keyed on the old-graph active SLUG set. Q5 (slug-removed-from-canonical-core-hash) introduces a third disposition: convergence, where two same-core-different-slug entries (reachable in the pre-0.1.12 corpus or via --acknowledge-neighbors) merge to one node under the slug-free hash, the latest slug winning (cutover-chronological-replay-by-file-position). The replay already converges correctly; the gate was the missing piece. Both the old and rebuilt graphs express {kind, axiom, mechanism_refs} as columns, so core-keyed reconciliation is mechanically feasible on the one-time cutover (the exact mechanism is plan-internal).

Related degenerate consequence, documented in vision section 2.1 / R11, NOT a separate decision because it falls out of the existing self-edge prohibition (V1-D13 source_id != target_id) under convergence: if the prototype corpus declares a kill-edge BETWEEN two now-converging entries (a rename-via-supersede/corrects where source and target share a canonical core — legal under slug-in-hash, degenerate under slug-free), that edge collapses to a forbidden self-reference and aborts the replay cycle_violation. This is an expected one-time pre-cutover corpus cleanup (the operator drops the degenerate edge; the two entries are one decision under Q5), surfaced loudly on the build-aside copy, never silent corruption. Both convergence and the degenerate edge are Q5-hash-change artifacts handled in section 2.1, not surprises.
**Amends:** cutover-gate-defaults-abort-with-p6-operator-override
**Cites:** slug-removed-from-canonical-core-hash


### embedding-text-excludes-mechanism-refs

**Decided:** Mitos's embedding_text(node) for a decision embeds the axiom only — mechanism_refs is excluded from the embedding vector (as is the mutable rejected_paths) — so a decision's semantic vector stays a single clean natural-language signal; mechanism-adjacency is served structurally (the V1b mechanism registry + the C4 structured-filter side) or by a separate vector later, never by folding folded identifier tokens into the axiom embedding.
**Rejected:** Appending mechanism_refs to the decision's embedding string (e.g. "axiom...\n\nMechanisms: postgres, redis") to boost mechanism-adjacent semantic recall (proposed by Gemini in the 2026-06-17 V1a vision-loop round): rejected. mechanism_refs are short, case- and ASCII-punctuation-FOLDED identifier tokens (mechanism_canonical_norm), not natural language — folding them into the axiom vector dilutes natural-language recall (the same dilution that keeps rejected_paths out of the embedding; see anti-knowledge-embedding-axiom-only-now-vector-later, Option B) for no reliable gain, since mechanism_refs is optional and often absent (V1-D8) and a decision that turns on a technology usually names it in the axiom anyway. Mechanism-adjacency has a cleaner structural answer — the V1b mechanism registry plus the C4 structured-filter side — and, if vector recall over mechanisms is ever wanted, a separate vector (the axiom-clean / separate-vector pattern of anti-knowledge), which strictly dominates polluting the one axiom embedding.

Justifying the mechanism exclusion via the immutability/cache-stability argument (the reason rejected_paths is excluded for churn): rejected as the WRONG reason. Unlike mutable rejected_paths, mechanism_refs IS canonical-core/immutable — it is part of the content hash {kind, axiom, mechanism_refs} — so including it would NOT churn the embedding cache (a new mechanism set mints a new node anyway). The mechanism exclusion is purely a recall-quality / vector-cleanliness choice, never a cache-stability one; conflating the two would mislead a future reader into believing mechanisms churn the cache and could invite a wrong "fix" that pollutes the golden-tested embedding_text shape.
**Scope:** vector-store, store, retrieval, substrate
**Context:** Surfaces in v-mitos-parser-and-store-v1a Section 5.1 C2 (the embedding_text(node) canonical-helper shape). embedding_text is a cross-vision contract: V1a owns, defines, and golden-tests it (V1a does zero embedding itself); V2's embedding pipeline + cache consume it unchanged. The exclusion of mechanism_refs was previously implicit ("returns axiom") with no stated rationale, so a careful reviewer (Gemini) asked whether it was intentional — this ADR makes the exclusion an explicit, reasoned decision so the question does not recur and a later author does not "fix" the golden-tested shape by appending mechanisms. Sibling to anti-knowledge-embedding-axiom-only-now-vector-later (the rejected_paths exclusion): both keep the per-decision vector a single clean axiom signal and push non-axiom recall to structured queries or a separate vector. Raised and declined in the 2026-06-17 V1a vision-loop round.
**Cites:** anti-knowledge-embedding-axiom-only-now-vector-later


### cutover-gate-defaults-abort-with-p6-operator-override

**Decided:** Mitos V1a cutover reconstruction-completeness gate defaults to abort-and-surface-offenders when a still-live old-graph active slug is missing from the rebuild (P5 Ironclad: never auto-advance a destructive swap into data loss), but exposes an explicit operator override that proceeds after the human reviews the offenders and confirms the drop is an intended corpus purge (P6: a clean re-parse of authoritative markdown is the truth), so the operator is never forced to restore purged content into decisions.md just to pass the gate.
**Rejected:** Hard-abort with no override (the prior cutover-build-aside-atomic-swap gate behavior): inverts P6. The prototype graph is append-only, so an entry the human intentionally deleted from the authoritative markdown still reads active in the old graph; an unconditional abort forces the operator to paste that garbage back into decisions.md purely to satisfy a check against the stale DERIVATIVE store — treating the derivative as more authoritative than the source of truth.

Default warn-and-proceed (auto-advance on any shortfall, markdown-is-always-truth): the swap is destructive (wipes the live graph + Qdrant, keeps only a timestamped .bak), and the dominant cause of a missing active slug is a rebuild BUG (missing archive file, parser regression), not an intentional purge; default-proceed silently loses active decisions the team relies on — exactly the "wrongful advance into permanent data loss" P5 Ironclad says to replace with freeze-plus-granular-quarantine. The .bak makes it recoverable, but recovery would be unconscious (the operator must happen to notice the warning), not a deliberate gate.

A heuristic that auto-classifies intended-purge vs bug (e.g. cross-check decisions.md git history): over-engineering on a one-time operator step; the human already has the context to judge the surfaced offenders, so a conscious override flag is simpler and more honest than a guess (P12 / P19). The override surfaces the offenders either way; only the default disposition differs.
**Mechanisms:** reconstruction-completeness-gate, build-aside
**Scope:** substrate, store, sync, setup, cutover
**Context:** Settled in the V1a parser-and-store vision paired-review loop (2026-06-17) by Claude and Gemini (his finding 2; Claude refined warn-vs-abort to the P5-default + P6-override hybrid). Folded into the vision doc §2.1 + R11 the same round.
**Amends:** cutover-build-aside-atomic-swap
**Cites:** v1a-cutover-wipe-and-rebuild


### questions-raised-order-significant-prose-not-sorted-set

**Decided:** Mitos treats an open_question questions_raised list as an ordered prose sequence whose authored order is identity-significant: the canonical-core hash and the embedding_text both preserve authored order with an order-preserving dedup and never sort it, unlike the unordered mechanism_refs tag set which is set-sorted, so reordering the questions is a meaningful canonical-core edit that mints a new node while an exact-duplicate question still converges.
**Rejected:** sorted(set(questions_raised)) — sorting questions under code-point collation for set-convergence (the prior V1a §11/§12 rule): scrambles the narrative / logical progression of natural-language questions before embedding_text feeds them to the model, degrading semantic recall on every multi-question OQ, purely to satisfy a hash-convergence rule that was never load-bearing for OQs. The slug-removed-from-canonical-core-hash ADR already records that cross-capture convergence is a weak benefit for Mitos's one-entry-one-decision corpus, and V1a OQs are hand-authored (no LLM dual-capture to converge). The vision's own §12 already case-preserves questions (prose) while case-folding mechanism_refs (tags); extending that same prose-vs-tags asymmetry from case to ORDER is the consistent move.

Decouple — sort for the hash but use authored order for embedding_text: breaks M8 ("embedding_text is a pure function of the immutable canonical core") — two authored orderings would share one id (sorted hash) yet derive different embedding strings, breaking V2's content-hash-keyed cache (the "re-derives the identical string" guarantee).

Pure authored order with NO dedup (max simplicity): loses cheap M2 duplicate-protection — an accidental exact-duplicate question would mint a divergent hash; order-preserving dedup removes that noise without scrambling the prose, the better balance (Claude + Gemini, 2026-06-17).

Doing nothing (ship the sort): the corpus has ZERO open questions today, so re-specifying the OQ hash form is free now but becomes a breaking re-mint of every OQ id once OQs exist — P20 wire-while-walls-open mandates fixing the contract while the wall is open.
**Mechanisms:** questions_raised_list_norm, embedding_text, canonical_core_string_norm
**Scope:** identity, hashing, substrate, embeddings, parser
**Context:** Settled in the V1a parser-and-store vision paired-review loop (2026-06-17) by Claude and Gemini; folded into the vision doc (§1, §5.1 C2, §6.2, §11, §12) the same round. Free to fix now: zero OQs in the corpus.
**Amends:** canonical-core-list-fields-dedup
**Cites:** open-questions-authored-in-separate-questions-md-file


### v1a-edge-catalog-check-defers-reference-table

**Decided:** V1a enforces the edge-type whitelist and same-kind constraint with a table-level CHECK on the STRICT edges table; the edge_type_matrix reference table is deferred to V1b, introduced via a contained edges-rebuild when the non-uniform cross-kind catalog (resolves, derives_from) lands in V1b.
**Rejected:** Shipping the edge_type_matrix reference table plus composite FK in V1a (the prior edge-catalog-reference-table decision). Rejected because V1a's matrix is uniformly same-kind over exactly two edge types (supersedes, corrects), which a table-level CHECK expresses completely, and edge validation is store-internal — consumers bind to store methods, not the edges DDL — so deferring the reference table breaks no cross-vision contract and the later migration to it is a cheap, contained edges-rebuild (milliseconds at Mitos scale, absorbed by the §5.2.5 migration ladder). The prior decision's load-bearing rationale was that a CHECK forces an "expensive later" rebuild; that framing was overturned this round (the rebuild is cheap), and once it is, P20's destructive-retrofit condition fails, so YAGNI/P12 keeps V1a on the simple CHECK rather than paying the relational tax up front. The reference table remains the right model for V1b's non-uniform cross-kind matrix — this defers WHEN it is introduced, not WHETHER. Application-layer (Python) enforcement stays rejected per Lesson 2 (structural over discipline): the CHECK is substrate-enforced, exactly as the reference table would be.
**Mechanisms:** edge_type_matrix, edges, check-constraint
**Scope:** substrate, store, schema, edges
**Context:** Settled in the V1a (parser-and-store) vision review round 4 (2026-06-17) with Gemini. Gemini flagged the reference table as a V1a relational tax for a 2-value same-kind enum; Claude agreed on the Retrofit Test and lifted V1-D13 to altitude — pinning the contract (substrate-enforced, additively-extensible edge validation) and the design intent (the reference table is V1b's natural form for its cross-kind matrix) while V1a uses a plain table-level CHECK. The prototype already ships a table-level CHECK on edges (verified this round via a codebase sweep), so V1a keeps that shape, hardened onto a STRICT table. Reasoning is symmetric with no-inline-derived-cache-reservations-on-nodes, which defers the same way when the retrofit is non-destructive and additive. Vision touchpoints: V1-D13 (§4.3), §8.2 edges row, §6.1 cluster 2, §4.2 C1 store-owns.
**Supersedes:** edge-catalog-reference-table


### mitos-pins-in-v1a-implemented-in-v3a-that-the-parser-skips

**Decided:** Mitos pins (in V1a, implemented in V3a) that the parser skips whole-line standalone HTML comments as non-content — a mid-line comment stays literal and transcript-internal comments stay verbatim — so V3a mark-rotation sync-annotation comments never become node commentary
**Rejected:** Leaving the mechanism open as a V3a-or-plan choice (the prior breadcrumb wording): the seam stays unresolved and risks each consumer re-deciding it. Having the mark writer hide its annotation in a tokenizer-discarded zone instead of touching the parser: infeasible for a per-entry sync annotation, since the only discarded span is the file preamble and that cannot annotate a specific entry, so the parser carve-out is the forced mechanism. Shipping the skip-rule code in V1a: dead code, because V1a ships no mark writer and the live corpus has zero in-entry-stream standalone comments (all are preamble or sentinel), so P12 defers the implementation to V3a while the mechanism is pinned now. Broad Markdown-context-aware comment stripping (fence, indent, blockquote tracking): a P19 and P7 complexity trap V1-D7 deliberately avoids — a whole-line skip is a line-level predicate, not context-aware parsing.
**Mechanisms:** parser, mark-rotation, html-comment
**Scope:** parser, substrate, format-spec, sync
**Context:** V1a parser-and-store vision, economics and framework review round (Gemini finding 3, Claude synthesis); upgrades the V1-D7 mark-rotation breadcrumb from an open mechanism to a pinned contract, in the pin-the-contract-defer-the-implementation shape of transcript-traversal-single-store-primitive
**Cites:** decisions-md-sentinel-over-regex-scan


### qdrant-payload-active-prefilter-hint

**Decided:** The Mitos Qdrant payload carries only a single non-authoritative active boolean as a server-side pre-filter hint (mutable slug and scope dropped, computed state never trusted); Qdrant pre-filters on the hint and SQLite always post-filters the returned ids against its live computed view as the sole source of truth, so a stale hint yields at worst a bounded self-healing false-negative on the semantic path, never a wrong answer.
**Rejected:** Option 1 (drop ALL payload — Qdrant a pure (point_id, vector) engine): the M3-purist shape, zero stored state, but it pays a P11 retrieval cost. To filter active/scope at query time, SQLite must either ship a ~10K-UUID `has_id` array to Qdrant per scoped query (array bloat at the 50K-decision horizon), or over-fetch top-K + buffer and post-filter in SQLite — where a deep `supersedes` chain ranks dead near-duplicates high and buries the one active node, making the over-fetch K fragile to size for guaranteed recall. Rejected because Option 3 delivers the same correctness (SQLite is always the truth) without either cost.

Option 2 (keep {slug, scope, kind} in the payload — the Q2 status quo): forces V1a's Outbox to re-enqueue the node on EVERY commit, including pure commentary edits (a slug rename, a scope edit), so the drainer re-pushes the mutable payload — replicating mutable data across two stores (a milder echo of the exact M3 drift Q2 killed for computed state), and redundant anyway with the SQLite hydrate V2 performs on every query regardless. Rejected as the heaviest Outbox shape for a self-describing payload nobody needs.

Both rejected: Option 3 is the only shape that is BOTH M3-coherent (truth stays computed in SQLite; the payload active flag is a disposable index hint, never trusted — distinct from Q2's sin, where state was read back and TRUSTED) AND P11-clean (Qdrant pre-filters on the boolean server-side — no id-array shipped, no over-fetch burial). The honest cost is one narrow self-healing window (see Context).
**Mechanisms:** qdrant, active-hint, pre-filter, pending_embeddings, m3, has-id
**Scope:** vector-store, store, substrate, retrieval, qdrant
**Context:** V1a parser-and-store vision-loop, Vinga's decision settling Q-Payload — an opera section 4.D override extending Q2 — 2026-06-17. Q2 (drop-computed-state-from-qdrant-payload) dropped the TRUSTED computed state but left mutable slug/scope in the payload, which forced the Outbox to enqueue on every commit to keep them fresh. Option 3 finishes Q2's logic: drop slug/scope too, and add back only a single non-authoritative active boolean as a server-side pre-filter hint.

The honest caveat (one narrow window): activation is NOT monotonic — section 4.5.1 resurrection flips a killed node back to active when its Supersedes/Corrects line is removed (a deliberate P6 feature). So a stale active:false on a just-resurrected node would wrongly EXCLUDE it from SEMANTIC results until the next drain — a bounded, self-healing FALSE-NEGATIVE (never a false-serve-dead). It is semantic-recall-only (structured / scope / exact-slug queries hit SQLite directly and always see it), self-healing (bounded to the drain interval), and mitigated by enqueuing the kill-edge target on edge-removal too.

V1a ripple: V1a ships UNCHANGED (committing-node-only Outbox; no Qdrant code). When V2 builds the active-hint payload it additively re-introduces a single-node kill-edge-target enqueue (on edge ADD and REMOVAL) so the hint refreshes — additive, no wall (an extra enqueue call in commit_parsed_entry; the pending_embeddings schema is unchanged). Amends drop-computed-state-from-qdrant-payload (keeps its no-trusted-state-in-Qdrant core; refines the payload to the non-authoritative active hint and re-adds the target enqueue). Still outstanding for Vinga's pen: opera section 4.D literally lists slug/scope/state/kind/embedding_text — even the Q2 state-drop looks un-applied there; this amendment folds in cleanly. Vision touchpoints: section 5.1 C2, section 8.2 pending_embeddings, and the committing-node-only rationale sites.
**Amends:** drop-computed-state-from-qdrant-payload


### overflow-warnings-as-debounced-data-not-per-write-prints

**Decided:** Mitos renders size-ceiling overflows as structured data on MitosRenderer.overflows (never printed mid-render); the shared write path attaches one debounced (at most once per 24h per workspace, via config.hint_due) scope_overflow summary line AFTER the success receipt, and the full per-file plus top-N-largest-decisions breakdown lives on mitos status — so a healthy growing active set never buries the record receipt under a per-write wall of warnings.
**Rejected:** Printing a "[Warning] ... exceeds N characters" line per over-ceiling file inside MitosRenderer.render_all on every write (the prior behaviour). Three reasons it failed: (1) it fired on every record/record_decision and buried the "Recorded ✓" receipt under a wall of warnings that climbs monotonically as the corpus grows — alarming on a healthy growing active set, the opposite of the tool's render-the-active-set thesis; (2) the renderer printing to stdout on the shared write path corrupts the MCP stdio JSON-RPC channel; (3) "divide into smaller sub-scopes" is not actionable mid-record. Also rejected: attaching a structured overflow field on every write with no debounce (still per-write noise), and adding a separate "doctor" verb (the detail was folded into the existing status health surface instead, since status already exists and is the natural home).
**Mechanisms:** mitos-status
**Scope:** ax
**Context:** Most-repeated AX_FEEDBACK complaint (~10 rounds). Fix: renderer records overflows as data on MitosRenderer.overflows; assemble_render is the single content source of truth shared by render_all (writes) and overflow_report (read-only sizing) so they cannot drift; config.hint_due is a shared fail-silent debounce backing both this summary and the older MCP-server hint. Thresholds renamed GLOBAL_OVERFLOW_WARN_CHARS / SCOPE_OVERFLOW_WARN_CHARS (the unit actually measured) with estimate_tokens() reported alongside, reconciling the tokens-vs-chars naming drift. The sync.py buffer-first + rollback write contract is untouched — this is presentation/throttling only, after the commit.


### cutover-chronological-replay-by-file-position

**Decided:** Mitos V1a's prototype-to-V1a cutover commits the corpus single-pass in chronological oldest-first order derived from file position (reversing newest-first decisions.md; archive files oldest-quarter-first, each reversed), not by topologically sorting a global DAG of declared kill-edges, because a mutable reusable slug can map to several entries so only the live committed state at each commit disambiguates which entry a Supersedes or Corrects line targets, and oldest-first replay both resolves every edge against that state and is itself a valid topological order for new-to-old kill-edges.
**Rejected:** Topological sort over a global pre-commit DAG of the declared Supersedes/Corrects slugs (the superseded cutover-topological-sort-not-file-order mechanism). It was adopted earlier on 2026-06-17 to let the human author decisions.md in any visual order, but that benefit is illusory and the mechanism is unsound. The slug is a mutable, REUSABLE handle: same-slug supersession (V1-D4 case 1) deliberately preserves a citation handle across a new id, so one slug maps to several entries (N1, N2, ... all carry beta). A global DAG built before any node commits cannot tell which entry a `Supersedes: beta` line targets; it either fabricates false cycles or links the edge to the wrong node. To disambiguate it would have to chronologically pre-sort the entries anyway, at which point the chronological order already IS the commit order and the DAG is redundant machinery (P19/Lesson 2). Topo-sort also leaves the convergence/independent-node tiebreak nondeterministic: graphlib's ordering of dependency-independent nodes is not a guaranteed authored-recency order, so the rebuilt graph's live-slug assignment for converged/renamed nodes varies across runs, a quiet break of the determinism / reproducible-rebuild ethos (MI-1, P5/P6).

Also rejected: preserving the "any visual order" ergonomic by adding a required Decided: date field to key replay on (instead of file position). It is a format-spec / C5 change with dogfood-corpus blast radius (every entry would need a date) and breaks P19's boring-at-the-core mandate; the existing newest-first convention (opera 4.A, verified present in the live corpus by the original cutover ADR) already encodes recency at zero cost. A genuinely mis-ordered paste surfaces as a loud, recoverable missing_target / cycle abort on the one-time, gated, build-aside cutover (the live graph never touched), never the silent mis-resolution topo-sort risks. The "DB strictness leaking into the text editor" concern the topo-sort ADR raised is answered by that loud-fail on a one-time operator step, not by an unsound global DAG.
**Mechanisms:** cutover, chronological-replay, commit_parsed_entry, supersedes, corrects
**Scope:** substrate, store, sync, identity, setup, cutover
**Context:** V1a parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-17. Reverses cutover-topological-sort-not-file-order (recorded earlier the same day) after Gemini's fresh-eyes pass, converging with Claude's independent determinism finding, showed topo-sort cannot disambiguate reused mutable slugs (same-slug supersession is first-class) and yields a nondeterministic rebuild tiebreak. Restores the chronological oldest-first substance of cutover-single-pass-oldest-first-drops-commit-batch (file reversal + archive ordering) with a new, sharper rationale: chronology is REQUIRED, not merely convenient, because slug reuse makes a global DAG ambiguous. Unchanged: no commit_batch primitive, strict per-entry atomicity (V1-D10), the source-active acyclicity guard (kill-edge-acyclicity-requires-active-source), and the active-bounded embedding seed (cutover-bounds-embedding-seed-to-active). Applied to vision sections 1.1, 2.1 (cutover mechanism + embedding-queue-bound + V1-D13 created_at clause wording), and 5.2.9. INVARIANTS.md note candidate at V1a closeout (cutover ordering is chronological file position, not topo-sort); INVARIANTS.md is Vinga's to edit.
**Supersedes:** cutover-topological-sort-not-file-order
**Cites:** cutover-single-pass-oldest-first-drops-commit-batch


### cutover-topological-sort-not-file-order

**Decided:** Mitos V1a's cutover computes its single-pass commit order by topologically sorting the declared kill-edges (stdlib graphlib) rather than reversing the newest-first file, so the human authors decisions.md in any visual order while every kill-edge target still commits before its superseder.
**Rejected:** File-position ordering (the prior cutover-single-pass-oldest-first-drops-commit-batch mechanism: reverse the newest-first file, commit archive files in filename-chronological order each reversed, abort with missing_target on a convention violation). It works only if the human keeps decisions.md perfectly newest-first and topologically consistent by hand; a superseder pasted below its target, or decisions grouped by topic instead of time, crashes the cutover and tells the human to manually re-sort a thousand-line markdown file to satisfy SQLite's foreign keys -- DB strictness leaking into the text editor, a direct violation of the "markdown for humans" thesis. Topological sort removes that constraint at trivial cost: graphlib is stdlib on the 3.13 floor (no new dependency, P19), and the cutover already loads every entry into memory for the reconstruction-completeness gate, so the DAG build is nearly free.

Also rejected: reintroducing the commit_batch two-pass primitive to get forward-reference transparency -- unnecessary, because topological sort over a strict single-pass commit_parsed_entry loop keeps per-entry atomicity (V1-D10) AND the source-active acyclicity guard, the two properties the relaxed two-pass discarded; the no-commit_batch-primitive half of cutover-single-pass-oldest-first-drops-commit-batch is unchanged.

Also rejected: relying on filename-chronological multi-archive ordering -- topological sort over all loaded entries needs no file ordering at all, so the multi-archive special case dissolves.

Topo-sort eliminates only ORDER-induced missing_target: a genuinely missing target (a slug cited with no entry anywhere) still fails missing_target at commit, and a true kill-edge cycle aborts loud -- both real corpus defects, not authoring-style friction.
**Mechanisms:** cutover, topological-sort, graphlib, commit_parsed_entry, supersedes, corrects
**Scope:** substrate, store, sync, identity, setup, cutover
**Context:** V1a parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-17, Gemini Saboteur Finding 2. The prior oldest-to-newest file-reversal cutover (cutover-single-pass-oldest-first-drops-commit-batch) forced the human to topologically pre-sort decisions.md by hand or watch the cutover abort with missing_target -- DB referential integrity leaking into the human's buffer, against the markdown-for-humans thesis. Python does the topological sort instead (graphlib.TopologicalSorter, stdlib on the 3.13 floor). Keeps single-pass commit_parsed_entry, per-entry atomicity (V1-D10), the source-active acyclicity guard (kill-edge-acyclicity-requires-active-source), and the active-bounded embedding seed (cutover-bounds-embedding-seed-to-active, unaffected -- the topo replay still commits every node transiently active then bounds the seed). Dependency-independent entries keep authored recency as the topo-sort tiebreak, preserving the Q5 convergence latest-slug-wins property the file-order gave for free. Applied to vision sections 1.1, 2.1 (cutover mechanism + embedding-queue-bound wording + V1-D13 created_at clause), and 5.2.9. Amends cutover-single-pass-oldest-first-drops-commit-batch: replaces its file-position ordering sub-mechanism with topological sort; the no-commit_batch / single-pass / source-active thrust is unchanged. Candidate INVARIANTS.md note at V1a closeout (cutover ordering is topo-sort, not file position); INVARIANTS.md is Vinga's to edit.
**Amends:** cutover-single-pass-oldest-first-drops-commit-batch


### outbox-reenqueue-resets-drain-state

**Decided:** The Mitos vector-sync Outbox re-enqueue UPSERT resets the row drain state (re-stamps queued_at and clears retry_count) on every commit rather than being a bare idempotent no-op, so a node the V3b drainer dead-lettered after a transient embedding-provider failure is never permanently stranded at max-retry and an explicit re-commit is always a fresh embedding attempt.
**Rejected:** A bare idempotent no-op UPSERT (the reading the simplified MI-12 was left with after flagless-outbox-cache-absorbs-reembed dropped the one-dominates-zero rule): it leaves retry_count untouched on a re-enqueue, so once V3b dead-letters a node whose embedding failed during a transient provider outage, a later re-commit cannot revive it and the node never embeds — a P5 self-healing violation (re-running must pick up cleanly). Resetting only when embedding_text changes was also rejected: embedding_text is the immutable canonical core (an axiom edit mints a new node_id, a fresh INSERT, never a collision on an existing row), so a same-node_id re-enqueue is either a commentary edit or a deliberate retry, and the simplest correct contract is to treat any explicit re-commit as a fresh drain attempt.
**Mechanisms:** pending_embeddings, retry_count, queued_at, outbox
**Scope:** store, substrate, vector-store, schema, sync
**Amends:** flagless-outbox-cache-absorbs-reembed


### node-scopes-casefold-scope-tags

**Decided:** Mitos casefolds node scope tags with Python str.casefold() before they land in the node_scopes join table, a permanent single-column casefold with no case-preserved twin, so Substrate and substrate collapse to one scope and indexed scope-membership joins never silently fragment by case; scope is a pure filter dimension with no citation or display role, which is why it permanently casefolds while the slug keeps a case-preserved form plus a derived slug_casefold.
**Rejected:** Storing scope case-preserved (the prior mitos-stores-node-scope-as-a-multi-valued-node-scopes-node-id shape, which indexed scope but never casefolded it): an LLM or human authoring Substrate once and substrate elsewhere creates two distinct tags, so a scoped C4 query or a V4 per-scope render for one silently misses the other — the same case-fragmentation slug_casefold already fixes for slugs, left open for scope. Also rejected: replicating the slug two-column preserve-plus-derive dance for scope (a scope + scope_casefold pair) — the slug keeps a case-preserved form because it is a citation/display handle (Use-SQLite must render as authored while matching use-sqlite), but scope has no display or citation role, so there is nothing to preserve and a permanent single-column casefold is simpler and sufficient. Also rejected: SQLite COLLATE NOCASE / LOWER() on the scope column — ASCII-only by spec, so it silently fails to match Lithuanian/CJK scope case-pairs (the same trap slug-casefold-derived-search-key documented); Python str.casefold() at the C1 boundary is Unicode-correct.
**Mechanisms:** node-scopes, scope, str-casefold, index, sqlite
**Scope:** substrate, store, schema, identity, performance
**Context:** V1a parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-16, simplification pass. Gemini's finding: node_scopes indexes scope for WHERE scope = ? membership joins but, unlike the slug, never casefolds it, so case-variant scope tags silently fragment a scoped query or per-scope render. Fix: casefold-then-dedup scope tags at the C1 boundary (mirroring the existing mechanism_canonical_norm fold-then-dedup precedent for mechanism_refs), stored permanently in the single scope column. Sibling to slug-casefold-derived-search-key — same Unicode discipline (Python str.casefold(), never SQLite NOCASE/LOWER) — but single-column because scope, a pure filter dimension, needs no case-preserved display twin. Applied to vision section 8.2 node_scopes (scope holds the casefolded form) and MI-9 (the parser emits a casefolded, deduped scope-tag list at the C1 boundary). Amends mitos-stores-node-scope-as-a-multi-valued-node-scopes-node-id (adds the casefold property to its node_scopes design, not reversing the join-table decision); cites slug-casefold-derived-search-key as the sibling Unicode-correct-indexed-key precedent.
**Amends:** mitos-stores-node-scope-as-a-multi-valued-node-scopes-node-id
**Cites:** slug-casefold-derived-search-key


### cutover-build-aside-atomic-swap

**Decided:** Mitos V1a cutover from the prototype builds the rebuilt graph aside in a separate file, runs the reconstruction-completeness gate across the still-live old graph and the new build, and atomically swaps the new graph into place while retaining the old as a timestamped backup, only after the gate passes, so the live graph is never mutated until the rebuild is proven complete and a hard crash mid-rebuild recovers by discarding the orphaned build with no manual restore.
**Rejected:** Backup-then-wipe-in-place-then-rebuild (the prior cutover-backs-up-and-reads-old-graph-for-gate mechanism): reversible via the timestamped .bak, but a hard crash (kill -9, power loss) between the in-place wipe and rebuild-complete leaves graph.sqlite half-built and broken, requiring a MANUAL .bak restore — short of P5's Unplugged Standard (recover after a hard crash with no manual intervention). The completeness-check FAILURE path already aborted cleanly (the gate runs before the wipe), so the only open gap was the crash-DURING-rebuild window; build-aside closes it by never touching the live graph until the atomic swap. Also rejected: dropping the backup entirely now that the old graph survives untouched until the swap — retaining the old as a timestamped backup AS the swap costs nothing and keeps P5 reversibility on top of the no-broken-intermediate-state guarantee. Also rejected: an in-place ALTER-TABLE migration (already foreclosed by v1a-cutover-wipe-and-rebuild — the graph is P6-derivative and the V1a re-hash re-mints every node id).
**Mechanisms:** cutover, graph-backup, build-aside, atomic-swap, sqlite
**Scope:** substrate, store, sync, setup
**Context:** V1a parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-16, simplification pass. Gemini's finding: the prior backup-then-wipe-in-place cutover leaves the live graph half-built and broken after a hard crash mid-rebuild, needing a manual .bak restore. Build-aside — write the rebuild to a separate file (e.g. graph.sqlite.new), run the reconstruction-completeness gate across the still-live old graph plus the new build, then atomically os.rename-swap the new graph in (renaming the old aside to a timestamped backup) only on a passing gate — never mutates the live graph until the rebuild is proven complete, so a hard crash recovers by deleting the orphaned build (P5 Unplugged Standard: hard-crash recovery with no manual intervention). The gate still reads its reference active-slug set from the old graph, now still-live rather than pre-backed-up. Plan-level (kept out of the vision body): the swap must checkpoint + close the new DB and clear stale -wal/-shm sidecars before the rename, and the os.rename / ATTACH mechanics are plan-internal. Applied to vision section 2.1. Supersedes cutover-backs-up-and-reads-old-graph-for-gate (its read-the-old-graph-for-the-gate half survives in this body; its backup-before-in-place-wipe half is replaced by build-aside-and-swap) and amends v1a-cutover-wipe-and-rebuild (the wipe-and-rebuild becomes build-aside-and-swap; the gated, P6-derivative, re-hashing rebuild is otherwise unchanged).
**Supersedes:** cutover-backs-up-and-reads-old-graph-for-gate
**Amends:** v1a-cutover-wipe-and-rebuild


### cutover-bounds-embedding-seed-to-active

**Decided:** Mitos V1a's cutover and bulk-rebuild bounds its final pending_embeddings seed to the active node set (active decisions plus Stage-1-surviving open questions), so the dead and superseded back-catalogue self-enqueued during the oldest-first replay is not left in the queue and V2's first sync embeds only the live architectural surface.
**Rejected:** (1) Leaving the cutover queue flooded with the whole corpus because V1a ships no drainer (inert until V2): rejected — it hands V2 a misleading "embed everything" queue (Operational Economics section 5: dossier handoffs must be scoped to what the next stage needs) and silently couples the active-bound onto V2's first-sync logic. (2) Adding a do-not-enqueue flag to commit_parsed_entry for the bulk path: rejected — it complicates the flagless single-path Outbox primitive (flagless-outbox-cache-absorbs-reembed); a cutover-level post-loop reconciliation of the queue leaves the per-entry primitive untouched. (3) Embedding the full dead history for possible future historical/Vibe semantic search: rejected as speculative (P20 / YAGNI) — no current or roadmapped consumer returns inactive vectors (V2 retrieval is active-scoped via has_id per drop-computed-state-from-qdrant-payload), and v0.3 Vibe can backfill-embed dead-node vectors additively if it ever needs them, with no wall broken.
**Mechanisms:** pending_embeddings, has_id
**Scope:** substrate, store, sync, vector-store, cutover
**Context:** The oldest-first cutover replay commits every corpus node while it is transiently active (the entry that supersedes or corrects it commits later in the same loop), so the per-entry self-enqueue (V1-D10) accumulates the entire corpus — dead nodes included — in pending_embeddings. Because V2 retrieval recomputes activeness and filters via has_id (drop-computed-state-from-qdrant-payload), inactive vectors are never returned, so embedding them is pure waste — acute at the P11 horizon, where a 40-deep supersedes chain is 39 dead plus 1 active. Bounding the cutover seed to the active set restores symmetry with the incremental path, already Q2-bounded to the committing live node only. Surfaced in the 2026-06-16 V1a economics review round, where Claude and Gemini both independently flagged the cutover queue flood. Vision touchpoints: section 2.1 "Embedding-queue bound" and the section 8.2 pending_embeddings row.
**Depends-On:** cutover-single-pass-oldest-first-drops-commit-batch
**Cites:** drop-computed-state-from-qdrant-payload


### provenance-source-field-in-markdown-for-rebuild-faithfulness

**Decided:** Mitos carries node provenance (the nodes.source field) via an optional **Source:** field in the markdown entry format — parser-read, validated against the {user, capture_llm, import_llm} enum, defaulting to user when absent — rather than stamping it out-of-band at commit, so provenance survives the wipe-and-rebuild that re-parses only the markdown corpus.
**Rejected:** Out-of-band commit-time source parameter (V3a/V6 pass source into commit_parsed_entry): rejected — section 2.1's wipe-and-rebuild re-parses only the markdown, so source would reconstruct as user on every rebuild, breaking P2/M7/P6 rebuild-faithfulness of provenance.
Drop the nodes.source column entirely: rejected — reverses V1-D20 and forfeits P6 provenance tagging plus the P20 backfill-avoidance argument.
Wire only the column now and add the format-spec field later at V3a-time: rejected — adding a field to format-spec.md after V1a's parser and V5's skill have pinned its revision is a coordinated C5 change across visions, the exact destructive retrofit P20 says to avoid; V1a owns format-spec creation, so the field is free now.
**Mechanisms:** source-field, format-spec, nodes-source, mi-11, parse-commit
**Scope:** substrate, parser, format-spec, provenance, identity
**Context:** Surfaced in the paired vision-review round (Claude + Gemini, 2026-06-16): V1-D20 wired the source column but left no markdown input channel, so under MI-11 (parse->commit is the only graph writer) capture_llm/import_llm were un-populatable dead slots. Framework-settled (P6/P2/P20), applied this round; the format-spec field edit is veto-able by Vinga.


### anti-knowledge-text-helper-deferred-to-v2

**Decided:** Mitos defers the anti_knowledge_text(node) embedding-input helper to V2, defining it when V2 builds the separate anti-knowledge vector rather than pre-shipping its shape in V1a, because V1a does zero embedding so an early helper shape protects no cache and V2 adds the pure Python function additively with no wall broken; the Q4 decision that embedding is axiom-only now with a separate anti-knowledge vector as the V2 target is unchanged.
**Rejected:** Wiring the anti_knowledge_text(node) helper shape into V1a now (Q4 part B, from anti-knowledge-embedding-axiom-only-now-vector-later): justified at the time under P20 (wire-while-walls-open) as preventing a corpus-wide re-embed when V2's anti-knowledge vector ships. Rejected on review: P20 governs STRUCTURAL state (schemas, columns) where adding-later forces a data migration; anti_knowledge_text is a pure Python function returning a string, and V1a does zero embedding, so there is no V1a-era embedding cache for the shape to protect. When V2 builds the vector it defines the helper and embeds against it in the same step, so nothing extra is re-embedded; the helper is additive-later with no wall broken (P12 do-not-pre-solve, P20 additive-retrofit), making a V1a pre-ship dead code that does not serve P20's purpose. Distinguished from embedding_text, which V1a keeps: embedding_text is coupled to V1a's canonical normalization (the open_question form is topic plus questions_raised in canonical-core sorted-and-deduped order, literally V1a's hashing output) and is golden-tested beside the section 11 hash goldens, so it earns its V1a place; anti_knowledge_text over MUTABLE rejected_paths has no such coupling and is pure V2.
**Mechanisms:** anti-knowledge-text, embedding-text, rejected-paths, p20
**Scope:** vector-store, store, retrieval, substrate
**Context:** V1a parser-and-store vision-loop paired review (Gemini Finding 3, Claude concur), routed to Vinga because it narrowly reverses part B of her same-day Q4 decision; Vinga chose remove, 2026-06-16. Amends anti-knowledge-embedding-axiom-only-now-vector-later: keeps its part A (axiom-only embedding now, a separate anti-knowledge vector over rejected_paths as the V2 target) and drops only part B (the wire-the-anti_knowledge_text-helper-shape-into-V1a-now sub-decision). Applied to vision section 1 deliverables, section 5.1 C2, section 6.2 (the golden-coverage sentence removed), and section 10 P20 (moved from the wired-hooks list to the Deliberately NOT wired list). embedding_text and its golden tests are untouched.
**Amends:** anti-knowledge-embedding-axiom-only-now-vector-later


### cutover-single-pass-oldest-first-drops-commit-batch

**Decided:** Mitos V1a ships no commit_batch primitive and its prototype-to-V1a cutover commits the corpus single-pass in oldest-to-newest dependency order by looping the strict per-entry commit_parsed_entry, so every kill-edge target pre-exists when its superseder commits, per-entry atomicity (V1-D10) holds universally with no relaxed-atomicity exception, and V3a and V6 build their own batch ordering additively if and when they need it.
**Rejected:** Shipping the two-pass commit_batch(entries) store primitive (v1a-commit-batch-two-pass-primitive) that upserts all nodes then reconciles all edges: it was justified as forward-reference transparency so V3a need not reinvent topological sort, but (1) every Mitos edge points new-to-old (V1-D13 universal directional invariant), so a forward reference is ALWAYS a causal violation, never legitimate, so there is nothing valid to protect; (2) single-pass oldest-to-newest is REQUIRED for the new source-active acyclicity guard to avoid false rejections, since a two-pass unordered edge phase could add the A-supersedes-B edge before B-supersedes-A and then wrongly reject the legitimate B-supersedes-A; (3) commit_batch relaxes per-entry atomicity (node in pass 1, edges in pass 2), which a single-pass commit_parsed_entry loop keeps strict; (4) the primitive is additive-later for V3a/V6 (a plain store method, no wall broken), so P20/P12 say do not pre-build it speculatively. The original cutover ADR (mitos-v1a-s-cutover-and-bulk-rebuild-path-commits-entries-in) already blessed single-pass-reversal as an equivalent form, so this picks the simpler pre-blessed option. Also dissolved: commit-batch-residual-collision-isolates-later-entry, whose entire subject (isolating a residual collision inside commit_batch's relaxed atomicity) no longer exists; under strict single-pass commit a residual same-casefold-slug collision between two distinct-core entries is just the second committer's normal V1-D4 case-3 slug_collision rollback. Also rejected: the worry that the multi-archive corpus has no global newest-first order for a single-pass reversal to exploit — handled by committing archive files in chronological filename order with each file reversed, and a within-file convention violation surfaces as a loud missing_target abort in the one-time, gated, .bak-backed cutover (recoverable, not silent corruption).
**Mechanisms:** cutover, commit_parsed_entry, bulk-commit, supersedes, corrects
**Scope:** substrate, store, sync, identity
**Context:** V1a parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-16. Gemini Finding 2, unified with Finding 1 (the acyclicity fix). Reverses the 2026-06-15 Claude+Gemini convergence that shipped commit_batch; the new argument is that single-pass oldest-to-newest both auto-satisfies the source-active acyclicity guard and rejects causal-violation forward references correctly, which the two-pass discards. Supersedes v1a-commit-batch-two-pass-primitive and dissolves commit-batch-residual-collision-isolates-later-entry (left in the corpus as a historical refinement of the now-superseded primitive; its mechanism is gone). Cites kill-edge-acyclicity-requires-active-source. Applied to vision sections 1.1, 2.1 (cutover mechanism), and 5.2.9 (rewritten as a no-batch-primitive note). V1-D10 per-entry atomicity now holds universally with no exception. Any future V6 batch importer inherits the source-active guard.
**Supersedes:** v1a-commit-batch-two-pass-primitive
**Amends:** mitos-v1a-s-cutover-and-bulk-rebuild-path-commits-entries-in
**Cites:** kill-edge-acyclicity-requires-active-source


### kill-edge-acyclicity-requires-active-source

**Decided:** Mitos guarantees kill-edge acyclicity structurally by requiring a kill-edge source node to be active at commit (on top of V1-D6 active-target), so an already-superseded-or-corrected node cannot mint a new kill-edge and the attempt fails cycle_violation; with both endpoints required active and the edge then deactivating its target, a cycle is impossible because each node would need its outgoing-edge commit to precede its incoming-edge commit all the way around.
**Rejected:** The prior claim that an active-target rule alone guarantees acyclicity (kill-edge-acyclicity-via-active-target-not-timestamp): unsound, because activeness is computed and CHANGES as edges commit. A cycle-closing kill-edge always points at a node that is active at that instant but that the edge itself deactivates, so V1-D6's target-active check passes and the cycle closes. Concretely: A superseded by B (A inactive, B active); the user edits A's still-in-buffer entry to add Supersedes B; B is active when A's edge resolves, so the edge commits, and B flips inactive, leaving A and B mutually superseding and both inactive. The slug-collision assertion misses it (zero active nodes then carry the slug, and zero is not greater than one). It is reachable in the incremental commit_parsed_entry path, not only the cutover, so the fix must live in the per-entry commit contract. Also rejected: Gemini's earlier created_at arrow-of-time assertion (already foreclosed: created_at is derivative and re-stamped by the rebuild). Also rejected: Gemini's alternative of accepting cycles and adding a runtime recursive-CTE cycle detector to get_transcript while dropping the structural claim, because the source-active guard keeps the structural guarantee and the boring/robust core (Lesson 2 structural-over-discipline) rather than paying a runtime walker. Also rejected: forbidding ALL outgoing edges from inactive nodes (too broad) — the guard targets net-new kill-edge CREATION only, so idempotent re-declaration of an existing edge and the section 4.5.1 resurrection path (an active survivor removing its own outgoing edge) are unaffected.
**Mechanisms:** edges, supersedes, corrects, cycle-violation, source-active
**Scope:** store, substrate, identity
**Context:** V1a parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-16, simplification / spirit-check lens. Gemini Finding 1 showed the active-target-only acyclicity proof is mathematically false. Recorded as supersedes (not corrects) matching the corpus precedent for directionally-right-but-wrong-on-mechanism corrections (the acyclicity-is-structural direction was right; the target-active-alone mechanism was wrong). cycle_violation expands from the O(1) self-edge guard to also cover the inactive-source rejection. Applied to vision V1-D6 (new Source-active requirement paragraph), V1-D13 Multiplicity, and section 5.2.2. Pairs with cutover-single-pass-oldest-first-drops-commit-batch: the cutover's single-pass oldest-to-newest commit auto-satisfies this guard, since a node is always active when it declares its kill-edge (the edge that later kills it commits afterward). Candidate new MI for V1a closeout lessons capture; INVARIANTS.md is Vinga's to edit.
**Supersedes:** kill-edge-acyclicity-via-active-target-not-timestamp


### transcripts-are-the-bounded-non-derivative-exception

**Decided:** Mitos honestly names stripped transcripts as the one bounded exception to the graph-is-purely-derivative claim (option c): a stripped transcript block lives only in the graph and is not rebuildable, which the cutover already accepts as a soft loss cushioned by the timestamped backup; V1a states the carve-out rather than papering over it, with no code change, while persisting stripped transcripts to a user-owned file (option b) is the better-coherence v0.2 and V3a target if transcript loss ever bites.
**Rejected:** Option a, delete the transcript on buffer-absence: cleanest pure-derivative story but destructive, reverses V1-D16 write-once-preserve, and is a P5 Ironclad red flag (an automated irreversible data-loss path), since stripping to save buffer space would silently erase historical synthesis. Option b now, persist stripped transcripts to a user-owned file: full P6/M7 coherence but adds a second primary-source surface plus a sync-time write-on-strip (a V3a ripple), heavier than V1a needs; deferred to v0.2 and V3a as the long-term target.
**Mechanisms:** transcripts, write-once-preserve, derivative-graph, m7
**Scope:** store, substrate, sync
**Context:** V1a vision, Vinga manual round, 2026-06-16, resolving Q8 (the transcript split-brain, P6/M7 coherence). Vinga: option c. Vision reconciliation pending (a one-paragraph carve-out in sections 2.1 and 4.4, plus the section 10 P6/M7 note). Note: Q5=A makes node ids stable across renames, so renames no longer strand transcripts; the corrects-only ancestry rule needs its rationale reworded under A (corrects is now an axiom-fix that keeps the same decision, not a slug-rename), but its conclusion holds; batch reconciliation.
**Cites:** transcript-ancestry-corrects-only


### keep-typed-mutation-edge-catalog-reject-collapse

**Decided:** Mitos keeps its typed mutation-edge catalog (corrects, supersedes, and the V1b amends and narrows as non-kill-edges) rather than collapsing all mutation into a single supersedes, because forcing every change to mint a complete standalone node manufactures axiom-drift on each restatement; the layered-delta amends plus the modifier-stamping seam keep the active view honest without that risk.
**Rejected:** Collapsing the catalog to one supersedes edge (the Saboteur proposal): gives an always-literal active view and drops the transcript-recovery walk, but reverses M1 (which names corrects), is V1a-touching (drops a V1a edge), and forces full re-statement of the whole axiom on every minor change, the exact divergence cost amends exists to avoid; Vinga rejected it on that drift risk. Making amends and narrows kill-edges (Tier 1): same full-restatement cost without the M1 reversal; also rejected in favor of keeping them non-kill (layered deltas) with the modifier-stamping seam as the read-hazard mitigation.
**Mechanisms:** edges, amends, narrows, corrects, supersedes
**Scope:** substrate, store, identity
**Context:** V1a vision, Vinga manual round, 2026-06-16, resolving Q7 (escalated this loop by a Saboteur challenge to collapse the mutation catalog). Vinga: keep as-is; the drift potential of forcing full re-statement is the deciding factor. No vision change (status quo confirmed); the modifier-stamping seam stays the read-hazard mitigation. Because the catalog is kept, Q5 (renames) and Q8 (transcript split-brain) stayed live and were settled separately.
**Cites:** v1a-ships-modifier-stamping-seam-not-reserved-slot


### successor-less-death-via-retired-marker-and-signal

**Decided:** Mitos models successor-less decision death (a decision whose governed feature is deleted, with no replacing node) as first-class retirement via a RETIRED marker backed by a retired signal in the signals table, not a new edge or a sentinel-source or self-edge: the active-view filter excludes nodes carrying a retired signal, state stays computed (M3, surfaced loud like is_drifted), and removing the marker deletes the signal to un-retire (reversible); forward-scoped to V1b and V3a, with V1a only reserving the retired signal_type slot alongside drifted and source_reencounter.
**Rejected:** A retires edge from a global sentinel node: introduces a weird special node and routes retirement through the edge catalog; a self-directed retires edge is forbidden (a self-edge fails the cycle guard). A computed orphan state: requires Mitos to detect that a feature was deleted, which it cannot, since it does not watch the code. Do nothing (decisions are forever): leaves dead-feature decisions reading as live commitments in the active view, polluting the what-is-our-current-architecture query; rejected because P4 requires explaining how a thing dies. Auto-detect plus auto-retire by LLM: a P5 Ironclad trap; the Lifecycle Gardener vision gates all retirement on human confirmation, never autonomous.
**Mechanisms:** retired-signal, retired-marker, signals-table, m3, active-view
**Scope:** store, substrate, identity, sync
**Context:** V1a vision, Vinga manual round, 2026-06-16, resolving Q6. Vinga: yes, model it (if we cannot explain how it dies, do not build it, P4). Mechanism is the RETIRED marker plus a retired signal reusing the signals table. Forward-scoped to V1b (catalog and signal write path) and V3a (deletion UX); V1a only reserves the retired signal_type slot. Pairs with the new ROADMAP Lifecycle Gardener vision.
**Cites:** signals-active-state-composite-pk


### anti-knowledge-embedding-axiom-only-now-vector-later

**Decided:** Mitos keeps embedding_text for a decision axiom-only in V1a and V2 (option A), so the embedding cache stays keyed on the immutable axiom, with a separate anti-knowledge vector over rejected_paths as the V2 target (option C) for first-class anti-knowledge search; V1a exposes the anti_knowledge_text(node) helper shape now (it does no embedding itself) so option C is pure-additive in V2 with no corpus-wide re-embed.
**Rejected:** Option B, folding rejected_paths into the one embedding: makes anti-knowledge searchable but dilutes normal-query recall (one vector serving the decision and its rejections) and re-embeds on every rejected_paths edit; option C delivers the same searchability at identical embedding cost with zero dilution, so C strictly dominates B. Not wiring the anti_knowledge_text helper shape in V1a: would force a corpus-wide re-embed when C ships later, since the embedding_text shape is the cross-vision cache contract (P20 decide-now).
**Mechanisms:** embedding-text, anti-knowledge, rejected-paths, embedding-cache
**Scope:** vector-store, store, retrieval, substrate
**Context:** V1a vision, Vinga manual round, 2026-06-16, resolving Q4. Two yeses: A is the V1a and V2 default (axiom-only embedding_text), C (a separate anti-knowledge vector over rejected_paths) is the V2 target; and yes, wire the anti_knowledge_text(node) helper shape into V1a now. Vision reconciliation (section 5.1 C2) pending.


### drop-computed-state-from-qdrant-payload

**Decided:** Mitos drops the computed state field from the Qdrant vector payload (reversing OPERA section 4.D), so the vector store holds no computed state and cannot drift from the SQLite graph; retrieval recomputes activeness from SQLite per query via a has_id filter (SQLite computes the active scope-filtered id-set and passes it to Qdrant so it ranks only within it, no over-fetch for scoped queries), falling back to over-fetch then SQLite post-filter for broad unscoped queries; the V1a consequence is dropping the first-order kill-edge-target pending_embeddings enqueue, narrowing the V1a Outbox to the committing node only.
**Rejected:** Keeping state in the Qdrant payload (status quo OPERA 4.D): a stored copy of a computed value drifts between a kill-edge flip and the next drain, reintroducing the exact status-drift M3 exists to kill, one layer down. Over-fetch as the sole strategy: passes no id-list but fetches and discards inactives; the has_id mechanism avoids over-fetch for scoped queries while staying M3-pure (the borrowed id-set is computed per query, never stored), so over-fetch is kept only as the unscoped fallback. A richer Outbox cascade to keep the state replica fresh: a large apparatus whose job is maintaining a computed-state copy; dropping the copy deletes the apparatus.
**Mechanisms:** qdrant, computed-state, has-id, pending_embeddings, m3
**Scope:** vector-store, store, substrate, retrieval
**Context:** V1a vision, Vinga manual round, 2026-06-16, resolving Q2 (an OPERA 4.D override). Vinga approved; the over-fetch concern resolved via has_id (her borrow-state-temporarily-and-discard instinct). Requires amending OPERA 4.D and the C2 contract, and dropping the V1a first-order kill-edge-target enqueue; vision reconciliation pending. Whether scope and kind also leave the payload is a V2 detail, not pinned.
**Amends:** flagless-outbox-cache-absorbs-reembed


### slug-removed-from-canonical-core-hash

**Decided:** Mitos removes the human-readable slug from the canonical-core hash (reversing V1-D2 and the prior slug-in-hash reading of M2), so a decision id is SHA-256 over kind plus axiom plus mechanism_refs only and stays stable across slug renames; the slug becomes a mutable readable handle edited in place (no new node on rename), a slug_aliases table preserves historical handles, edge-target resolution consults the active view first and slug_aliases on a miss (live slug always wins over an alias), and renaming onto a slug another active node holds is an explicit collision rule.
**Rejected:** Option C, keep slug-in-hash plus slug_aliases as a fallback resolution tier: fixes the severe citation-rot additively without touching the kernel, but leaves the cosmetic minting-churn (a slug typo-fix still mints a new node plus a permanent corrects tombstone in the human-facing markdown, and an awkward auto-derived slug stays permanent). Vinga chose A to kill the minting-churn too, judging clean in-place renames worth a deliberate kernel amendment. Option B, status quo: leaves both citation-rot and minting-churn, betting renames are rare; rejected once A is taken. The convergence argument (two captures of the same axiom with different slugs now merge) as the MAIN case for A: rejected as the primary driver, because Mitos is one-entry-one-decision with no cross-entry semantic dedup, so convergence is a weak benefit here; the real driver is the rename ergonomics (clean in-place edits, no permanent ugly slug). Keeping the slug identity-constituting on the ground that a citation handle should be a permanent anchor: rejected, because the slug stays citable and aliases preserve old handles, and the editable-handle flexibility is judged worth more than permanent-anchor immutability for this corpus.
**Mechanisms:** content-hash-identity, slug, slug-aliases, canonical-core, m2
**Scope:** identity, schema, substrate, store
**Context:** V1a parser-and-store vision, Vinga's manual round resolving open question Q5 (remove the slug from the canonical-core hash, an M2-kernel reversal), 2026-06-16. Decided with full understanding of M2 after Claude explained it: A preserves M2's actual purpose (content-hash identity, collision-free parallel minting, and convergence, which it improves) while reclassifying the slug from identity-constituting to mutable readable commentary. Blast radius to reconcile: M2 wording in FRAMEWORK.md and MI-2 in INVARIANTS.md (flagged for Vinga, since the kernel and invariant catalogs are hers to edit); V1-D2, V1-D4 (mutable-slug collision handling on rename), V1-D23 (slug_aliases fallback tier), section 8.2 (slug_aliases table; slug moves out of the canonical-core hash columns to mutable commentary), and sections 11 and 12 (canonical JSON form drops slug; trace table updated) in the vision doc (Claude batches these). Consequence: because renames are now in-place edits, the corrects edge no longer models slug renames; corrects narrows to fixing a buggy canonical core (an axiom or mechanism error), which still mints a new node under M1 axiom-immutability. This coheres with Q7 kept-as-is (the typed catalog stays, corrects survives, just narrowed). The slug_casefold derived search key still applies to the now-mutable slug and its aliases; the cutover wipe-and-rebuild still holds, with node ids now stable across slug renames. Amends canonical-core-identity-not-importance (its identity-not-importance principle stands; the slug reclassifies from identity-constituting to mutable commentary).
**Amends:** canonical-core-identity-not-importance


### open-questions-authored-in-separate-questions-md-file

**Decided:** Mitos authors open questions in a separate questions.md buffer at the workspace root rather than mixed into decisions.md, so kind is determined by file (questions.md vs decisions.md) not by field-presence, while open_questions stay first-class graph nodes with the same hashing and cross-kind edges (Derives-From an OQ to its host decision, Resolves a decision to an OQ); the inline parked-marker form and its park-reason argument are deleted, an open question is a standard top-level slug entry carrying a topic and a questions list, and V1a init scaffolds questions.md with its own entry-stream sentinel and canonical sample while format-spec documents both file shapes.
**Rejected:** One-file kind-by-field-presence (refined Option A inside decisions.md — an OQ as a standalone entry discriminated from a decision by which fields it carries): interleaves unsettled questions into the settled-decisions ledger and keeps kind-by-field-presence as a discriminator; a separate questions.md gives a clean settled-ledger vs open-board split and makes kind a function of file (Vinga's call, 2026-06-16). The inline marker-based OQ (a parked marker carrying a topic, plus a questions list): dead on the parser axis — the shipped parser emits a third shape (slug plus a park-reason, no topic) matching neither format-spec nor the vision, and the corpus has zero OQs, so there is no working target to align to; deleting the marker plus its reason argument removes parser complexity outright. Deferring OQ authoring entirely to V1b (Option C): ships an OQ schema/view/goldens with no producer and guts already-designed V1a surface; a separate authored questions.md is cheaper than carrying a producerless schema. Open questions OUT of the graph entirely (a plain markdown TODO list): reverses the ROADMAP v0.1 two-node-kinds (decision plus open_question) commitment and loses the resolve and derives-from edges plus semantic search over open questions, so the separation stays at the authoring-buffer layer only. Deferring the questions.md split until OQ authoring lands (non-destructive with zero OQs): instead wire the file in at V1a init now (P20 wire-the-seam) so V1b and V3a author into it without retrofitting init or format-spec.
**Mechanisms:** questions-md, open-question, format-spec, mitos-init, entry-stream-sentinel
**Scope:** parser, format-spec, setup, substrate, identity
**Context:** V1a parser-and-store vision, Vinga's manual round resolving open question Q1 (the open-question authoring model), 2026-06-16. Walked through with Claude: Vinga chose reading (a) — separate file, OQs still graph nodes — and chose to wire questions.md into V1a init now rather than defer. The store is unchanged: it holds decision and open_question nodes identically regardless of source file (buffer-blind per C1), so this is a parser/authoring-layer choice that does not expand V1a's store work. Reconciles V1-D8, V1-D9, format-spec.md, section 5.2.2, section 1, section 3, section 6.1, and section 8.2 — to be applied to the vision doc in the batch reconciliation. The inline parked marker, the marker-not-yet-supported store code, and the park-reason argument all disappear. Cross-kind edges (Derives-From an OQ to its host decision; Resolves a decision to an OQ) are V1b edge types, warn-deferred in V1a like Cites; also relates to v1b-relationship-fields-warn-defer (the Derives-From warn-defer path).
**Cites:** decisions-md-sentinel-over-regex-scan


### commit-batch-residual-collision-isolates-later-entry

**Decided:** Mitos V1a commit_batch isolates a genuine residual casefold-slug collision (two batch entries resolving to the same casefold(slug) with no kill-edge between them, caught by the post-edge-pass uniqueness assertion) to the later-batch-position colliding entry: a targeted delete of its node plus declared edges, reported as a slug_collision failure item, leaving the earlier entry active, never a batch-abort.
**Rejected:** Batch-aborting the whole set on a residual collision: the v1a-commit-batch-two-pass-primitive contract forbids batch-abort (one malformed entry must never abort the rest), and a collision is no exception, so isolating the later entry keeps the rest committed. Rolling back the EARLIER colliding entry instead of the later one: arbitrary and inconsistent with commit_parsed_entry, where the second/committing entry is the one whose post-mutation assertion fails (V1-D4 case-3); the later-batch-position entry is the batch analogue of that second committer. Treating the collision as a per-entry transaction rollback like commit_parsed_entry: impossible, because commit_batch consciously relaxes per-entry atomicity (the node lands in pass 1, edges in pass 2), so both colliding nodes are already committed when the post-edge-pass assertion fires, leaving no per-entry transaction to roll back, hence the targeted node-plus-edge delete. Leaving it unspecified (status quo): the primitive ADR pins only WHEN the assertion runs (after the edge pass, to avoid false-firing on same-slug supersession) but not WHAT happens on a true fire, leaving the planning Claude and the V6 importer to guess; the collision is reachable via a V6 import of two same-slug prose extractions.
**Mechanisms:** commit_batch, slug-collision, slug-casefold, two-pass-commit
**Scope:** substrate, store, sync, identity
**Context:** V1a parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-15, Saboteur round. Claude finding; Gemini concurred (targeted rollback of the later-position entry is the correct fallback when full batch atomicity is intentionally relaxed). Backstops: the section 2.1 cutover reconstruction-completeness gate (reference_active subset of reconstructed_active) catches a wrongly-dropped active slug and aborts the cutover; V6 surfaces the slug_collision item for the user to disambiguate. Applied to vision section 5.2.9. Refines but does not reverse v1a-commit-batch-two-pass-primitive (pins the residual-collision branch the primitive left open).
**Cites:** v1a-commit-batch-two-pass-primitive


### v1a-commit-batch-two-pass-primitive

**Decided:** Mitos V1a ships the cutover two-pass commit as a named, tested store primitive commit_batch(entries) that upserts every node in pass 1 then reconciles every edge in pass 2, so each intra-batch forward reference (a Supersedes or Corrects target appearing later in the same batch) resolves with no ordering work; the section 2.1 cutover and bulk-rebuild is its first consumer (replacing bespoke inline two-pass) and V6 importer is a likely second. Contract: per-entry failures accumulate and never batch-abort (preserving the section 5.2.2 isolation contract, shared failure envelope with commit_parsed_entry); the V1-D4 / MI-13 casefold-uniqueness assertion runs after the edge pass (not per-node) so same-slug supersession within a batch does not false-fire slug_collision; per-entry atomicity (V1-D10) is consciously relaxed because an entry node lands in pass 1 and its edges in pass 2; the store stays buffer-blind (operates on the passed ParsedEntry list, C1 intact). V3a is OFFERED commit_batch as a fourth batch-ordering option giving forward-reference transparency without reinventing topological sort, but is NOT mandated and may instead keep strict per-entry commit_parsed_entry plus topological-sort or staging or fail-cleanly to preserve per-entry atomicity; commit_parsed_entry itself stays strict and ordering-naive.
**Rejected:** Mandating commit_batch as V3a's batch path: would lock V3a into the two-pass per-entry-atomicity relaxation before its interactive constraints are shaped, foreclosing the fail-cleanly and staging options the cutover ADR leaves to V3a; offering a composable primitive (P1) preserves V3a autonomy (P12 do-not-pre-solve). Leaving the two-pass logic cutover-internal (status quo): the cutover already builds it, but burying it inline forces V3a to reinvent topological-sort batch ordering from scratch for the same forward-ref transparency, a P1 composable-primitive and P20 wire-the-seam miss; the prototype today exposes only commit_parsed_entry and sync.py loops it per-entry (540, 1127), handling zero forward references. A single all-or-nothing batch transaction: would batch-abort the whole sync on one malformed entry, violating the section 5.2.2 per-entry failure isolation contract (cross-vision, V3a and V5 switch on it). Running the V1-D4 / MI-13 casefold-uniqueness assertion per-node during the node pass: same-slug supersession within a batch (a new node plus its Supersedes target both present) shows two actives with no edges yet mid-node-pass and false-fires slug_collision, so the assertion must move to after the edge pass. Making commit_parsed_entry itself batch-aware: the substrate-floor per-entry primitive stays strict and ordering-naive, consistent with the cutover ADR; batch behaviour lives only in the separate commit_batch primitive.
**Mechanisms:** commit_batch, two-pass-commit, sqlite
**Scope:** substrate, store, sync
**Context:** Settled in the V1a vision-loop paired review (Claude + Gemini), 2026-06-15. Gemini raised the commit_batch simplification (expose the cutover two-pass so V3a need not reinvent topological sort); Claude reframed it as a trilemma (forward-ref transparency vs per-entry atomicity V1-D10 vs per-entry isolation section 5.2.2) and proposed offer-not-mandate; full convergence.
**Amends:** mitos-v1a-s-cutover-and-bulk-rebuild-path-commits-entries-in


### graph-store-append-only-nodes-declarative-mirror-edges

**Decided:** The Mitos graph store is append-only for nodes only (a committed node is never purged; retirement is edge-driven via supersedes/corrects), whereas edges are declaratively mirrored to each processed entry per V1-D21 and thus deletable when an edge line is removed from a still-present entry, while the store never acts on whole-entry buffer-absence so the reachability and no-auto-GC invariant stands.
**Rejected:** The prior ADR framing append-only for nodes AND edges (graph-store-append-only-edge-driven-retirement): accurate for nodes and for whole-entry absence, but false for edges, since V1-D21 declarative-mirror reconciliation DELETEs an outgoing edge whose line the author removes from a still-present entry (the section 4.5.1 resurrection path), so edges were never append-only. The over-broad and-edges would mislead V1b declarative reconciler and V3a deletion-reconciliation UX into treating committed edges as immutable. Also rejected: weakening the no-auto-GC-on-absence half, which stands unchanged (P5 Ironclad Invariant; buffer-absence is never a retirement signal). Recorded as supersedes not corrects: per V1-D2 corrects is reserved for slug-renames where the axiom is unchanged, whereas here the axiom moves to the accurate edge-storage statement, matching the corpus precedent where mitos-stores-node-scope-as-a-multi-valued-node-scopes-node-id superseded the directionally-right-but-wrong-on-mechanism nodes-scope-index.
**Mechanisms:** append-only, edges, declarative-mirror, supersedes, corrects, sync-reconciliation
**Scope:** store, substrate, sync, identity
**Context:** V1a parser-and-store vision review, paired round with Gemini, 2026-06-15, Simplification and Spirit-Check lens. Gemini Finding 2: the recorded ADR append-only for nodes and edges directly contradicts V1-D21 (outgoing edges no longer declared are DELETEd). The decision substance (no auto-GC on buffer-absence, edge-driven retirement, reachability invariant) is unchanged; only the edge-storage sub-claim is corrected to declarative-mirror. Vision section 1.1 amended the same round.
**Supersedes:** graph-store-append-only-edge-driven-retirement


### no-inline-derived-cache-reservations-on-nodes

**Decided:** Mitos V1a does not pre-reserve Trace-summary or Vibe-extras render caches as inline columns on the nodes table; derived-cache columns add additively as side-tables when v0.2/v0.3 design their shape, keeping the active-decisions-view full scan of nodes dense.
**Rejected:** Reserving trace_summary_json and vibe_extras_json as inline nullable columns on nodes (the prior section 8.2 'v0.2+ reservation slots, P20' shape). Two reasons. (1) P17 hot-path: the active-decisions view is a full scan of nodes (M3 — no status column, kill-edge anti-join against edges), so bulky inline JSON columns bloat the hottest read in the system 3-5x at 50k scale — the exact pathology transcripts-separate-table hardened away from. (2) These are derived-cache columns (render outputs of the Trace and Vibe retrieval modes), not provenance like source or confirmed_by: there is nothing to backfill (they populate on first use), and adding them later is purely additive via the migration ladder, so they fail the P20 destructive-retrofit test. Reserving them inline is itself the destructive-retrofit-prone shape, because moving an inline column to a side-table later is the wall-break the reservation was meant to avoid.
**Mechanisms:** nodes, trace-summary, vibe-extras
**Scope:** substrate, store, schema, performance
**Cites:** transcripts-separate-table


### transcript-traversal-single-store-primitive

**Decided:** Mitos exposes transcript-ancestry traversal as a single store-owned primitive get_transcript(node_id) returning an optional string (one recursive-CTE or clean-loop implementation), never reimplemented per-consumer; V1a pins this contract plus the corrects-only edge-set rule, but the implementation lands with the transitive-walker machinery (V1b's cascade walker or V5 as the first consumer), not in V1a, which ships no transitive kill-edge walker.
**Rejected:** Each consumer (V5 MCP transcript-fetch, v0.3 Vibe-mode) implementing the corrects-walk itself (the prior section 5.2.3 'V5/v0.3 own the traversal algorithm' wording): two readers hand-rolling the same graph walk would drift and write N+1 queries, and leaking edge-semantics interpretation to consumers violates P1 composable-primitives — the edge-semantics owner must expose the operation, not the raw edges. Also rejected: shipping get_transcript's implementation in V1a — it needs a transitive corrects-walker and has zero V1a consumer (Letter mode never returns transcripts), and V1a deliberately ships no transitive kill-edge walker (1-hop anti-join active view; cascade walking defers to V1b per section 5.2.4; dangling-edge-reports-one-hop-killer already declined a recursive walker in V1a as a Core-vs-Shell violation). The single-primitive contract is pinned now so the implementation lands once, wherever the walker machinery first exists.
**Mechanisms:** get_transcript, recursive-cte
**Scope:** substrate, store, retrieval
**Cites:** transcript-ancestry-corrects-only


### cutover-backs-up-and-reads-old-graph-for-gate

**Decided:** Mitos V1a's prototype-to-V1a cutover backs up graph.sqlite to a timestamped .bak file before the wipe (making the otherwise-irreversible wipe reversible per P5), and the reconstruction-completeness gate reads its reference active-slug set directly from the still-present backed-up old graph as a kill-edge-filtered active set, never from a manual pre-upgrade mitos list text dump.
**Rejected:** The manual pre-upgrade 'mitos list --state active' dump as the gate's reference source: a fragile P3-violating ritual, because an upgrade is often git pull && pip install, which overwrites the prototype CLI before the operator thinks to dump; round-7's 'zero old-schema-reading code' purity was overstated, because the whole cutover path is one-time and transient, never the runtime substrate, so a bounded old-graph read inside it is not forever-dead-weight. Also rejected: dropping the completeness gate in favour of backup-only, which reverses v1a-cutover-wipe-and-rebuild (that ADR conditioned the wipe on the gate and rejected an unconditional wipe). Also rejected: a raw 'SELECT slug FROM nodes' as the reference set, which returns all nodes including legitimately buffer-deleted-but-retained inactive ones (per graph-store-append-only-edge-driven-retirement) and would false-positive the gate; the reference must be the kill-edge-filtered active set.
**Mechanisms:** sqlite, cutover, graph-backup
**Scope:** substrate, store, sync, setup
**Amends:** v1a-cutover-wipe-and-rebuild


### transcript-ancestry-corrects-only

**Decided:** Mitos transcript ancestry traversal (V5 MCP transcript-fetch and v0.3 Vibe-mode, walking from a node that has no transcript of its own to find an ancestral one) follows corrects edges only and never supersedes: it walks corrects transitively and stops at any supersedes, so a superseded node that left no transcript reports none rather than inheriting its predecessor's synthesis.
**Rejected:** Traversing both corrects and supersedes edges (the prior section 5.2.3 contract). corrects leaves the axiom unchanged — only the slug differs in canonical-core per V1-D2 — so the corrected ancestor's synthesis still describes the same decision and inheritance is sound. But supersedes moves the axiom forward (e.g. use-mongo to use-postgres), so the predecessor's transcript is the synthesis of the old, replaced axiom; inheriting it onto the superseder attaches actively-contradicting reasoning as the new decision's provenance — Vibe mode and MCP transcript-fetch would serve the argument for the very thing the decision explicitly killed. The stranding of a superseded transcript is therefore correct, not a recovery target.
**Mechanisms:** transcripts, corrects, supersedes
**Scope:** substrate, store, retrieval, llm-integration
**Context:** Surfaced in V1a vision review (paired round with Gemini, 2026-06-15) as a logical bug in the read-side transcript contract. Section 5.2.3: V5/v0.3 own the traversal algorithm, V1a owns the kill-edge graph it walks, so the edge-set is a V1a-pinned cross-vision contract. Both corrects and supersedes are kill-edges shipping in V1a, but only corrects preserves the axiom, which is what makes transcript inheritance sound. amends/narrows (V1b) stay excluded too — non-kill, both nodes remain active, no stranding.


### config-loader-tomllib-strict-flat-keys

**Decided:** Mitos V1a hardens the existing config.py MitosConfig loader instead of shipping a greenfield one: it replaces the hand-rolled key=value parser that fails silently to defaults with a tomllib-based loader that hard-fails on malformed TOML and on type mismatch (symmetric with OD1 strict parsing), and the v0.1 config.toml schema stays flat snake_case keys (rotation_mode, qdrant_url, and the rest), never nested or dotted.
**Rejected:** Dotted or nested TOML keys (rotation.mode, embedding.cache_max_entries): at only eight v0.1 keys a nested hierarchy adds TOML-table complexity and breaks the prototype's existing flat .mitos/config.toml (rotation_mode, qdrant_url, qdrant_collection) for zero benefit, so flat snake_case is kept and unifies with the ADR-pinned flat qdrant_* surface. Also rejected: keeping the prototype's silent-fallback-to-defaults on any parse error (config.py lines 111-113) — it swallows a corrupt config and is the inverse of OD1 strict-deterministic parsing, so a malformed config must hard-fail loud instead. Also rejected: framing the section 5.2.6 config loader as greenfield — config.py already ships MitosConfig, so V1a hardens it rather than builds it, and the greenfield framing hid the behavior reversal. The tomllib switch does not breach P19: tomllib is stdlib on the 3.13 floor, so it is not a new dependency, though it deliberately replaces the hand-rolled config reader that the project CLAUDE.md cites as the P19 pattern.
**Mechanisms:** tomllib, config-toml, mitos-config, flat-keys
**Scope:** config, setup, substrate, cli
**Context:** V1a parser-and-store vision, round-12 codebase-verification review (Claude finding, Gemini concur). Section 2 archaeology never named config.py, and section 5.2.6 read as a greenfield validating loader while actually reversing the shipped loader's behavior (manual key=val parse to tomllib; silent-fallback to hard-fail) and its key shape (flat to dotted). Gemini's call settled the key shape: keep flat. Applied to vision section 2 (name config.py), section 5.2.6 (reframe as hardens-the-existing-loader, state the reversal, flatten the six dotted keys), section 5.2.7 (flat keys keep init leave-present-config-untouched coherent), and section 8.0 (flatten the catalog rows).
**Cites:** v1a-cutover-wipe-and-rebuild


### mitos-v1a-s-cutover-and-bulk-rebuild-path-commits-entries-in

**Decided:** Mitos V1a's cutover and bulk-rebuild path commits entries in dependency order, oldest-first, by reversing the newest-first decisions.md corpus (equivalently, committing all nodes before resolving any edge), so every supersedes or corrects kill-edge target pre-exists when its edge resolves; the incremental per-entry commit path stays ordering-naive (strict V1-D6 guard, forward references fail) and V3a owns live cross-batch ordering policy.
**Rejected:** Committing the cutover in naive file order (newest-first). decisions.md is authored newest-first, so a superseder entry sits above its older target. Verified in the live corpus: flagless-outbox-cache-absorbs-reembed supersedes outbox-stores-reembed-flag-not-text, which sits far lower and older; mitos-stores-node-scope-as-a-multi-valued-node-scopes-node-id supersedes nodes-scope-index likewise. A file-order commit resolves the Supersedes edge before its target node exists and fails V1-D6 referential integrity with missing_target on every historical supersession, crashing V1a's own day-one cutover before V3a (which owns live batch-ordering) even exists. Also rejected: making the incremental per-entry path batch-aware to solve this. V3a owns live cross-batch ordering for interactive sync via topological sort, staging, or fail-cleanly; the substrate-floor per-entry primitive stays strict and ordering-naive. Also rejected: a created_at arrow-of-time ordering, already foreclosed because created_at is derivative and re-stamped by the rebuild (see kill-edge-acyclicity-via-active-target-not-timestamp).
**Mechanisms:** cutover, bulk-commit, supersedes, corrects, edges
**Scope:** substrate, store, sync, identity
**Context:** V1a parser-and-store vision, paired review round (Gemini fresh-eyes finding, Claude strong concur, grounded in the live corpus). Section 1.1 punted cross-batch forward-reference ordering to V3a, but section 2.1's cutover is V1a-owned and rebuilds from exactly the newest-first corpus, so without an ordering guarantee V1a would crash its own cutover on day one. The round-10 acyclicity ADR already noted decisions.md is newest-first (a rebuild parses the superseder before its target) but used that only to reject a created_at arrow-of-time assertion; this decision connects the same fact to the cutover commit order. Reversal is sound because a newest-first append log is a valid reverse-topological order for kill-edges (a superseder is always authored after its target); a two-pass nodes-then-edges commit is the equivalent order-independent form, and the plan picks. The reconstruction-completeness check already does in-memory batch processing, so dependency-ordering the commit is consistent with it. Applied to vision sections 1.1 and 2.1. Does not change V3a's live batch-ordering policy.
**Cites:** v1a-cutover-wipe-and-rebuild


### v1a-ships-modifier-stamping-seam-not-reserved-slot

**Decided:** Mitos V1a ships the modifier-stamping seam itself, not merely a reserved payload slot: get_modifiers and get_modifiers_map read incoming supersedes and corrects edges and every V1a read surface stamps the modifier keys (superseded_by and corrected_by live; amended_by and narrowed_by reserved for V1b) with a test per read path, even though the stamped set is always empty on V1a's kill-edge-only active view.
**Rejected:** Reserving only the payload slot in V1a and deferring the get_modifiers apparatus plus the per-surface stamping to V1b (when amends and narrows make it non-vacuous). Rejected on two grounds: (a) every-read-surface-stamps-modifiers is a shipped, tested invariant of the prototype (project CLAUDE.md, MODIFIER_EDGE_KEYS plus the test_modifier_surfacing suite), and V1a is a wipe-and-rebuild that must not regress it; (b) P20 wire-while-walls-open: if the seam is deferred, V1b must retrofit the get_modifiers call into every read surface when it ships amends and narrows, breaking walls, whereas shipping the seam in V1a means V1b only extends the MODIFIER_EDGE_KEYS dict with zero read-surface change. The seam is functionally a no-op in V1a since kill-edge targets leave the active view, but it ships for invariant-preservation and additive extensibility.
**Mechanisms:** get-modifiers, modifier-edge-keys, read-surface, supersedes, corrects
**Scope:** store, substrate, identity, llm-integration
**Context:** V1a parser-and-store vision, round-10 review (Claude finding, Gemini concur). The vision previously framed reverse-edge modifier stamping as reserve-the-slot-now-no-op-in-V1a, ambiguous between shipping the seam and deferring it. Pinned: the seam, the per-surface stamp, and the test ship in V1a; the C3 active-view rows and the C4 Letter payload carry the modifier keys (empty in V1a). The amended-axioms-read-as-live trap this closes was found via AX feedback and fixed in the prototype on 2026-06-15. Applied to section 5.1 C3 and C4, the section 1.2 DoD S3 payload shape, and a new section 6.2 cross-cutting constraint.


### kill-edge-acyclicity-via-active-target-not-timestamp

**Decided:** Mitos kill-edge acyclicity is structural via V1-D6: closing a kill-edge cycle requires declaring a kill-edge whose target is an already-inactive node, which the dangling-edge contract rejects at commit, so multi-hop kill-edge cycles are unreachable by construction; cycle_violation narrows to the O(1) self-edge guard source_id not-equal target_id, with no created_at assertion and no cycle walker.
**Rejected:** Gemini's proposed O(1) arrow-of-time assertion source.created_at greater-or-equal target.created_at to guarantee acyclicity. It is unsound for Mitos because created_at is a derivative field, not authored: entries carry no date (round-9 dropped it), created_at is synthesized at commit time, and the section 2.1 cutover wipes and rebuilds the graph so every node is re-stamped. Worse, decisions.md is newest-first, so a rebuild parsing top-to-bottom stamps the superseding node earlier than its target, making the assertion reject legitimate supersedes edges (or be vacuous if timestamps are stamped equal). Also rejected: an application-level cycle walker (recursive CTE or pulling edges into Python) to detect multi-hop cycles, which V1a otherwise never needs since its active-view filter is a 1-hop anti-join and the cascade walker defers to V1b.
**Mechanisms:** edges, supersedes, corrects, dangling-edge, cycle-violation
**Scope:** store, substrate, identity
**Context:** V1a parser-and-store vision, round-10 review (Gemini Finding 3 catch, Claude resolution). Gemini correctly flagged that declaring cycle prevention structural while also enumerating a cycle_violation code is under-specified. The resolution locates the structural witness in V1-D6's active-target-at-commit requirement rather than a timestamp or a walker. Applied to V1-D13 Multiplicity and the section 5.2.2 store-stage cycle_violation code.
**Cites:** dangling-edge-reports-one-hop-killer


### migration-idempotency-via-transactional-version-gate

**Decided:** Mitos migration-ladder idempotent replay is guaranteed structurally by version-gated transactional steps (a step runs only when user_version is below its version, inside a transaction that bumps user_version, and SQLite DDL is transactional so a crash mid-step rolls back DDL and version together) plus IF NOT EXISTS on CREATEs as a cheap backstop; manual column-exists reflection probes and try-except-on-duplicate-resource are not mandated.
**Rejected:** Mandating the full distributed-system idempotency boilerplate from VISION_GUIDANCE (IF NOT EXISTS plus column-exists reflection probes plus try-except on duplicate-resource errors) for every migration step. That lesson targets multi-worker deployments where restart orders shift and DDL may be non-transactional; Mitos is a single-writer local SQLite with a transactional user_version gate, so a step is exactly-once and a crash mid-step rolls back the DDL and the version bump together, leaving no partial application to probe for. The reflection-probe and try-except boilerplate guards against a double-apply that is unreachable here, so it is brittle ceremony. IF NOT EXISTS is kept as a near-free structural backstop (Lesson 2) for a fixture or future-vision CREATE against a table-present but version-wrong DB.
**Mechanisms:** sqlite, pragma-user-version, migration-ladder
**Scope:** substrate, store, schema, sync
**Context:** V1a parser-and-store vision, round-10 review (Gemini Finding 2, Claude synthesis). Refines the idempotency mechanism under migration-ladder-pragma-user-version-only without reversing it: that ADR dropped the schema_version table; this pins how replay stays idempotent. SQLite transactional DDL is the load-bearing fact that makes the boilerplate redundant. Applied to section 5.2.5 contract clause b and the section 9.1 R3 mitigation. The VISION_GUIDANCE cross-project lesson could carry a SQLite transactional-DDL carve-out, deferred to lessons consolidation.
**Cites:** migration-ladder-pragma-user-version-only


### flagless-outbox-cache-absorbs-reembed

**Decided:** The Mitos vector-sync Outbox (pending_embeddings) stores only node_id (plus queued_at and retry_count) with no needs_reembed flag; the V3b drainer always re-derives embedding_text at drain, calls the cache-backed embedding provider, and upserts Qdrant in one unflagged path.
**Rejected:** Carrying a needs_reembed flag (1 = re-embed, 0 = payload-only) per the superseded outbox-stores-reembed-flag-not-text ADR, plus the MI-12 one-dominates-zero UPSERT-collision rule. The flag existed only to let the drainer skip the embedding provider on an unchanged-text re-enqueue, but embedding_text is a pure function of the immutable canonical core, so a re-enqueued existing node_id re-derives the identical string and the v0.1-mandated embedding cache (ROADMAP item 8) returns it at zero token cost; the only real provider calls are new node_ids (cache miss). The flag therefore guarded against a cost the cache already removes, while costing a schema column, the MI-12 dominance rule, and a commit-and-drain branch. This adopts the bare node_id-only row; the re-embed-vs-payload-only distinction the prior ADR preserved is economically moot once the cache is mandatory.
**Mechanisms:** pending_embeddings, embedding-cache, qdrant, m8
**Scope:** store, substrate, vector-store, schema
**Context:** V1a parser-and-store vision, round-10 review (Gemini Finding 1, Claude concur). Reverses the round-5 reshape on the ground that the mandated embedding cache makes the flag redundant. MI-12 collapses to durability-only: the first-order rows commit in the entry transaction and survive a crash, and an UPSERT collision is an idempotent no-op since the row is just node_id. Independent of Q2, which decides whether the kill-edge-target row exists at all; the committing-node row and, pending Q2, the first-order kill-edge-target row both enqueue as bare node_ids. Applied to section 5.1 C2, section 8.2 pending_embeddings, MI-12, and section 10 P15.
**Supersedes:** outbox-stores-reembed-flag-not-text


### v1b-relationship-fields-warn-defer

**Decided:** Mitos V1a recognizes all nine relationship-field names in the parser but the store commits only supersedes and corrects edges and warn-defers the seven V1b edge types (parsed, not committed, logged with a loud deferred-to-V1b notice), so the live dogfood corpus that already authors Cites edges self-parses clean without silent data loss.
**Rejected:** Rejecting a V1b edge field at the store with a hard failure: it would make the live corpus (which already has Cites edges) fail the section 1.2 DoD #3 self-parse on every sync until V1b ships, treating a legitimate authored relationship as an error. Silently dropping unrecognized relationship fields is also rejected (an OD1 silent-data-loss breach). Warn-defer keeps the declaration in the buffer so V1b declarative reconciliation (V1-D21, which knows all nine types) commits it on a later sync.
**Mechanisms:** parser, edges, cites, format-spec
**Scope:** parser, store, substrate, format-spec
**Context:** V1a parser-and-store vision, round-8 codebase-grounding review (Claude finding B, Gemini concur). Verified: parser.py recognizes all nine relationship fields (lines 65-78); format-spec.md lists all nine; five corpus entries already author Cites. V1a ships only supersedes/corrects edges, so the seven V1b types are recognized-and-deferred. Adds a third branch (c, recognized V1b-deferred feature) to the section 6.2 self-parse closeout protocol. Distinct from the PARKED marker (a whole-entry OQ) which stays a hard marker_not_yet_supported rejection pending Q1.


### canonical-core-list-fields-dedup

**Decided:** Mitos dedups the mechanism_refs and questions_raised canonical-core list fields (set semantics, after per-item normalization, before sort and hash) so a duplicate list item cannot mint a divergent content-hash identity.
**Rejected:** Hashing the raw sorted list without dedup (the prior section 11/12 form): an entry authored Mechanisms: sqlite, sqlite would hash to a different canonical core than Mechanisms: sqlite, minting a distinct node id for a semantically identical decision and defeating M2 convergence (two independent captures of the same decision, one with an accidental duplicate, fail to merge). Mechanism refs and questions are semantically sets (M6 mechanism entities), not ordered multisets.
**Mechanisms:** content-hash-identity, mechanism-refs, questions-raised, canonical-core
**Scope:** identity, schema, substrate
**Context:** V1a parser-and-store vision, round-8 review (Claude finding D, Gemini concur). Dedup runs on the post-normalization form so folded duplicates collapse for mechanism_refs (SQLite and sqlite fold to one) while case-preserved questions dedup on their exact form. Affects the section 11 canonical JSON forms, the section 12 list-norm rules, the embedding_text open_question helper (mirrors the deduped list), and the MI-1 golden trace table (a dedup golden row is added in the plan).


### dangling-edge-reports-one-hop-killer

**Decided:** Mitos V1a reports the immediate 1-hop killer of an inactive edge target in the dangling_edge failure, never the transitive chain head, and defers chain-head resolution to V1b where the cascade walker ships.
**Rejected:** Reporting the transitive chain head (the prior V1-D6 wording): finding it requires a recursive walk of supersedes/corrects kill-edges from the inactive target up to the active node, but V1a ships no kill-edge walker (its active-view filter is a 1-hop anti-join) and section 5.2.4 explicitly defers transitive cascade walking to V1b. Building a recursive walker in V1a solely to enrich one error message, before the cascade infrastructure exists, is a Core-vs-Shell violation. The 1-hop killer is a cheap single-edge lookup and is enough to orient the author.
**Mechanisms:** edges, supersedes, corrects, dangling-edge
**Scope:** store, substrate, identity
**Context:** V1a parser-and-store vision, round-8 review (Gemini finding 2, Claude strong concur). V1-D6 originally mandated identifying the chain head on an inactive edge target; that forces a transitive kill-edge walk V1a otherwise never needs and section 5.2.4 defers to V1b. The immediate killer is an indexed single-hop lookup (edges where target_id = inactive AND edge_type in supersedes/corrects, served by edges-incoming-key-index). Changes the cross-vision dangling_edge payload (V3a/V5 relay it) but keeps the code name. Applied to V1-D6 and section 5.2.2.
**Cites:** edges-incoming-key-index


### mitos-stores-node-scope-as-a-multi-valued-node-scopes-node-id

**Decided:** Mitos stores node scope as a multi-valued node_scopes(node_id, scope) join table with scope indexed, not a single nullable nodes.scope column, because scope is authored multi-valued in the corpus and only a join table delivers indexed scope-membership filtering at P11 scale.
**Rejected:** A single nullable nodes.scope column with WHERE scope = ? equality (the prior nodes-scope-index draft) cannot hold the comma-separated multi-scope tags the prototype parser already splits into a list and the dogfood corpus authors everywhere (substrate, store, performance); it would mis-store the joined literal or lose scope data. A JSON-array nodes.scope TEXT column (the prototype shape) is also rejected: SQLite cannot B-tree-index membership inside a JSON array, so it cannot satisfy the indexed-scope-filtering goal nodes-scope-index was recorded for. Application-layer scope filtering is rejected per Lesson 2 (structural over discipline).
**Mechanisms:** sqlite, node-scopes, index, join-table
**Scope:** substrate, store, schema, performance
**Context:** V1a parser-and-store vision, round-8 codebase-grounding review (Claude finding A, Gemini strong concur). Verified against shipped code: parser.py splits Scope on commas into a list of str (lines 208, 459-461); store.py stores scope TEXT as a JSON array (line 165) and the per-scope render iterates the list (562-570); format-spec.md section 1 says comma-separated tags. The round-7 nodes-scope-index ADR was directionally right that scope filtering must be indexed at P11 but wrong on mechanism (it assumed single-valued). node_scopes reconciles inside the V1-D10 per-entry transaction since scope is mutable commentary (V1-D16); a node with no scope has zero rows so the MI-9 empty-string-to-NULL sentinel dissolves. INVARIANTS.md MI-9 needs the matching correction.
**Supersedes:** nodes-scope-index


### nodes-scope-index

**Decided:** Mitos indexes nodes(scope) so the C4 structured-filter Letter query (WHERE scope = ?) and V4's per-scope render are indexed lookups rather than O(N) full scans of the nodes table.
**Rejected:** Leaving scope unindexed (the prior V1a draft named scope-filtering as an operation but was silent on the index, a P20-forbidden silent deferral). At P11/Dyson scale (50k decisions) a scope-filtered active read with no scope index degrades to an O(N) nodes full scan on every V5 MCP scoped surface and every V4 per-scope render, the same pathology the two sibling index pins (slug-casefold-derived-search-key and edges-incoming-key-index) already fixed for the slug lookup and the edge anti-join. An index is transparently addable later, but consumer query plans bind to the indexed shape now and the deferral must be made explicitly (P20), not left silent.
**Mechanisms:** sqlite, index, nodes-table
**Scope:** substrate, store, performance
**Context:** V1a parser-and-store vision, round-7 economics review (Claude's finding 5). Third sibling to the two existing hot-read index pins, slug-casefold-derived-search-key and edges-incoming-key-index.


### signals-active-state-composite-pk

**Decided:** Mitos's signals table is an active-state table keyed by composite primary key (node_id, signal_type, source) with source TEXT NOT NULL DEFAULT empty-string, carrying no surrogate id and no secondary indexes, so exactly one active signal exists per (node, signal_type, source) and drift resolution is DELETE-on-resolve.
**Rejected:** A surrogate-id append-only-log shape (the prior V1a draft). The table is read via the is_drifted EXISTS query (WHERE signal_type = 'drifted'), which only reads correctly against active state, so the claimed "substrate supports either DELETE-on-resolve or a paired drift_resolved event" was false: an append-log leaves a resolved node's drifted row in place and EXISTS stays true forever. Also rejected: a narrower (node_id, signal_type) primary key, which forbids source_reencounter's required one-row-per-(node, source) multiplicity (MI-4). The composite PK serves is_drifted off its node_id-leading prefix AND subsumes that uniqueness, dropping both the old (node_id, signal_type) filter index and the source_reencounter partial-unique index. Drift history, if v0.2 metrics ever need it, is a separate additive channel (P4: drift signals are transient and expire on resolve), not this table.
**Mechanisms:** sqlite, signals-table, primary-key
**Scope:** substrate, store, schema
**Context:** V1a parser-and-store vision, round-7 economics/simplification review. Gemini diagnosed the append-log-shape-vs-active-state-query contradiction; this composite-PK refinement extends his (node_id, signal_type) fix to also serve source_reencounter and subsume two indexes.


### init-refuses-pre-v1a-schema-routes-to-cutover

**Decided:** mitos init detects a pre-V1a prototype graph schema (PRAGMA user_version = 0 on a non-empty graph whose nodes table is non-STRICT or lacks slug_casefold) and refuses with guidance, routing the operator to the one-time wipe-and-rebuild cutover, never ladder-advancing the old schema in place.
**Rejected:** Letting mitos init's normal open-and-ladder-advance path run against the prototype graph: the V1a migration ladder is forward-only and idempotent (CREATE TABLE IF NOT EXISTS), so it would no-op over the existing non-STRICT prototype tables rather than rebuild them, producing a silent hybrid schema (old tables plus whatever the ladder adds) that passes init without error. Also rejected: an ALTER-TABLE in-place migration of the prototype schema — already foreclosed by v1a-cutover-wipe-and-rebuild (the graph is P6-derivative and slug-in-canonical-core re-mints every node id).
**Mechanisms:** mitos-init, pragma-user-version, sqlite, migration-ladder
**Scope:** substrate, store, setup, sync
**Context:** V1a parser-and-store vision, round 6 (Claude finding). Reconciles a collision between §2.1 (cutover = wipe-and-rebuild) and §5.2.7 (init re-run idempotency, which said a present graph.sqlite is opened-and-ladder-advanced). The cutover is a distinct one-time operation (a documented operator step or an explicit init --rebuild surface); the idempotent init re-run refuses-and-routes when it detects the pre-V1a schema, rather than silently producing a Frankenstein schema.
**Cites:** v1a-cutover-wipe-and-rebuild


### decisions-md-sentinel-over-regex-scan

**Decided:** The Mitos decisions.md parser locates the preamble/entry boundary using an explicit BEGIN-ENTRIES HTML-comment sentinel (everything up to and including it is preamble; entries are the ## or ### slug blocks after it), never by scanning for the first entry-header regex.
**Rejected:** A sentinel-less header-regex scan (treat everything before the first ## or ### slug header as preamble): the canonical entry header is slug-only (### slug, no date), so the in-preamble auto-healing canonical sample (### example-slug) is structurally identical to a real entry and the regex would parse the sample as a real decision. Also rejected: deleting the inline sample from decisions.md to make regex-scan safe, which contradicts opera §4.A's auto-healing-sample-block-at-top-of-file commitment (an opera change, not a clean parser simplification); and fencing the sample to dodge the regex, which reintroduces the Markdown-context-aware (fence/indent) parsing trap the sentinel was introduced in round 1 to avoid.
**Mechanisms:** decisions-md, begin-entries-sentinel, format-spec, parser
**Scope:** parser, substrate, format-spec
**Context:** V1a parser-and-store vision, round 6 (Gemini Finding 3 simplification plus Claude shipped-format grounding). Header form confirmed against format-spec.md §1 (### or ## plus slug) and the live decisions.md. The sentinel is load-bearing because the slug-only header is not self-distinguishing from the sample; absent sentinel the parser hard-fails loudly and mitos init re-heals the sentinel plus sample block. Reaffirms with grounding the round-1 choice of a sentinel over Markdown-context-aware comment-skipping. C5 surface: V5 skill content and V6 importer must honor the preamble/sentinel convention.


### outbox-stores-reembed-flag-not-text

**Decided:** The Mitos vector-sync Outbox (pending_embeddings) stores a needs_reembed flag per node, never the embedding text; the V3b drainer re-derives the text at drain from current node state via the embedding_text(node) helper, because embedding_text is a pure function of the immutable canonical core.
**Rejected:** Storing the derived embedding_text in the Outbox row (the prototype pending_embeddings.embedding_text NOT NULL column, and the nullable-text-as-flag variant the V1a draft proposed): it duplicates primary state already derivable from the node (an M8 violation), and a node that changes between enqueue and drain leaves the queued text stale versus the live graph (a TOCTOU race). A bare is-pending boolean is also rejected: it loses the re-embed versus payload-only distinction (a scope edit needs payload-only; a new node needs re-embed; a flag that re-embeds both wastes embeddings, P15). Moving the flag, retry_count, and queued_at onto the nodes table as columns is rejected too: it fights transcripts-separate-table by bloating the hot active-view full scan.
**Mechanisms:** pending_embeddings, needs-reembed, embedding-text, qdrant, m8
**Scope:** store, substrate, vector-store, schema
**Context:** V1a parser-and-store vision, round 5 (Gemini simplification finding plus Claude M8 grounding). embedding_text(node) for a decision is the normalized axiom and for an open_question is topic plus questions, all immutable canonical core, so the string can never change for a node_id and re-deriving at drain always yields the freshest correct text. The flag still distinguishes re-embed (needs_reembed=1, new node) from payload-only (needs_reembed=0, a scope edit or a first-order kill-edge-target view-flip). The separate single-node table is retained per the transcripts-separate-table cold-path logic. Independent of the open Q2 (drop computed state from the Qdrant payload), which only governs whether the kill-edge-target payload-only row exists at all. On UPSERT collision needs_reembed=1 dominates 0 (MI-12).
**Cites:** transcripts-separate-table


### migration-ladder-pragma-user-version-only

**Decided:** Mitos cross-vision schema-migration ladder tracks version solely via PRAGMA user_version (atomic, stored in SQLite header, transactional) with no schema_version audit table; each step is keyed by a monotonically increasing integer, runs in a transaction, and sets user_version on success, so idempotent full-ladder replay on an at-head database is a no-op.
**Rejected:** A schema_version(version, applied_at) audit table alongside user_version — redundant for a single-user local DB; a migration-history timestamp log is over-engineering, and P11 (additive) means it can be added later if a future vision ever needs the trail.
**Mechanisms:** sqlite
**Scope:** substrate, store, schema
**Context:** Forward-only ladder (no down/rollback in v0.1); V1b/V2/V3b/V6 extend it forward. user_version alone suffices for monotonic forward steps plus idempotent replay (MI-3). Converged with Gemini in vision round 4.


### v1a-cutover-wipe-and-rebuild

**Decided:** Mitos V1a is a hardening rebuild of the existing in-repo prototype substrate, and its cutover wipes the derivative SQLite graph and Qdrant collection and rebuilds by re-parsing the decisions.md corpus plus archives under the hardened parser and hashing (never an ALTER-TABLE migration of the prototype schema), gated behind a reconstruction-completeness check that aborts if any active decision exists only in the graph.
**Rejected:** ALTER-TABLE migration of the prototype schema (over-engineering for a P6-derivative store, and V1-D2 slug-in-canonical-core re-mints every node id anyway); treating V1a as greenfield (false — the full prototype ships in-repo, and that framing hid both the cutover and the re-hash); an unconditional wipe (would silently drop graph-only state: stripped transcripts, confirmation metadata, archive-only entries).
**Mechanisms:** sqlite, qdrant
**Scope:** substrate, store, sync, identity
**Context:** Vinga confirmed 2026-06-15: wiping the derivative graph is fine as long as the actual ADRs are reconstructible from the corpus. Re-hash is safe because edges are slug-declared in the buffer and re-resolve against new ids. The graph-store-append-only-edge-driven-retirement reachability invariant (every active node present in buffer, archives, or a buffer-present kill-edge) IS the pre-wipe completeness check. Stripped DECISION_TRANSCRIPT provenance is an accepted soft loss. Invalidates if the live graph holds active decisions not reconstructible from decisions.md plus archives, which would force a preserving dump-and-restore instead of a clean wipe. Converged with Gemini in vision round 4.
**Cites:** graph-store-append-only-nodes-declarative-mirror-edges


### edge-catalog-reference-table

**Decided:** The edge-type whitelist and the (source_kind, target_kind, edge_type) kind-matrix are enforced at the SQLite substrate via an edge_type_matrix reference table — the triple as primary key plus a composite foreign key from edges — not a table-level CHECK constraint, so extending the edge catalog is a data-level INSERT and never a structural edges rebuild.
**Rejected:** A table-level CHECK matrix on edges. SQLite cannot ALTER a CHECK constraint, so every catalog extension (V1b adds 7 edge types beyond V1a's supersedes and corrects, and more may follow) would force a full twelve-step edges table rebuild — copy all rows, recreate every index and composite foreign key — a P11/P20 "expensive later" migration on a core structural table that grows across visions. Application-layer (Python) enforcement is also rejected per Lesson 2 (structural over discipline): the kind-matrix must be substrate-enforced.
**Mechanisms:** edges, edge-type-matrix, foreign-key, sqlite
**Scope:** substrate, store, schema, identity
**Context:** V1a parser-and-store vision, round 3 (Claude finding, Gemini concurrence). The edge catalog is extended by every future vision; a CHECK-matrix makes each extension a structural rebuild, while a reference table makes it INSERT-only forever (Cathedral Mindset P19 plus Dyson Horizon P11). Companion to edges-incoming-key-index — same edges table, same additive-durability logic. The exact composite-foreign-key DDL stays plan-internal; the contract is that catalog extension is data-level, never a rebuild. V1-D13 and the section 8.2 schema (new edge_type_matrix row) carry the vision-side pin.
**Cites:** edges-incoming-key-index


### graph-store-append-only-edge-driven-retirement

**Decided:** The Mitos graph store is append-only for nodes and edges and retirement is edge-driven only (supersedes and corrects); deleting an entry from decisions.md is never an auto-GC or retirement signal at the store layer, and incremental-sync-versus-rebuild coherence is held by the invariant that every active node is reachable from the current markdown corpus — present in the buffer, in an archive file, or killed by a kill-edge present in the buffer — enforced by V3a sync reconciliation.
**Rejected:** Auto-GC of graph nodes on buffer absence, or a hard sync failure on a buffer-absent active node. Both violate P5's Ironclad Invariant: run the cascade test — a parse glitch or a botched buffer edit that makes entries transiently absent would mass-retire and mass-corrupt the graph irreversibly. The store must freeze/keep, never advance on ambiguity. Also rejected: treating a raw entry deletion as equivalent to supersession or correction. Deletion is ambiguous (archival versus mistake versus retire), so retirement must be an explicit kill-edge or an explicit V3a reconciliation choice, never inferred from absence.
**Mechanisms:** append-only, supersedes, corrects, sync-reconciliation
**Scope:** store, substrate, sync, identity
**Context:** V1a parser-and-store vision, round 3 (Gemini fresh-eyes finding). Surfaced as a P6 coherence question: incremental sync (append-only, keeps a deleted entry's node) and a from-scratch rebuild (never re-mints it) diverge for genuinely-deleted-never-archived entries. The resolution keeps the store append-only (P5 Ironclad — no destructive auto-GC) and names the corpus-reachability coherence invariant, assigning enforcement to V3a's sync reconciliation (prompt on a buffer-absent, un-archived, un-killed active node — archive, retire via edge, or re-insert). V1a has no buffer awareness (C1); it exposes the active_decisions view that makes the reconciliation diff possible but does not own the policy. Vision section 1.1 was reframed to stop over-claiming the deletion policy at the store layer.


### canonical-core-identity-not-importance

**Decided:** Canonical-core membership (which fields enter a node's content-hash identity) is decided by IDENTITY — does changing this field change which decision this is — not by IMPORTANCE, so a required field such as rejected_paths lives outside the canonical core as mutable-in-place commentary; required and identity-constituting are orthogonal axes.
**Rejected:** Reading Framework M5's "rejected_paths is part of the decision, not commentary on it" as placing rejected_paths inside the canonical-core hash or making it immutable. That conflates importance with identity, would mint a new node on every rejected_paths refinement (breaking M1's mutable-commentary surface and V3a's interactive edit-in-place loop), and contradicts M1's own listing of rejected_paths among the mutable commentary fields. Also rejected: treating "required" and "canonical-core" as the same property. They are orthogonal — rejected_paths is required-but-mutable, invalidates_if is optional-and-mutable, axiom and slug are required-and-identity-constituting.
**Mechanisms:** content-hash-identity, rejected-paths, canonical-core
**Scope:** identity, schema, substrate
**Context:** V1a parser-and-store vision, round 3 (Gemini fresh-eyes finding). Resolves an apparent Framework contradiction: M1 lists rejected_paths among mutable commentary fields while M5 says it is "not commentary on it." The two use "commentary" in different registers — M1 is a storage class (outside the identity hash, therefore mutable in place); M5 is an importance class (required, load-bearing, not an afterthought). No real conflict: the vision already requires rejected_paths (the parser rejects a decision lacking it, V1-D8) AND updates it in place on a canonical-core hash match (V1-D16). The general test "identity, not importance" governs every future canonical-core membership question. Flagged to Vinga that FRAMEWORK M5's prose could carry a one-clause parenthetical.


### edges-incoming-key-index

**Decided:** Mitos indexes edges(target_id, edge_type) so the active-decisions view incoming-kill-edge anti-join, the V1-D4 slug-collision assertion, and V1-D6 and V1-D23 active-target resolution are indexed exact-matches rather than an O(N) scan of edges.
**Rejected:** Relying on the edges primary key (source_id, target_id, edge_type), which is source_id-leading and serves only V1-D21 outgoing-edge reconciliation rather than the incoming filter target_id equals X and edge_type in (supersedes, corrects); or expecting the composite foreign key to auto-create the index. SQLite does NOT auto-index foreign-key child columns, so the active-view filter (V4 on every render, V5 on every MCP surface) degrades to a full scan of edges, an O(N) P11 / Dyson violation at 10k-plus edges.
**Mechanisms:** sqlite, edges, index
**Scope:** substrate, store, performance, identity
**Context:** V1a vision round. Companion to slug-casefold-derived-search-key: that ADR fixed the slug-lookup half of the active-view hot read; this fixes the incoming-edge anti-join half. See section 8.2 edges row and V1-D23.
**Cites:** slug-casefold-derived-search-key


### transcripts-separate-table

**Decided:** Mitos keeps DECISION_TRANSCRIPT blocks in a separate 1:1 transcripts table keyed by node_id, never as an inline nullable TEXT column on nodes, so the active-decisions view full scan of nodes stays dense.
**Rejected:** Inlining transcript as a nullable TEXT column on nodes relying on SQLite overflow pages: overflow spills only the bytes past the local-payload limit (about 500-1000B on a 4KB page), so every transcript-bearing row keeps roughly a 1KB inline prefix on the nodes leaf pages that the active-view full scan (M3: no status column, anti-join against edges) must read, bloating the hottest read in the system about 3-5x once transcripts accumulate at P11 / 50k scale. It also trades a structural cold/hot partition for column-order plus never-SELECT-star discipline (Lesson 2 regression) and saves no atomicity, since the per-entry commit already spans edges and signals.
**Mechanisms:** sqlite, transcripts
**Scope:** substrate, store, schema, performance
**Context:** V1a vision round (parser-and-store v1a). Gemini proposed collapsing transcripts into nodes via SQLite overflow; declined on scan-density grounds (P17 cold-path-off-hot-path). See V1-D16 and the section 8.2 transcripts footnote.


### slug-casefold-derived-search-key

**Decided:** Mitos stores a derived, indexed slug_casefold column (the slug run through Python str.casefold() at write time) on nodes and performs casefolded slug lookups — V1-D23 edge-target resolution and the V1-D4 active-slug collision assertion — as indexed exact-matches on it, never by pulling the active node set into Python.
**Rejected:** Python-side casefold over the active set pulled into memory — O(N) per edge resolution and per commit; violates P11 (Dyson Horizon) at 10k+ active decisions, so the original V1a draft's "cheap at v0.1 scale" rationale does not survive scale. SQLite COLLATE NOCASE / LOWER() directly on the slug column — ASCII-only by spec; silently fails to match Lithuanian (kabutė↔KABUTĖ) and CJK case-pairs, leaving non-ASCII slugs unciteable while passing ASCII-only fixtures. A partial UNIQUE index over casefold(slug) on active nodes — structurally impossible under M3 (no "active" column; partial indexes cannot join the edges table).
**Mechanisms:** sqlite, slug-casefold, str-casefold, content-hash-identity
**Scope:** substrate, identity, store
**Context:** Settled in V1a (parser-and-store) vision review, Gemini + Claude paired round. V1-D4/V1-D23 originally mandated Python-memory casefold comparison over the active set, justified as "cheap at v0.1 scale." The slug_casefold column keeps Unicode correctness (casefold computed in Python at write time, not SQLite's ASCII-only NOCASE/LOWER) while making the hot lookups indexed O(1) in the engine. slug stays case-preserved for the canonical-core hash (M2); slug_casefold is derived, non-unique, indexed. Active-slug uniqueness (MI-13) remains a post-mutation application-layer assertion because M3 forbids DDL uniqueness over a computed view, but its read is now an indexed query. General pattern: store a Python-normalized search key as a derived indexed column for exact-match DB lookups whenever the normalization (Unicode casefold/NFC) exceeds the substrate's native ASCII-only collation.


### mitos-status-and-per-project-setup

**Decided:** Mitos ships a 'mitos status [path]' command (exit 0 ready / 1 not, plus --json) and a per-project SETUP.md walkthrough, so any human or LLM can check whether Mitos is set up for a project and follow a clear path to set it up; the setup skill also adds a SETUP.md pointer to the project's agent files (AGENTS.md/CLAUDE.md/GEMINI.md) noting it is per-project.
**Rejected:** Make the check an MCP tool only — the MCP server only runs once Mitos is already wired, but the whole point is checking projects where it is NOT, so a CLI command works regardless. Leave setup discovery to prose docs alone — humans and LLMs must find the right doc and guess the steps.
**Mechanisms:** cli, mcp
**Scope:** setup, cli, llm-integration
**Context:** Mitos is per-project; onboarding a new project needs init + key + Qdrant + MCP wiring. 'mitos status' makes that state machine-checkable; SETUP.md + a setup skill make the walkthrough findable; the AGENTS.md pointer propagates Mitos-awareness to every agent that later opens the project.


### init-scaffolds-gitignored-env

**Decided:** mitos init scaffolds a gitignored .env at the workspace root with explicit empty credential slots (GEMINI_API_KEY required; ANTHROPIC_API_KEY optional), and the CLI auto-loads .env, so credential setup is unambiguous for any human or LLM.
**Rejected:** Leave credential discovery to docs only — humans and LLMs must guess env-var names and where to put them. Commit a non-gitignored .env template — risks leaking real keys once filled.
**Mechanisms:** dotenv, gemini-api, anthropic-api
**Scope:** setup, config, llm-integration
**Context:** Mitos reads keys straight from os.environ; previously the CLI loaded no .env (only tests did) and init created none, so a setting-up human/LLM had no signposted place for keys. init now scaffolds + gitignores .env and the CLI loads it on startup (dependency-free, P19).


### mitos-dedicated-qdrant-port

**Decided:** mitos defaults its Qdrant endpoint to a dedicated port (http://localhost:7333) and ships a docker-compose for it, so mitos never co-locates its collections inside whatever Qdrant the user already runs on the standard :6333; QDRANT_URL still overrides.
**Rejected:** Default to the standard :6333 — fails dangerous: it silently joins the user's existing general-purpose Qdrant (the standard port is the MOST-likely-occupied one) and shares its wipe/contamination risk. 'Keep :6333 because it is standard' is backwards: standard = most-likely-occupied = highest risk.
**Mechanisms:** qdrant, docker-compose
**Scope:** vector-store, config, setup
**Context:** Per-project collections prevent mitos-internal mixing but do NOT protect against landing in a DIFFERENT app's Qdrant on :6333. A mitos-dedicated :7333 fails safe (semantic degrades if mitos's Qdrant is down) instead of dangerous. v0.2 sqlite-vec removes the separate-Qdrant need entirely.


### per-project-qdrant-collection

**Decided:** Each Mitos workspace derives its own Qdrant collection name (mitos-<project>) by default, so a single shared Qdrant instance never mixes decisions across projects.
**Rejected:** Shared single 'mitos' collection across all projects — causes cross-project semantic contamination and content-hash point_id collisions when two projects record the same axiom. Separate Qdrant instance per project — operational overhead with no benefit, since collections already isolate within one instance.
**Mechanisms:** qdrant, content-hash-identity
**Scope:** vector-store, config
**Context:** config.qdrant_collection defaulted to a hardcoded 'mitos' and 'mitos init' wrote the same literal into every config.toml; point ids are hash_to_uuid(content_hash) per M2, so identical axioms across projects would collide. The fix derives the collection from the workspace basename, overridable via .mitos/config.toml.


### identity-rebuilt-in-3a-compute-hash-flip-deferred-to-5a-8a

**Decided:** Mitos V1a's slug-free canonical-core SHA-256 identity — mitos/identity.py::compute_node_id plus the five section-12 normalization rules and the section-11 golden trace table — is authored and golden-proven in Phase 3a but is NOT wired into any live caller; the prototype slug-inclusive store.compute_hash stays byte-for-byte intact and live, so the test suite stays 100% green through phases 3a-4b. The commit-path flip (pointing commit_parsed_entry at compute_node_id, keyword-only and slug-less) defers to Phase 5a in lockstep with the schema boot-flip and the commit_parsed_entry rebuild; the consumer reconciliation and the retirement of store.compute_hash (the sync.py and importer.py call sites plus the two test_adversarial_invariants recompute-and-assert sites) defer to Phase 8a.
**Rejected:** A back-compat shim keeping the compute_hash name with the old signature — impossible, the old output is slug-inclusive and one call cannot emit both the slug-inclusive and the slug-free form. Flipping the hash in 3a (changing compute_hash plus every caller plus every golden test now) — gains nothing because the new hash is equally proven by 3a's pure goldens either way, and pays six phases (3a-8a) of red CI because test_adversarial_invariants:343/379 recompute and assert the slug-inclusive id against the live commit path, the exact contained red window this deferral exists to prevent.
**Mechanisms:** compute-node-id, compute-hash, content-hash-identity, canonical-core
**Scope:** identity, substrate
**Context:** V1a parser-and-store vision, Phase 3a implementation, 2026-06-18. Direct analog of v1a-schema-built-in-2b-live-flip-deferred-to-5a (the schema construction-vs-migration containment). Tracked in WIRING_LEDGER entry-002 (owning_phase 5a, with an 8a consumer/retirement tail) and pinned live by a deferred-flip tripwire test tests/test_store.py::test_identity_hash_still_slug_inclusive_until_phase_5a that asserts the current slug-inclusive reality and must be consciously updated when 5a flips the commit path. Slug-free convergence is proven structurally at the unit level (the section-11 trace table slug-convergence rows) and end-to-end by 5a's commit path.
**Cites:** v1a-schema-built-in-2b-live-flip-deferred-to-5a


### mechanism-canonical-norm-ascii-punct-ws-fold-to-hyphen

**Decided:** Mitos's mechanism_canonical_norm folds a single mechanism token by NFC-normalizing, then casefolding, then collapsing every maximal run of ASCII punctuation OR ASCII whitespace to a single hyphen and stripping leading and trailing hyphens — so str_casefold, str-casefold and Str Casefold all converge to str-casefold, SQLite! and sqlite to sqlite, while node-scopes stays node-scopes. The fold is ASCII-only: non-ASCII characters (CJK, accented letters, Unicode punctuation, a no-break space) pass through NFC and casefold untouched, so two distinct non-ASCII tokens never collide.
**Rejected:** Folding only punctuation and leaving whitespace intact — splits Str Casefold from str_casefold, defeating convergence on one identifier authored with different separators. A bare regex backslash-s (Unicode whitespace, without the re.ASCII flag) — folds a no-break space the same as an ASCII space, contradicting the ASCII-only spec and risking cross-corpus divergence on non-ASCII tokens. NFKC instead of NFC — over-normalizes compatibility characters (ligatures, full-width forms) and is not the pinned form.
**Mechanisms:** mechanism-canonical-norm, mechanism-refs, content-hash-identity, canonical-core
**Scope:** identity, substrate
**Context:** V1a parser-and-store vision, Phase 3a implementation, 2026-06-18. The mechanism_canonical_norm name was already established and cited (node-scopes-casefold-scope-tags, canonical-core-list-fields-dedup) but no prior Decided clause pinned the precise byte form. This ADR pins it because V1b's mechanisms.canonical_name PK reuses it byte-for-byte, a cross-vision contract flagged for Gemini's review. The section-11 trace-table rows D8 (str_casefold / str-casefold / Str Casefold to str-casefold) and D9 (sqlite / SQLite / sqlite! to sqlite) lock it; implemented as a precompiled re-pattern over ASCII whitespace and ASCII punctuation, substituting a single hyphen then stripping leading and trailing hyphens, applied after NFC normalization and casefold.
**Cites:** canonical-core-list-fields-dedup



### config-loader-rotation-mode-enum-hard-fail

**Decided:** The V1a strict config loader hard-fails (raises ConfigError) on a rotation_mode value that is the correct type (str) but outside the {archive, mark, prune} enum — a third failure mode beyond the malformed-TOML and type-mismatch cases already pinned, and a deliberate behavior reversal of the prototype's set_attribute, which silently kept the old value on an off-enum rotation_mode. This extends OD1-symmetric loudness to invalid VALUES, not just invalid types: a typo'd rotation_mode = "achive" must speak, not quietly fall through to "archive" and then archive when the author may have meant "mark".
**Rejected:** Warn-and-default on an off-enum value (treat section 5.2.6's "type mismatch" wording as the exhaustive failure set, log a calm line and keep the default): rejected because rotation_mode steers a destructive action (archive/mark/prune of decision entries), so a silently-coerced wrong mode lands data in the wrong rotation regime — exactly the silent-shrug OD1 forbids; a value the author clearly intended but mistyped is louder-is-safer territory. Also rejected: the prototype's silent-ignore (set_attribute's `if val in (...)` kept the previous value with no signal), the original bug this hardening kills.
**Mechanisms:** rotation_mode, ConfigError, ROTATION_MODES, mitos-config
**Scope:** config, substrate, cli
**Context:** V1a parser-and-store vision, Phase 6a implementation, 2026-06-18. The parent ADR config-loader-tomllib-strict-flat-keys pinned hard-fail on malformed TOML + type mismatch but did not address a valid-type/invalid-value enum; the prototype handled it by silent-ignore in set_attribute (now retired). Plan section 4 Decision 4 flagged folding the enum hard-fail into the loader ADR; recorded as a focused build-on rather than mutating the parent's canonical core (M1). Implemented in config.py as a post-type-check membership test against ROTATION_MODES raising ConfigError, pinned by tests/test_config.py::test_rotation_mode_out_of_enum_raises_config_error.
**Cites:** config-loader-tomllib-strict-flat-keys


### same-sync-handle-handoff-surfaces-collision-not-batch-ordering

**Decided:** When a same-sync cross-node slug-handle handoff (node A relinquishes handle foo while node B claims foo in the same sync) hits an eager UNIQUE(handle_casefold) collision because V1a commits atomically per entry, Mitos surfaces the ordinary loud slug_collision P3 vector error and lets the handoff converge on re-sync, and adds NO cross-entry batch-ordering machinery (no topological sort, no two-phase all-DELETEs-then-all-INSERTs pass) to make the handoff collision-free inside a single sync.
**Rejected:** Mandating the handoff MUST NOT raise and delegating a within-sync DELETE-before-INSERT ordering to the plan (the parent slug-alias-union-strict-casefold-uniqueness's reconciliation clause): rejected because V1a's commit boundary is atomic-per-entry (confirmed in the shipped code — sync.py commits "atomically per entry" and commit_parsed_entry wraps each entry in a single `with conn:` transaction), so A's relinquishing DELETE and B's reclaiming INSERT live in different transactions and cannot be ordered within one sync without either (a) a cross-entry topological sort of the batch or (b) a two-phase all-DELETEs-then-all-INSERTs pass — and (b) breaks per-entry atomicity and the MI-12 crash-safety resting on it. Both are a substrate complexity spike to hide a rare cross-node rename. Letting the collision surface is OD1-safe (loud, never a silent mis-resolution), P5-safe (A's relinquish never collides, so once it lands foo is free and B succeeds on the next sync — at most two syncs regardless of processing order, never a permanent wedge), and P3-honest (slug_collision names the colliding handle and its current holder). P19 (fewest moving parts) settles it against the ordering machinery.
**Mechanisms:** node-handles, slug-collision, slug-casefold, per-entry-transaction, mi-12
**Scope:** identity, store, substrate, sync
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-18, Gemini's Finding 1 (Archaeology/Simplification lens: the MUST-NOT-raise mandate forces either a topological sort or a transaction-boundary break against V1a's established per-entry commit model — a complexity spike for a rare cross-node handoff). Claude verified the per-entry-atomic commit boundary against the shipped code (sync.py "Commit … atomically per entry"; commit_parsed_entry's per-entry `with conn:`) and both converged on relaxing the mandate. Amends slug-alias-union-strict-casefold-uniqueness — relaxes ONLY its same-sync-handoff reconciliation clause (the DELETE-before-INSERT ordering requirement), leaving the union-uniqueness invariant, the loud slug_collision raise, and the V1b-owns-invariant / V3a-owns-disposition altitude split untouched. A Claude+Gemini loop ADR, not one of Vinga's manual-round calls, so recorded as an amending ADR rather than escalated. Recorded directly in decisions.md because the dogfood graph predates V1a and awaits cutover, so the buffer is the only writable surface this round; the amend edge reconciles on rebuild. Applied to vision section 4.1.
**Amends:** slug-alias-union-strict-casefold-uniqueness


### oq-resolution-source-liveness-holds-against-event-completion

**Decided:** Mitos OQ Stage-2 resolution keeps the still-active-source requirement — an open question is resolved only by an incoming resolves edge from a still-active decision — and rejects the source-liveness-independent event-completion model (any resolves edge resolves the OQ permanently, never reverting when the resolver is superseded), because the decisive ground is P5-Ironclad fail-direction: a re-surfaced OQ on resolver supersession fails safe, loud, and recoverable, whereas an OQ kept silently resolved by a decision that later abandoned its topic is the wrongful-advance direction; the genuine underlying choice — whether an open_question models a point-in-time knowledge gap (closed once bridged) or a standing concern (resolved only while live coverage exists) — is a product-semantics call escalated to Vinga, and absent her ruling the framework default holds the active-source requirement.
**Rejected:** Source-liveness-independent event-completion (Gemini's F6, 2026-06-20: an OQ with ANY incoming resolves edge is resolved permanently regardless of whether the resolving decision is still active; re-open only by removing the Resolves: line for a live resolver, or by authoring a new question for an archived one): rejected in-loop. Its genuinely-new point over the already-rejected transitive-liveness form — that it invents no edge, so the M3 "singles out resolves for inheritance through the supersedes chain" objection does not apply — is correct but defeats only one of the parent ADR's three counter-arguments. The decisive one survives and lands harder: under event-completion, if D2 resolves OQ-X and a later decision pivots away from OQ-X's topic, OQ-X stays silently resolved by a decision that abandoned it — the exact wrongful-advance P5-Ironclad and P3 forbid; and it is worse than transitive-liveness, which at least reverts to parked on successor-less resolver death whereas event-completion never reverts on supersession at all. The P11 "infinite Resolves: carry-forward bloat" rebuttal does not outweigh the Ironclad fail-direction: the accumulation is O(open-questions a lineage actually resolves) — small and chain-bounded (P11 bounds amendment chains) — not O(lineage length), a bounded annoyance rather than a Dyson-scale explosion. Also re-rejected (reaffirming the parent): transitive-liveness through the supersedes chain.
**Mechanisms:** resolves, oq-state-view, supersedes, oq-stage2
**Scope:** substrate, store, retrieval, sync
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-20. Gemini's fresh-eyes pass re-raised (as "F6") the OQ-resolution reversal he himself proposed on 2026-06-18 (recorded-and-rejected as oq-stage2-resolution-stays-explicit-edge's "Finding 1", transitive-liveness) — this time in the stronger source-liveness-independent event-completion form. Claude initially agreed via an (A) transitive-inference vs (B) event-completion distinction, then surface_decisions returned the parent ADR; on re-examination the parent's decisive P5-Ironclad fail-direction ground applies to (B) too (and harder), so Claude retracted in-loop and Gemini concurred ("the fail-safe direction correctly overrides my bloat concern under the framework's rules"). The mitos anti-knowledge system caught a re-proposal of foreclosed ground — exactly its purpose. Amends (does not supersede) oq-stage2-resolution-stays-explicit-edge: the parent stays active and correct; this widens its rejected-paths from transitive-liveness-only to also foreclose event-completion, so a third fresh-eyes round closes fast. The substrate semantics are UNCHANGED this round (status quo holds); no vision text changed on this point. The underlying product-semantics question (point-in-time-gap vs standing-concern OQ model) is logged for Vinga in VINGA_QUESTIONS as an override-invited flag, not a blocker — recurrence across two independent fresh-eyes rounds plus genuine framework tension (P5-Ironclad/M3 vs P11/P4/M1) makes the model choice hers. Recorded directly in decisions.md because the dogfood graph predates V1a and awaits cutover, so the buffer is the only writable surface this round; the amend edge reconciles on rebuild.
**Amends:** oq-stage2-resolution-stays-explicit-edge


### mutation-cycle-prevented-by-scalar-insertion-order-not-reachability-traversal

**Decided:** Mitos V1b enforces mutation-graph acyclicity with an O(1) scalar write-time check — every supersedes/amends/narrows edge's source must be strictly newer than its target on a tie-free insertion-order key (a rowid or a monotonic sequence — a strict total order) — rather than a recursive-CTE reachability traversal; the strict high→low ordering makes the supersedes ∪ amends ∪ narrows union acyclic by mathematical construction and enforces the new→old directional invariant directly.
**Rejected:** The recursive-CTE write-time reachability probe (the prior vision mechanism under mutation-cycle-prevented-at-write-time-not-read-defended): rejected as both more expensive and strictly weaker — a reachability probe only rejects an edge that CLOSES a cycle and silently accepts a direction-violating old→new edge that does not yet close one (a latent breach of the new→old invariant the prose already claims), whereas the scalar rejects every old→new mutation edge on sight, enforcing direction AND yielding acyclicity (including every mixed cross-kind cycle, e.g. A supersedes B, B amends C, C narrows A) as a free corollary with no union traversal. The parent ADR's rejected-path #2 over-claimed that "a genuine write-time reachability traversal is required"; this refutes that premise — the directional invariant is enforceable as an O(1) scalar comparison, so no traversal is required. A coarse created_at compared with >=: rejected because created_at can tie within a tick, letting a same-tick pair pass and sneak a cycle through; the key MUST be a strict total order, compared strictly. Dropping get_lineage's loud homeostasis bound now that cycles are scalar-prevented: rejected — the scalar check is app-layer (as the probe was), so an out-of-band / corrupt SQLite edit can still present a cycle the write path never authored; get-lineage-carries-loud-homeostasis-cycle-bound survives untouched as the read-time defense-in-depth.
**Mechanisms:** rowid, insertion-order, supersedes, amends, narrows, get-lineage, cycle-violation
**Scope:** edges, store, substrate, retrieval
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-21, Gemini's Finding 3 (Complexity Elimination lens). Amends mutation-cycle-prevented-at-write-time-not-read-defended — the core decision (acyclic-by-construction at write time, over the union, get_lineage carries no correctness-cycle-defense) STANDS; this refines only the mechanism (reachability traversal → scalar insertion-order invariant) and corrects the parent's "a reachability traversal is required" over-claim. Genuinely new ground, not in the parent's rejected set (which covered read-time defense, extend-V1a's-kill-edge-guard, and per-type checks). Also restores vision altitude — the prior text over-specified a recursive-CTE. A Claude+Gemini loop ADR, framework-settled (P17/P19). Recorded directly in decisions.md because the dogfood graph predates V1a and awaits cutover, so the buffer is the only writable surface this round; the amend edge reconciles on rebuild. Applied to vision §4.3 (Cycle prevention), §1.2 DoD #11, §6.1 clusters 1 and 4, §10 P16/P17.
**Amends:** mutation-cycle-prevented-at-write-time-not-read-defended


### questions-md-rotates-as-write-buffer-with-minimal-oq-visibility

**Decided:** Mitos V1b makes questions.md a write-buffer that rotates on successful sync exactly like decisions.md (synced open-question entries leave the buffer for the archive; the graph is their source of truth), not a standing worklist, and pairs the rotation with a minimal CLI read over oq_state_view (a list verb or a sync-end active-OQ summary) so the user keeps v0.1 open-question visibility without a V4 markdown render.
**Rejected:** Leave questions.md a standing document the user manages (the prior vision §7 framing): rejected — a standing questions.md invites the user to prune OQs to tidy it, but the no-graph-GC rule (§1.1) keeps the dropped OQ node, so it sits parked forever and floods V5's open-questions channel: a silent workspace-vs-graph divergence (the deletion-divergence trap). M7 (input docs are buffers / render targets, not managed sources of truth) and M3 (OQ state is computed from the graph, not from doc membership) both say questions.md must be a rotating buffer, and the Q1 ADR already calls it a "buffer" — so §7's "stays in questions.md / does not rotate" was vision-text drift from the recorded buffer framing, corrected here. Drag V4's markdown open-questions render into V1b to provide visibility: rejected as scope creep — the human-readable open-questions render (live_questions / per-scope sub-section) is V4/V5; a trivial CLI read over the oq_state_view V1b already ships bridges the v0.1 visibility gap without it. Rotate AND add stale-entry detection (>14 days) for questions.md now: not done — the >14-day stale-pending flag is a separate lifecycle feature, still deferred; only rotation (the buffer semantics + trap fix) is in V1b scope, and the full sweep of long-resolved questions out of the archive is Vision 7's Lifecycle Gardener.
**Mechanisms:** questions-md, oq-state-view, rotation, write-buffer, sync
**Scope:** sync, store, cli, substrate
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-21, Gemini's Finding 2 (the deletion-divergence trap a standing questions.md invites). Claude agreed the trap is real and M7/M3 settle the direction, surfaced the wrinkle that rotating questions.md empty with no v0.1 open-questions render strands user visibility (decisions.md rotation works only because live_axioms.md ships), and converged with Gemini on the minimal CLI read (list verb or sync-end summary) as the lean bridge that keeps the V4 render out of V1b. Consistent with — and citing — open-questions-authored-in-separate-questions-md-file, which already frames questions.md as a buffer. A Claude+Gemini loop ADR, framework-settled by M7, not a Vinga call. Recorded directly in decisions.md because the dogfood graph predates V1a and awaits cutover; the cite edge reconciles on rebuild. Applied to vision §7, §6.1 cluster 3, §5.1 C3.
**Cites:** open-questions-authored-in-separate-questions-md-file


### node-handles-validation-matches-slug-write-side

**Decided:** Mitos's Aliases: handles pass the identical write-side validation as a minted slug — V1a's r1/r2 character-class + Unicode discipline — not merely the identical casefold; one handle namespace, one legality rule-set, so an alias can never admit a handle a slug could not and a malformed alias value cannot slip past the field-name-recognition parser.
**Rejected:** Validate the fold but not the handle's legality (the gap the prior vision left — §8.2 pinned handle_casefold = slug fold via node-handles-fold-matches-slug-casefold-exactly but said nothing about char-class / Unicode validation): rejected — an alias is just a handle claiming a spot in the slug ∪ alias namespace, so skipping slug validation lets an alias admit a handle a slug never could (an asymmetric namespace), and a weird alias value (an embedded colon / newline / illegal Unicode) could confuse the r2 field-name-recognition regex and be mis-parsed as a field, corrupting the entry. The fold-parity ADR closes silent citation CAPTURE; this closes malformed-handle ADMISSION — the sibling integrity hole.
**Mechanisms:** node-handles, slug-casefold, slug-validation, parser, handle-casefold
**Scope:** identity, store, substrate, parser
**Context:** V1b parser-and-store vision-loop paired review (Claude + Gemini), 2026-06-21, Claude's own finding (Gemini concurred 100%). Sibling to node-handles-fold-matches-slug-casefold-exactly: that ADR pins fold parity (so a slug and a same-string alias collide under the UNIQUE index); this pins validation parity (so the namespace admits exactly the handles a slug could). Distinct axiom (validation vs fold), so recorded as a citing sibling rather than an amend. Recorded directly in decisions.md because the dogfood graph predates V1a and awaits cutover; the cite edge reconciles on rebuild. Applied to vision §4.1, §8.2, §6.1 cluster 6.
**Cites:** node-handles-fold-matches-slug-casefold-exactly

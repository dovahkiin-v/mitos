# Completed

## 2026-07-23

- **`v-mitos-record-write-pause-20260719` — Record write-pause rework: the dominant write path becomes agent-autonomous (the ★-stance delivery for `record_decision`).** `record_decision` commits directly, bypassing sync, so the shipped Conflict sensor never touched the maintainer's real write path — this vision gave it its conflict coverage, **agent-judged, with no LLM anywhere on the write path** (ADR `record-conflict-backstop-agent-judged-no-write-path-judge`, Vinga's Q1=B call; `mitos check` stays the sole judge consumer for the [0.76, 0.80) tail and already-landed contradictions). **The enriched pause:** neighbour discovery migrated off the pause-local `_review_neighbors`/`_adjacent_decisions` scan onto the Vision-1 `gather_candidates` + `screen_candidates` (OQ-safe kind filter, live `active ∪ drifted` re-verify, floored at the 0.80 pause threshold vs the sensor's 0.76 — a deliberate consumer-profile asymmetry), layering the pause's own broad declared-target exemption (all relations ∪ transitive lineage suppression — a declared weak-edge `cites` target never re-pauses) and per-survivor `candidate_payload`: each neighbour surfaces its **axiom + `rejected_paths` + `scope` + score + `amended_by`/`narrowed_by` modifier stamps** — a **stamped decision-read surface** (ADR `record-pause-neighbors-are-stamped-decision-read-surface`, superseding the recall-pointer classification) the authoring agent judges tenability from in-flow, attaching the resolving edge or acknowledging; the pause never picks the verb (★ invariant). **Deleted outright:** the `possible_tension` polarity heuristic (`_polarity_mismatch`/`_has_negation`/`_NEGATION_CUES` — the #34 mislabel source) with *nothing guessed in its place*, and the post-commit `related` recall echo + the orphaned `_adjacent_decisions` scan (Vinga's Q2=B call; ADR `related-echo-deleted-pause-composes-gather-directly`) — which is also the P17 fix for the scan's latent open-question `KeyError` whose blanket catch silently emptied the whole pause (regression-pinned: an active-OQ near-neighbour leaves the pause populated, never `[]`). **Honest degradation (fail-open both fault classes):** typed `Unavailable` pass-through + a call-site catch of exactly `(DatabaseError, ValidationError)` → commit proceeds with a calm `neighbor_review_unavailable` receipt notice — "couldn't check" never reads as "checked, clean"; clean-empty carries no notice. Protocol text rewritten on both surfaces from the shared seam (calm imperative, no tension label), CLI render enriched and scannable (axiom 60 chars / rejected 80 / scope untruncated, stamp marker on the head line), CLI⇄MCP parity pinned (`test_cli_mcp_record_pause_parity`, key-order contractual). Phase B byte-untouched across the whole vision (verified by diff at review); no new telemetry — `surface='record'` stays reserved-unused; no new dependency (no Anthropic key on the write path). 5 leaf phases (1a–3a), all 8 Wiring Contract rows + 8 e2e rows SATISFIED with verified evidence (re-verified at vision review — zero false claims); full suite green at review (**1518 passed / 11 legitimate skips / 0 failures** + packaging lane 2/2; the 4 environmental skips were live-lane quota/timeout — both live lanes ran green in the 3a walk same-day). T8 live dogfood on the real 228-node corpus: a close paraphrase paused at **0.9707** with zero residue (md5 + node/edge counts identical), no-LLM proven structurally (sole `run_conflict_check` site = sync surface) *and* at runtime (`sys.modules` probe); 2a's own ADR record genuinely paused live at 0.8001 and was resolved from the enriched payload alone with `--cites`. Five superseded/superseding ADRs verified closed in the graph. One review finding, fixed inline: a stale comment citing the superseded recall-pointers ADR for `edges_created`. Shipped at **`mitos 0.10.0`** (one minor bump at merge per `vision-phases-on-branch-one-bump-at-merge`; merge `1256fa7`). Deferred by design: clear-by-reference (stateful held-draft **permanently foreclosed** by ADR `near-dup-clear-by-reference-stays-stateless-no-held-draft`; stateless hash-guard an open option), write-path tombstone/graveyard surfacing (recall/confidence-semantics vision), inline-hydrating stamps with the modifier's axiom (evidence-gated), pause-floor retuning (the 0.80–0.85 👁-watch owns the evidence).

## 2026-07-07

- **`v-mitos-conflict-check-cli-20260705` — `mitos check`: the corpus-wide conflict audit + `--staged` CI gate (v0.2 roadmap "Vision 1b"; the zero-MCP / autonomous consumer of the shipped Conflict judgment core).** A user- and agent-invokable CLI verb that runs the shipped deliverable-1 pipeline in two new shapes, **additively — no signature break anywhere** (`gather_candidates` / `screen_candidates` / `run_conflict_check` / the renderer / the executor all keep their shipped forms; the new composition lives in a dep-free Tier-1 leaf `mitos/check.py`). **Corpus mode** (default) sweeps the live decision set: node-shaped proposal projection reusing `judge_input_from_node`, an **id-native either-direction strong-edge pair screen** (drops any pair joined by a `supersedes/amends/narrows/contradicts/corrects` edge in *either* direction — structurally removing the *declared* half of the 4b-measured global↔scoped narrows FP class, the measured harbor pair never reaching the judge), unordered-pair dedup with content-hash orientation, oriented-proposal batch grouping, and **verdict reuse** from the day-1 `conflict_checks` corpus keyed on `{hash_lo, hash_hi, prompt_version, model_alias}` (latest-`created_at` wins) so an unchanged corpus re-checks at **zero** LLM spend (M1 froze the axioms; a prompt/tier bump invalidates reuse by construction — the mechanism that lets the scheduled role eval ship as a swap, not a migration). **`--staged` mode** consumes the sync-time facade `run_conflict_check` verbatim over the pending `decisions.md` buffer as a pre-commit / CI gate. **Fail-closed** disposition (the opposite of sync's fail-open): a `0/1/2` exit contract — 0 clean-or-known-only, 1 a **NEW** contradiction (corpus: the CHK-D10 novelty gate; staged: any finding), 2 degraded/partial/refused (2 dominates 1) — with a `pending_embeddings` stale-index probe at run start **and** end (completeness certified only when both read clean), a poison-row escape (over-tolerance backlog disclosed as coverage exclusions, never a permanent red gate), and a CHK-D5 spend preflight (the two-stage plan/execute seam makes the judge structurally unreachable before the disclosure count exists; headless-above-threshold-without-`--yes` exits 2 zero-spend). The **novelty gate** reddens only on genuinely-new findings (a finding is *known* iff the latest prior verdict for its pair already re-derived as a finding — a standing contradiction rides a non-gating "standing (previously reported)" section, so a cron/agent gate goes red once per new tension, not forever) with **no suppression store** (the partition is derived, zero-bookkeeping). One additive telemetry rung: a **`surface`** column (`'sync'`/`'check'`, `'record'` reserved for the next vision), a provenance-only `judgment_batches.model_id`, and a **`check_runs`** summary table (the P18 repetition memory — standing-count trend, derived time-to-resolution / nag-count, the FP-suspect list the role eval consumes). The check **writes nothing but telemetry** (DoD-6 no-write lint extended over the new module family — behavioral `_WriteSpyStore` proof + an exact-set static AST closure). Prompt caching **declined honestly** (the static prefix sits below Anthropic's minimum cacheable length, so the flip would silently no-op — re-homed to the role eval; caching stays off, asserted). The v0.2 "autonomous sweep" ships as the **agent-block habit line** (run `mitos check` after a `record_decision` burst) + cron/pre-commit recipes — **no daemon**. 12 leaf phases (1a–5b); all 12 Wiring & Integration Contract rows (W1–W12) + 13 e2e rows (T1–T13) SATISFIED with verified evidence; full deterministic suite green at vision review (**1433 passed / 8 legitimate live-gated skips / 0 failures**), live golden + hook suite green on real Gemini+Anthropic+Qdrant (**12 passed / 1 baseline-reseed skip**) independently re-confirming DoD-1..4; the dogfood walk on the real 188-decision corpus **caught a genuine standing `record_decision`-landed contradiction** (`parser-comment-skip-matches-reserved-namespace-only` ✗ `html-comment-skip-markdown-aware`, confidence 0.93) at zero re-run spend. **Pre-existing bug fixed in-flight (not this vision's debt):** the shipped `gather_candidates` had no `kind=="decision"` filter, so a resolved open-question indexed near a decision crashed `mitos check` *and* sync-time `run_conflict_check` with `KeyError: 'core_axiom'` (a resolved OQ reports `get_node_state=="active"` — kill-edge-only — and carries no `core_axiom`); the full-corpus golden fixtures surfaced it and it was fixed at the one gather site + a loud `ValueError` boundary guard, with regression tests (`0.7.3`). Shipped at **`mitos 0.7.3`** (minor 0.7.0 for the verb + patch bumps through the staged gate, habit line, and the OQ fix). Still deferred behind passed Retrofit Tests: the record-time backstop, the `check_conflict` MCP tool, staged verdict reuse, telemetry retention, and the prompt-cache flip.

## 2026-07-03

- **`v-mitos-conflict-sensor-20260613` — The Conflict sensor: sync-time contradiction detection (v0.2 Vision 1; lights up the third dossier sensor).** An **advisory, read-side** capability wired to exactly one surface — the `mitos sync` per-entry review prompt: given a proposed decision axiom it gathers scope-blind embedding nearest-neighbours (CONF-D2 — `is_query=False` document-space, single bounded over-fetch, per-node computed-state re-verify so a stale vector never yields a retired node), drops the entry's declared strong-relationship targets (`{supersedes, amends, narrows, contradicts, corrects}`) + its own slug (the false-self-conflict guard), floor-gates the survivors at the calibrated `CONFLICT_SIMILARITY_FLOOR = 0.76`, judges the batch for `tenable_together` in **one batched Claude `SONNET` call** at temp 0.3 (CONF-D3/D5, rationale emitted before the gate — the chain-of-thought lever), and **surfaces a not-tenable finding at the accept prompt only at confidence ≥ 0.85** (CONF-D4). The sensor **never writes** to `nodes`/`edges`/`decisions.md` and **never blocks a commit** — accept-anyway always commits verbatim/undeclared; resolve = declare a relationship in `decisions.md` + re-sync (the declared candidate is then dropped); quit aborts. `--yes`/`auto_accept` skips the check entirely (no prompt to fire). Every judged pair persists one verbatim, replay-idempotent row (both sides' fed axiom/rejected_paths/scope, `proposed_hash_if_any` = `compute_node_id` join key, tenable/confidence/surfaced, batch-attributed `token_usage`+`elapsed_ms`, `sync_run_id`, rationale) to a **dedicated `conflict_checks` table in a sibling `.mitos/telemetry.sqlite`** (CONF-D8, its own migration ladder, survives `mitos rebuild`/`rm graph.sqlite`) — the day-1 corpus the future CONF-C1 learning layer graduates on, with `resolution` derived from `edges` at consumption (never a second row class). Degradation is fail-open (CONF-D10): Qdrant/embedding/judgment-LLM unreachable or past the 15 s hard cap → loud interactive notice, no row, commit untouched; an aggregate breaker caps a downstream outage at one penalty per run, not N. The reusable **library pipeline** (candidate-gather → tenability judgment → confidence gate) is the core Vision 1b's `mitos check` + the deferred record-time backstop consume additively (§7 Retrofit Test). 10 leaf phases (1a–6b), all 18 Wiring & Integration Contract rows (W1–W10, T1–T8) SATISFIED with verified evidence; golden Layer-B `conflict:` fixtures + Part-C metrics (not-tenable precision/recall, same-polarity FP rate, confidence-calibration curve) added, floor empirically calibrated recall-first, the `CONFLICT_SURFACE_THRESHOLD = 0.85` pin validated. Full suite green at vision review + remediation verification (1236 passed / 13 legitimate skips / 0 failures, packaging lane separate). Shipped at **`mitos 0.6.0`** (minor — `mitos sync` gains a user-facing conflict check). No remediation phases: the three vision-review items forensically verified as real but correctly dispositioned tracked-deferrals/environmental — the global↔scoped-narrows judge FP (`not_tenable_precision = 0.75`, routed to the scheduled Sonnet-vs-Flash role eval — see the ⚠ follow-on in ROADMAP.md), the belt-and-suspenders live dogfood re-run (blocked on a spent Gemini embed quota; contradiction-surfacing already proven live at Phase 4a + golden Layer-B), and the ≤5-width P95 latency miss (~9–11 s, measured via `elapsed_ms`, under the 15 s hard cap, thinner in production). **Design boundary (Vinga, 2026-07-01):** this ships the *sync-time interactive* surface only; the maintainer's dominant write path (`record_decision`, which bypasses sync) gets its agent-autonomous conflict coverage in the later record-write-pause rework + Vision 1b's autonomous `mitos check` — both consume this vision's judgment core.

## 2026-07-01

- **`v-mitos-ax-ux-hardening-20260623` — AX/UX hardening of the agent-facing CLI/MCP surface (first v0.2 vision, sequenced before the Conflict sensor).** A plumbing-only, **zero-architectural-risk** pass — no schema, edge-catalog, identity/hash, or recall/ranking change — cleaning the agent surface before the Conflict sensor adds more on top. Drawn from the `AX_FEEDBACK.md` real-use log (mined 2026-06-22, re-verified against `0.5.2` 2026-06-23: the 42-item log collapsed to 6 confirmed-live items). **Delivered all 7:** (1) **scope-vocabulary discovery** — a dedicated `mitos scopes` verb (tag→two live counts: active decisions + parked OQs, busiest-first; `--archived` for the dead-domain spine) + its MCP twin `list_scopes`, plus an **unused-scope soft-recovery vector** (did-you-mean via stdlib `difflib` + top-K candidate slice + pointer) on `surface`/`list`/`open-questions`/`list_decisions` — exit-0, signal in-band on `--json`/MCP, never a hard-error; (2) **`--json` uniformity** on `query`/`show`/`open-questions` + `record`'s write receipt, with `--brief` parity on `query`; (3) **`--limit`** on the ranked surfaces (sets top-k, raise-or-lower) + an all-superseded blackout recovery vector; (4) **`ensure_ascii=False`** across every display emitter, crash-safe on a non-UTF-8 stdout (the named non-ASCII/Lithuanian gotcha); (5) **global `-C`/`--directory`** (git-style, one process-entry `chdir`, total retarget); (6) **`show` as a vector** — state-agnostic slug resolution (active-first, else most-recent superseded, marked) + a static-hedged `mitos sync` not-found pointer + its MCP twin `show_node` (exact-handle dereference of the superseded graveyard — the access-primitive `query_decisions`' active-view slug path can't reach); (7) **help-as-API-doc** — a worked-example `epilog` + alias-suppressed usage banner + terse MCP description parity. **MCP surface grew 4→6 tools** (`list_scopes` + `show_node`). DRY-first: two shared `display.py` leaves (`dumps_display`, `letter_payload`) make CLI⇄MCP parity *structural*; `get_scope_counts` reuses the read verbs' own liveness SQL so counts==verbs by construction. 14 leaf phases (1a–6a), every Wiring & Integration Contract row (14 W + 12 T) SATISFIED with verified evidence; full suite green at vision review (1003 passed / 9 legitimate skips / 0 failures, packaging lane included); no drift, no orphans. Shipped at `mitos 0.5.16` (per-phase patch bumps 0.5.5→0.5.16). The `record` multi-value relation-flag fix + sync-strict-deterministic prerequisites shipped separately (0.5.3/0.5.4), not deliverables here. Deferred by design: semantic graveyard recall + the near-dup-pause clear-by-reference fix (with/after the Conflict sensor); the EXTRA_CONTEXT optional polish + `capture --json` hard-deferred.

## 2026-06-23

- **`mitos rebuild` — corpus rebuild & upgrade path (`0.5.0`–`0.5.2`).** A new CLI verb that re-commits the full corpus (`decisions.md` + the quarterly archives) oldest-first through the current edge catalog + mechanism registry into a build-aside graph, atomically swapped in (old graph backed up to `graph.sqlite.bak_<ts>`). Closes the in-place-upgrade gap: a schema-ladder migration widens the DDL but never re-commits, so the V1b catalog stays empty until a rebuild — and steady-state `sync` structurally can't backfill it (reads the buffer alone; idempotency-skips unchanged cores). Reuses the cutover engine: the per-entry quarantine + intra-sync fixpoint extracted to `mitos/replay.py` as a shared primitive; `rebuild_and_gate(strict=…)` per-caller policy keeps `cutover` strict (aborts on a defect) and makes `rebuild` resilient (casualties surfaced as a guided per-class punch-list, swap refused unless `--allow-drops`, never silent loss). The completeness gate generalized to baseline against the current graph's **presence** set — a node the rebuild correctly *retires* (a supersession the stale graph never saw) is retained-but-inactive, not flagged as dropped (`0.5.2` fix). `mitos status` nudges when a rebuild is due. No ADRs at risk — the markdown is the source of truth (M7/P6); the swap is reversible. 14 tests in `tests/test_rebuild.py`; the design recorded as 3 ADRs in `decisions.md`. Verified end-to-end on the live corpus (138 committed, full catalog populated, the one stale citation it surfaced now fixed).
- **`v-mitos-parser-and-store-v1b-20260519` — Catalog completion + mechanism registry + OQ Stage-2 (closes the v0.1 parser-and-store substrate).** The warn-defer→commit flip for the seven non-kill edge types (`amends`, `narrows`, `depends_on`, `contradicts`, `derives_from`, `cites`, `resolves`) — including the three cross-kind shapes (`cites` any→any, `derives_from` OQ→D, `resolves` D→OQ) landed via a faithful `edges` kind-CHECK widening rebuild, comma-separated multi-valued relationship fields; the `mechanisms` entity registry (first-seen-wins identity, `canonical_name` PK) with auto-registration + a pre-commit feedback read; OQ Stage-2 resolution (`oq_state_view` computed `state ∈ {parked, resolved}` + self-healing, all derived at query time) with steady-state `questions.md` ingestion (per-entry whole-`CommitError`-class quarantine + intra-sync fixpoint retry converging any acyclic cross-file chain in one sync) and a minimal CLI OQ-visibility read; `get_lineage` (the `supersedes ∪ amends ∪ narrows` mutation-union walk) feeding a write-time reachability cycle guard and transitive near-dup suppression off one shared traversal; the `source_reencounter` signal-write path; modifier-stamping light-up with a source-liveness filter (no dead amender ghost-stamps a live target); successor-less death modelled as a `supersedes` deletion decision (no `[RETIRED]` marker); and a pre-ladder DB snapshot as the populated migration's binary reversal. All 15 Definition-of-Done gates closed; the Wiring & Integration Contract reached zero OPEN / zero false-SATISFIED rows. **Scope departures from the v0.1 plan (both settled during the vision):** the `slug_aliases` citation-rot subsystem was **stripped, not built** (a renamed-away slug breaks loud as `missing_target`; deprecation-with-history is a `supersedes` deletion decision); and **no render cascade ships — V4 full-regenerates** from the computed active view (MI-6 dissolved), with the committing-node-only embedding Outbox inherited unchanged. Shipped at `mitos 0.4.0`. Two test-only remediation cycles closed clean: cycle 1 (r1 stale-skip hygiene, r2 live-suite environmental-failure→LOUD-skip robustness via a single-sourced `tests/live_helpers.py`) and cycle 2 (r3 generative-enrichment-quota active-probe + dead-import prune) — final gate 808 keyless passed / 4 documented skips / 0 failed, live integration 25 passed / 2 loud environmental skips. Delivers v0.1 must-include #2 (full typed-edge catalog) and #3 (mechanism registry). Carried forward for Vinga (not remediation): refresh the global pipx install `0.3.2`→`0.4.0`, the `INVARIANTS.md` MI-6/MI-12/MI-2 reconciliation, and her uncommitted `decisions.md` ADRs.

## 2026-06-18

- **`v-mitos-parser-and-store-v1a-20260519` — Substrate floor of v0.1.** Hardening rebuild of the prototype substrate: the strict deterministic runtime parser (`decisions.md` + `questions.md`, kind-by-file), the SQLite graph store with slug-free content-hash identity (slug is a mutable handle; same-core entries converge), the two kill-edges (`supersedes` + `corrects`) wiring the computed active-decisions view, the `PRAGMA user_version` migration ladder, the `pending_embeddings` outbox, modifier stamping, the `tomllib` config loader, `mitos init`/`status` on the v0.1 schema, and the build-aside + completeness-gate + atomic-swap cutover tooling (`mitos cutover`, fixture-proven). Stands up single-lane CI with the MI-1 golden-trace byte-equality gate. Shipped at `mitos 0.2.0`. Remediation cycle 1 (r1 write-side slug casefold discipline, r2 parser field-regex hardening) closed clean — 602 passed / 23 documented skips / 0 failed, CI green. Backs v0.1 must-include #1 (parser) and the identity + kill-edge substrate of #2; the remaining edge catalog, mechanism registry, and cascade wiring carry to the sibling V1b vision.
